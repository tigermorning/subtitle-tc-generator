# 규정(`rules/netflix/common.yaml`·`en-translation.yaml`)이 비워 둔 자리를
# 실제 자막에서 관측한 값으로 채운다. 규칙 11 — `origin: learned`은 지우지 않는다.
#
# 2026-08-26: 드라마A(6부작) + 예능B(8부작) + 예능A
# 시즌2(2회차 — 15·16회. 19회는 파일 손상으로 뺌). 셋 다 **한국어 원어 →
# 영어 번역**(진짜 번역 방향). 대괄호 화자 표시가 없어 번역(dialogue-only)로
# 분류했다 — SDH는 `ko-sdh.yaml`에 있다.
#
# 예능B는 트랙에 제목이 없어서 후보가 여럿이었는데, **사용자가 실제
# 넷플릭스 화면 스크린샷으로 어느 트랙이 진짜 영어 자막(CC 아님)인지
# 확인해 줬다**(2026-08-26). 예능A는 트랙 제목이 명확해서(`ENG`) 문제없었다.
#
# 규정이 이미 값을 정한 항목(줄당 글자 수 42자, 읽기 속도 20자/초)은 여기 없다.

schema_version: 1
platform: netflix
language: en
kind: translation
source:
  official: false
  origin: learned
  from:
    작품:
      - {제목: "드라마A S01", 형태: "드라마 6부작", 원어: 한국어, 줄_합계: 3930}
      - {제목: "예능B S01", 형태: "예능 8부작", 원어: 한국어, 줄_합계: 12460}
      - {제목: "예능A", 형태: "드라마 2회차(15·16회)", 원어: 한국어, 줄_합계: 2334}
    줄_합계: 18724
  measured: "2026-08-26"

observed:
  # 2026-09-08 추가. `chars_per_cue`가 생기기 전에 뽑힌 파일이라 이 값만
  # 없었다 — `resplit.py`가 학습값을 소비하는 경로(T14)는 이 칸을 보므로,
  # 없으면 그 경로가 이 조합에서 아무 일도 하지 않는다. **같은 정답지에서
  # 다시 쟀고**(영상은 없어도 `.srt`는 남아 있다), 기존 항목을 함께 다시
  # 재서 적힌 값과 전부 일치하는 것을 확인했다(같은 자료라는 증명 —
  # `tools/learned_chars_per_cue.py`, 자막 18724개).
  chars_per_cue:
    중앙값: 28.0
    p95: 54.0
    최대: 82.0
  duration_ms:
    중앙값: 1752
    p5: 868
    p95: 3670
    범위: [833, 7007]
  gap_ms:
    중앙값: 84
    p5: 66
  lines:
    한_줄: 13555
    두_줄: 5169
    세_줄_이상: 0
    전체_대사자막: 18724
