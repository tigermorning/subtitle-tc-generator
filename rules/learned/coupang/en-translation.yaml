# `rules/private/coupang/`에 영어 프로파일이 없다(한국어만 있다) — 추측해서 만들지
# 않는다(규칙 9). 순수 관측이다.
#
# 2026-08-26: 드라마E E05(쿠팡플레이), 한국어
# 원어 → 영어 번역. 회차 1개뿐.

schema_version: 1
platform: coupang
language: en
kind: translation
source:
  official: false
  origin: learned
  from:
    작품:
      - {제목: "드라마E E05", 형태: "드라마 1화",
         원어: 한국어, 줄_합계: 532}
  measured: "2026-08-26"

observed:
  duration_ms:
    중앙값: 1793
    p5: 959
    p95: 3670
    범위: [834, 6006]
    # 2026-09-11 추가(`tools/learned_short_cue_floor.py`로 같은 정답지에서 잼).
    # 규정은 표시 시간의 하한만 정한다 — 실무는 말이 짧을수록 아웃점을 더 늘려
    # 약 1초 언저리에 자막을 세운다(드라마B E02·E03 정답 대조에서 발견, 발주처
    # 넷 이상에서 같은 방향). `generate()`가 생성 경로에서만 이 중앙값을 최소
    # 표시 시간으로 쓴다(`timing.TimingLimits.preferred_min_duration_ms`).
    # 검사 경로는 규정 하한 그대로다.
    짧은자막:
      기준: 가중 글자 수 6 이하, 효과음 전용 자막 제외
      # 표본이 적다 — 규칙 12(최소 2편)에 못 미치는 참고값
      개수: 19
      중앙값: 1001
      p25: 917
      p75: 1085
  gap_ms:
    중앙값: 959
    p5: 83
  chars_per_line_unweighted:
    중앙값: 24.0
    p95: 39.0
    최대: 42.0
  cps_unweighted:
    중앙값: 13.8
    p95: 24.0
  lines:
    한_줄: 397
    두_줄: 135
    세_줄_이상: 0
    전체_대사자막: 532
