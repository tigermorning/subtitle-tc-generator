# AGENTS.md — 이 저장소에서 작업하는 AI 도구를 위한 입구

`AGENTS.md`는 여러 도구(opencode, Codex, Curson 등)가 **자동으로 읽는** 파일이다.
`CLAUDE.md`는 Claude Code만 읽으므로, 규칙이 도구에 따라 빠지지 않게 여기서 가리킨다.

## 이 프로젝트가 무엇인가

**자막 및 TC 생성기.** 전문 번역가·자막 작업자용 도구다. 영상 하나에서
전사 → 대조 → 번역 → 재분할 → 스포팅까지 이어 붙인다.

- 저장소: `subtitle-tc-generator` (로컬 폴더 이름도 2026-08-14에 맞췄다)
- 한국어 교정은 **여기서 하지 않는다.** 옆 프로젝트
  [korean-subtitle-corrector](https://github.com/tigermorning/korean-subtitle-corrector)를
  라이브러리로 부른다(`checker/korean.py`). 자막 지식은 여기가, 한국어 규범 지식은
  그쪽이 갖는다.
- 최상위 목표: **전문가가 신뢰하고 쓸 수 있으며 전문가의 시간 낭비를 줄이는 도구.**
  우선순위는 **자동 교정 오답 0 > 플래그 건수 감소 > 선택적 recall**이다.
  이 우선순위가 설계 결정을 여러 번 뒤집었다.

## 시작하기 전에 읽을 것 — 순서대로

1. **`CLAUDE.md`** — 작업 규칙 10개. **전부 한 번씩 틀렸다가 사용자 지적으로
   바로잡은 것들이다.** 새 기능을 붙이기 전에 반드시 읽는다.
2. **`docs/BACKLOG.md`** — §1 큰 그림, §2 끝난 것, §3 남은 작업(순서와 이유),
   §4 결정 대기, §6 되풀이하지 말 것.
3. `checker/pipeline.py` 독스트링 — 단계 순서를 왜 그렇게 정했는지.

## 기준선

```bash
python3 tests/run_tests.py
```

**"전부 통과"를 확인하고 시작한다.** GUI(`app/`)를 건드리면 PySide6가 있는
파이썬으로도 돌린다 — 시스템 `python3`에는 PySide6가 없어서 GUI 시험이 조용히
건너뛰어진다.

```bash
cmd.exe /c "..\korean-subtitle-corrector\.venv\Scripts\python.exe tests\run_tests.py"
```

## 넘어가기 쉬운 함정 셋

`CLAUDE.md`와 `docs/BACKLOG.md §6`에 전부 있지만, 이 셋은 실제로 여러 번 당했다.

1. **테스트를 파이프나 세미콜론으로 잇지 않는다.**
   `python3 tests/run_tests.py > /dev/null && git commit ...` 꼴로 쓴다.
   파이프(`| tail`)는 종료 코드를 가리고, 세미콜론(`;`)은 실패해도 다음 줄을
   실행한다. 둘 다로 실패한 테스트를 커밋한 적이 있다 — **세 번**이다.
2. **`docs/BACKLOG.md §3`의 순서를 뒤집지 않는다.** 순서에 이유가 적혀 있다.
   예: T17(존댓말·반말 혼용 검사)은 캐릭터 시트보다 뒤다 — 같은 화자가 말투를
   섞는 것은 **상대가 다르면 정상**이고, 자막에 상대는 표시되지 않으므로 관계를
   모르는 채 지적하면 오답이 된다.
3. **"만들지 않기로 한 것"을 빠진 것으로 보지 않는다.** §3에 이유와 함께 적혀
   있다(멜로·느와르 장르 프로파일, 다큐 `요` 줄 단위 검사, 근거 없는 임계값).
   뒤집어야 한다고 생각하면 **먼저 묻는다.**

## 답은 한국어로 한다

사용자가 한국어로 쓴다. 코드 주석과 커밋 메시지도 한국어다.
