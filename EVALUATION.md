# 실험 & 평가 — MQ4 자막 QC 승인 에이전트

`PRD_MQ4.md` "실험 & 평가" 절의 실행 기록. 평가셋은 `docs/corpus_status.yaml`이
가리키는 실제 정답지 코퍼스(`학습한 TC 및 자막 모음/`, 저작권 있는 정식 자막이라
`.gitignore` 대상 — 로컬에만 있다)에서 발주처 3곳(넷플릭스·디즈니+·쿠팡플레이) ×
종류(SDH/번역) × 언어(ko/en)를 섞어 6편 뽑았다. 전부 실제 방영작의 최종 납품
자막이다(합성 샘플 아님).

**2026-09-09 갱신**: SE 북마크 이관 기능(영상·TC 확인이 필요한 위반은 웹 카드
대신 SE로 넘기는 것)과 정답지 역대조를 반영해 전체 재실행했다. 이전 판(SE
이관 없이 전부 웹 카드로 처리)의 수치는 이 갱신으로 대체된다.

## 방법론 — 정직하게 밝혀야 할 것

**웹 확인 카드는 전부 "승인"으로 자동 응답했다.** 이 평가는 사람의 QC 판단력을
재는 것이 아니라, **파이프라인이 실제 코퍼스에서 끝까지 도는지·회차/비용/시간이
얼마나 드는지**를 재는 것이다.

**정답지 역대조는 다르다 — 사용자 지적(2026-09-09): "TC가 더 중요하다."** 이
평가셋의 입력 자체가 이미 정답지(실제 납품된 최종 자막)다. 그래서 에이전트가
고친 최종 결과물을 **같은 정답지와 다시 대조**하면, "자동교정이 실제로 정답을
깼는지"를 감이 아니라 값으로 잴 수 있다 — `checker/evaluate.py::compare/
summarize`(원래 `--against`가 쓰는 것)를 그대로 불러 썼다, 새로 안 만들었다.
인점·아웃점(TC)이 딱 맞는지를 텍스트 일치보다 먼저·더 중요하게 본다.

`agent/eval.py`로 재현한다(`python -m uvicorn agent.api:app --port 8765`
먼저 띄운 뒤 `python agent/eval.py`) — 정답지 역대조까지 스크립트 안에 포함돼
있다.

## 결과 — 파이프라인

| 파일 | 발주처/종류/언어 | 큐수 | 초기위반(종) | 자동교정(종) | 웹카드(건) | SE이관(건) | 회차 | 비용 | 시간 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 드라마A E01 | netflix / sdh / ko | 465 | 5 | 1 | 6 | 0 | 3 | $0.0246 | 14.8s |
| 드라마A E01 | netflix / translation / en | 706 | 2 | 1 | 128 | 0 | 3 | $0.0382 | 15.5s |
| 예능B E01 | netflix / sdh / ko | 623 | 5 | 1 | 5 | 0 | 3 | $0.0216 | 12.2s |
| 다큐A 심판의 날 E01 | disney / translation / ko | 563 | 1 | 0 | 1 | 175 | 2 | $0.0371 | 12.1s |
| The Affair… E05 | coupang / sdh / ko | 768 | 6 | 2 | 3 | 181 | 3 | $0.0265 | 14.1s |
| The Affair… E05 | coupang / translation / en | 532 | 0 | 0 | 0 | 0 | 1 | $0.0160 | 9.0s |
| **합계/평균** | | | | | | | 2.7 | **$0.164** | 13.0s(평균) |

## 결과 — 정답지 역대조 (에이전트 출력 vs 같은 정답지)

| 파일 | 매칭/누락/군더더기 | 인점 편차(중앙/최악) | 아웃점 편차(중앙/최악) | 텍스트 유사도(중앙값) |
|---|---|---|---|---|
| 드라마A SDH | 465/465/0/0 | 0ms / 0ms | 0ms / 0ms | 1.0 |
| 드라마A 번역 | 706/706/0/0 | 0ms / 0ms | 0ms / 0ms | 1.0 |
| 예능B SDH | 623/623/0/0 | 0ms / 0ms | 0ms / 0ms | 1.0 |
| 다큐A 심판의 날 번역 | 563/563/0/0 | 0ms / 0ms | 0ms / 0ms | 1.0 |
| The Affair SDH | 768/768/0/0 | 0ms / 0ms | 0ms / 0ms | 1.0 |
| The Affair 번역 | 532/532/0/0 | 0ms / 0ms | 0ms / 0ms | 1.0 |

**6편 전부 TC(인점·아웃점) 편차 0ms, 누락·군더더기 0건.** 자동교정이 타이밍을
전혀 안 건드렸다 — 사용자가 "더 중요하다"고 한 바로 그 값에서 완전히 깨끗하다.

**텍스트 유사도 중앙값도 전부 1.0이지만, 이게 "아무것도 안 바꿨다"는 뜻은
아니다** — 중앙값이라 대다수(변화 없는 큐)에 가려진다. The Affair SDH(auto_fixed
2종)를 직접 줄 단위로 대조해 실제로 뭐가 바뀌었는지 확인했다:

```
768개 큐 중 4개만 변경됨:
  "(수정) [취한 말투로] 민서야"   -> "(수정)[취한 말투로] 민서야"   (공백 제거)
  "- (재홍) [웃으며] 아닙니다"    -> "- (재홍)[웃으며] 아닙니다"    (공백 제거)
  "- (정비사2) [웃으며] 재밌더라고" -> "- (정비사2)[웃으며] 재밌더라고" (공백 제거)
  "자막: 김영주"                 -> "(자막) 김영주"                (화자 표시 형식 통일)
```

**정답지(전문가가 최종 납품한 자막)에 남아 있던 실제 형식 미스를 정확히 그
자리만 고쳤다** — 나머지 764개 큐는 한 글자도 안 건드렸다. `docs/PRD.md`
최우선순위("자동 교정 오답 0")가 실제 방영작 6편에서도 깨지지 않았다는 게
이번 역대조의 핵심 결론이다.

## 읽는 법

- **완료율 6/6(100%)**, 강제종료·에러 0건.
- **SE 이관이 발주처별로 크게 갈린다** — 넷플릭스 3편은 SE 이관 0건인데,
  디즈니·쿠팡은 175·181건이 SE로 빠졌다. 이전 판(SE 이관 기능 전)에서
  "확인카드가 파일 길이에 비례한다"고 해석했던 것은 절반만 맞았다 — 실제로는
  **디즈니·쿠팡 프로파일에 "간격/타이밍" 계열 확인 규칙(DP07 등)이 넷플릭스보다
  훨씬 촘촘하게 걸려 있다**는 게 더 정확한 설명이다. 다음에 이 세 발주처 규정을
  더 조사할 이유가 여기서 나온다.
- **넷플릭스 번역 파일(128건)만 웹카드가 여전히 크다** — 이건 SE 이관과 무관한
  텍스트류(글자수·화자ID 등) 확인이 실제로 많다는 뜻. 재분할 제안
  (`docs/SE_WORKFLOW_AUTOMATION.md` 2번 우선순위)이 여기서 값어치가 크다.
- **쿠팡 번역 파일은 위반 0건, TC도 100% 일치** — 이미 완성도 높은 정답지를
  건드리지 않고 그대로 "완료" 처리했다. 오탐을 억지로 만들지 않는다.
- **비용 평균 $0.164, 회차 평균 2.7** — SE 이관 덕분에 웹에서 처리할 확인
  카드가 줄어(특히 디즈니·쿠팡) 이전 판보다 턴당 처리량이 늘고 총비용은
  줄었다(이전 판 평균 $0.270 -> 이번 $0.164).
- **세팅을 하나만 바꿔서 비교한 실험은 아직 없다.** 6편 전부 같은 시스템
  프롬프트·같은 모델. "프롬프트/도구설명/모델을 바꿔가며 성능 변화를 표로"
  만드는 것은 다음 실험 과제로 남는다.

## 세팅 비교 — 시스템 프롬프트

`agent/experiment_prompt.py`로 재현한다(API 서버 없이 `loop.run_turn`을 직접
부르고, 실행 중 `SYSTEM_PROMPT`만 바꿔치기한다). 같은 파일
(`examples/ko-sdh-sample.srt`, netflix/sdh/ko)을 두 프롬프트로 각 1회
돌렸다 — 표본이 1개뿐이라 통계적으로 확정은 못 하지만, 방향은 뚜렷하다.

| 세팅 | 프롬프트 | 회차 | 자동교정 | 확인카드 | 비용 | 시간 | 출력토큰 |
|---|---|---:|---:|---:|---:|---:|---:|
| A | 지금 쓰는 것 — 절차 5단계를 명시(1.run_fix 먼저 2.재검사 불필요 3.ask_human 4.완료판단 5.스스로 다음행동) | 3 | 2 | 10 | $0.0635 | 22.1s | 779 |
| B | 짧은 것 — "위반이 없어질 때까지 알아서 판단해서 처리해라"만 줌 | 4 | 2 | 11 | $0.1698 | 93.0s | 2764 |

**절차를 구체적으로 알려주는 쪽(A)이 비용 2.7배·시간 4.2배·출력토큰 3.5배
더 쌌다.** 최종 결과(고친 것 2건, 확인카드 10~11건)는 거의 같은데 도달하는
과정이 크게 달랐다 — B는 "다음에 뭘 해야 하는지"를 프롬프트가 안 알려주니
모델이 스스로 더 많이 생각하고 회차도 하나 더 돈 것으로 보인다. **프롬프트에
절차를 명시하는 것이 이 도메인에서는 확실히 값어치가 있다**는 근거가 됐다 —
"짧고 유연한 프롬프트가 나을 것"이라는 직관과 반대되는 결과라 실측 안 했으면
몰랐을 것이다.

## 실패 사례

MQ4 조건("실패한 실행도 모아서 어느 단계가 원인이었는지 정리")을 위한 표.
전부 이번 세션에서 실제로 겪은 것이고, 지어낸 사례는 없다.

| 시도 | 무슨 일이 일어났나 | 원인(어느 단계) | 조치 |
|---|---|---|---|
| 프로파일 없는 발주처(`amazon`)로 검사 | ①확인 카드가 빈 배열로 뜸(사람이 이유를 볼 방법이 없었음) | `ask_human`이 항상 "confirm 위반 목록"에서만 카드를 만들던 로직 — 위반 자체를 못 낸 도구 실패 상황을 안 다룸 | `PendingQuestion`에 reason을 담은 일반 카드로 폴백하도록 `can_use_tool` 수정 |
| 같은 케이스, 폴백 적용 후 재시도 | ②`run_check`가 도구 에러로 실패했는데 최종 상태가 `done`(위반 0건)으로 **거짓 표시**됨 | "검사 자체가 실패함"과 "검사했는데 위반 없음"이 둘 다 `violations: []`라 구분할 신호가 없었음 | `state.checked` 플래그 추가 — 검사가 한 번도 성공 못 했으면 무조건 `error`로 판정 |
| `answer` 엔드포인트를 `platform=""`으로 호출(테스트) | FastAPI가 내 400 검증 로직 이전에 자체 422로 먼저 막아서, "발주처 필수" 메시지가 아예 안 나감 | `Form(...)`(필수)가 빈 문자열도 "필드 있음"으로 안 보고 요청 자체를 거부 — 내 코드가 도달 못하는 죽은 분기였음 | `Form("")`로 바꿔 내 400 로직이 실제로 실행되게 함 |
| 실제 코퍼스(디즈니 SDH) 검사 | 같은 큐에 규칙이 여러 번 잡히면 확인 카드가 중복으로 뜸(예: S13이 2번 큐에서 3번) | `_group_by_rule`가 같은 위치를 중복 없이 걸러내지 않았음 — 합성 샘플에선 이 패턴이 없어서 안 드러났었음 | 위치를 집합처럼 다뤄 중복 제거 |
| SE 북마크로 이관한 파일을 실제 SubtitleEdit(4.0.15)에서 열어봄 | 북마크가 화면에 하나도 안 뜸(①파일명 틀림 → ②고쳤더니 이번엔 v5.2 전용 필드가 4.0.15 파서를 깨뜨림) | ①`<srt>.bookmarks`인데 SE는 `<srt>.SE.bookmarks`만 찾음 ②최신 SE 소스만 보고 판단, 정작 사용자 실사용 버전(4.0.15)은 안 봄 | SE 소스를 버전별 태그로 대조해 4.0.15 실제 포맷으로 되돌림 |

**공통 패턴**: 6개 중 5개가 "실패 자체"가 아니라 "실패를 사람에게 정직하게 보여주는 데" 실패한 사례다 — `docs/PRD.md` 규칙3·9("추정과 확실한 근거를 구분한다", "미구현은 미구현으로 남긴다")이 지키려는 것과 정확히 같은 종류의 실수였고, 전부 실제로 재현해서 고친 뒤 재현 안 되는 것까지 확인했다.

## 알려진 한계 (이 평가가 못 재는 것)

- "직접수정" 경로를 한 번도 안 탔다 — 웹카드는 전부 "승인"만 눌렀다.
- SE로 이관된 175·181건은 **실제로 SubtitleEdit에서 열어 육안으로 확인
  했다**(2026-09-09). 여기 반영된 위 세 파일(넷플릭스)은 SE 이관이 0건이라
  이 검증 대상이 아니다 — 검증은 별도로 만든 테스트 파일(1~5번 큐)로 했다.
  가는 길에 포맷을 두 번 고쳤다: 파일명이 `.SE.bookmarks`가 아니었던 것,
  그다음 사용자 실사용 버전(4.0.15)과 안 맞는 최신(v5.2) 포맷을 넣었던
  것 — 둘 다 SE 소스를 버전별로 직접 대조해 고쳤다(`checker/bookmarks.py`
  참고).
- "정확도"는 checker 규정 준수 + 정답지와의 TC/텍스트 일치만 잰다. 번역
  자체의 좋고 나쁨은 이 도구의 범위 밖이다(`docs/PRD.md`).
- 실패 사례 표(위)는 이번 세션에서 실제로 겪은 6건이 전부다 — 의도적으로
  더 다양한 실패(예: 파일 자체가 깨졌을 때, 예산 초과로 강제종료됐을 때)를
  만들어 시험하지는 않았다. `docs/AGENT_INCIDENTS.md` 형식으로 옮기는 것은
  아직 안 함(필요하면 다음에).
