# PRD — Main Quest 4: 자막 QC 승인 에이전트

이 문서는 [Main Quest 4](https://app.notion.com/p/Main-Quest-4-3d58820152dc80318287cf325709bc69)
제출용 PRD다. `subtitle-tc-generator`가 이미 가진 검사·교정 엔진(`checker/`)을
**도구**로 삼아, 그 위에 계획-실행-관찰을 반복하는 에이전트 레이어를 얹는다.
기존 [`docs/PRD.md`](docs/PRD.md)·[`docs/MVP.md`](docs/MVP.md)가 정한 원칙(자동
교정 오답 0, 원본 불변, 확실한 근거와 추정 구분)은 그대로 상속한다 — 이 문서는
그 위에 "에이전트가 무엇을 스스로 판단하고 무엇을 사람에게 묻는가"만 새로 정의한다.

## 문제 정의

자막 작업자는 발주처 규정(넷플릭스/디즈니+/쿠팡플레이 × SDH/번역)에 맞춰 파일을
검사하고, 위반을 고치고, 애매한 자리는 판단해서 다시 검사하는 과정을 **위반이
0건이 될 때까지 수동으로 반복**한다. `checker`는 이미 이 각 단계(검사·자동교정·
한국어 교정 연동)를 CLI 명령 하나씩으로 제공하지만, "검사 → 결과 보고 → 다음에
뭘 돌릴지 사람이 정하고 → 다시 명령 실행"을 사람이 직접 오간다.

이 에이전트가 대신 처리할 일: **srt 파일 하나와 발주처 조건을 받아, 위반이 0건이
되거나(자동 가능한 것 전부 처리) 더 이상 자동으로 줄일 수 없는 상태(사람 확인만
남음)가 될 때까지 검사→교정→재검사를 스스로 반복**하고, 그 과정에서 기계가
확신 못 하는 자리만 사람에게 묻는다. 결과물은 수정된 srt 파일 + 무엇을 왜
고쳤는지·무엇이 왜 남았는지 적은 리포트이며, 이것이 QC 담당자에게 최종
감수용으로 넘어간다 — 이 에이전트는 감수를 대신하지 않고 감수 앞 단계만
처리한다.

## 타겟 유저

이 도구를 쓰는 사람은 QC(최종 감수) 담당자 자신이 아니라, **QC로 넘기기 전
단계를 처리하는 자막 작업자**(TC/번역/SDH 작업자)다 — [`docs/PRD.md`](docs/PRD.md)와
동일 대상. 맞춤법을 몰라서 쓰는 사람이 아니라, 반복되는 검사-교정 왕복을
줄이려는 사람이다.

**최종 감수는 이 에이전트가 대신하지 않는다.** `docs/PRD.md`의 궁극적 성공
기준이 이미 이렇게 정해 놨다: "정답지 없는 실전에서도 **전문가가 감수만
하면 되는** 수준으로 스스로 초안을 만드는 것." 이 에이전트가 하는 일은 그
감수 앞 단계(규정 검사·확실한 자동교정·애매한 자리 사람 확인)를 반복 없이
한 번에 처리해서, **QC 담당자에게 넘어가는 결과물의 완성도를 높이는 것**이지,
QC 담당자의 최종 눈 검수를 없애는 것이 아니다.

## 카드뉴스 예시와의 관계 — 가져온 것 / 안 가져온 것

MQ4가 준 실습 예시(AI 최신소식 카드뉴스 에이전트)는 이 도메인과 무관하다.
**틀만 빌리고 내용은 새로 짠다.**

| | 카드뉴스 예시 | 이 PRD |
|---|---|---|
| 가져온 것 | 세션 상태를 `waiting_for_user`/`running`으로 저장해 새로고침·재시작에도 질문이 안 사라지는 구조, 실행 엔진(SDK/CLI) 연결 방식, "질문에 답 전엔 다음 단계로 안 넘어간다" 원칙, 확인 카드 UI 패턴 | 아래 "워크플로 설계"·"에이전트 루프" 절 |
| 안 가져온 것 | 카드뉴스 고유 내용(캐러셀·후킹 문구·독자 타겟·이미지 생성) | 전부 자막 QC 도메인 내용으로 대체 |

즉 입력·조사·선택·승인·산출물이라는 **칸 구조**는 같지만, 칸 안의 내용은
카드뉴스와 아무 관계 없다(아래 워크플로 설계 표 참고).

## 핵심 설계 원칙 — 에이전트에도 원문을 보내지 않는다

이 프로젝트에는 이미 이 원칙이 두 곳에 못 박혀 있다 — [`CLAUDE.md`](CLAUDE.md)
규칙6("미공개 자료는 밖으로 내보내지 않는다": 전사·번역 전부 로컬)과
[`docs/COMMERCIALIZATION.md`](docs/COMMERCIALIZATION.md) 1.5절("고객 자막의
비밀 유지" — "지금 설계가 로컬 처리라 유리하다. **이 성질을 문서로 약속하고
지킨다**"). 검사·교정·전사·번역 엔진에는 이미 적용돼 있다(전부 로컬). 이
에이전트는 그 약속을 깰 수 있는 새 구멍이다 — 에이전트를 움직이는 LLM(Claude)은
클라우드다.

그래서 에이전트가 보는 것과 못 보는 것을 나눈다.

| 에이전트(클라우드 LLM)가 보는 것 | 에이전트가 못 보는 것 |
|---|---|
| 규칙 ID, 위반 건수, `[자동]`/`[확인]` 구분, 재시도 횟수, 이전 선택 이력 | 자막 대사 원문 |
| 도구 호출 결과의 **구조화된 요약**(예: `{rule: "T16", count: 4, auto_fixable: 2}`) | 화자명·인물 정보·영상 파일 |
| 사람에게 물어볼 **질문 템플릿**(카드 번호·규칙 근거 조항) | — |

즉 **에이전트는 오케스트레이터일 뿐, 대사를 읽고 판단하는 자리에 없다.** 대사를
읽고 고치는 일은 지금처럼 로컬 `checker`·`korean-subtitle-corrector`·로컬
Ollama가 한다. 사람에게 "이 자리 확인해 주세요"라고 보여줄 때도, 실제 문장은
웹 화면이 로컬 파일에서 직접 읽어 렌더링한다 — 에이전트(LLM) 프롬프트에는 실리지
않는다. 이 경계가 깨지는 유일한 예외는 사용자가 스스로 그 줄을 복사해 에이전트와
대화창에 붙여넣는 경우뿐이며, 이는 앱이 유도하지 않는다.

## 워크플로 설계

```
트리거   사용자가 srt 파일 업로드 + 발주처·종류(SDH/번역)·언어 선택
              │
① 검사      [도구] checker --check  →  위반 목록(규칙ID·건수·자동/확인 구분)
              │
② 계획      [AI 판단] 자동 교정 가능한 위반이 남아 있으면 ③으로,
             없고 확인 필요만 남았으면 ④로, 위반 0건이면 ⑥으로
              │
③ 자동교정  [도구] checker --fix  →  수정된 srt, 새로 생긴 위반 없는지 재검사 예약
              └──────────────┐
              │              │ (재검사 필요 — ①로 루프)
④ 확인요청  [AI 판단] "확인" 표시된 위반을 카드로 묶어 웹 화면에 질문으로 제시
             (규칙 근거 조항·위반 위치·선택지: 승인/거부/수정 요청)
              │
⑤ 사람결정  [사람] 카드별 승인·거부·직접 수정. 반영되면 ①로 루프(변경분만 재검사)
              │
⑥ 완료판정  [AI 판단] 위반 0건, 또는 (확인만 남고 사람이 전부 판단 끝) → 종료
              │
결과물     수정된 srt + 리포트(무엇을 왜 고쳤는지, 무엇이 왜 남았는지, 실행 로그)
```

### 도구 vs AI 판단 vs 사람 결정 — 조항별 근거

| 단계 | 하는 일 | 자리 | 근거 |
|---|---|---|---|
| 검사 실행, 자동교정 적용 | 결정론적 규칙 계산 | **도구**(`checker`) | 이미 `--check`/`--fix`로 존재, LLM이 재발명할 이유 없음 |
| "자동 가능 남았나 vs 확인만 남았나" 분기 | 구조화된 카운트 비교 | **AI 판단**(가벼움) | `docs/SE_WORKFLOW_AUTOMATION.md`가 이미 "제안 → 확인"을 목표로 명시 |
| 확인 카드를 사람에게 어떻게 묶어 보여줄지(우선순위·그룹핑) | 여러 위반 중 어떤 걸 먼저 물을지 | **AI 판단** | 카드뉴스 예시의 "방향이 모호할 때만 선택지" 원칙과 동일 |
| 실제 승인/거부/수정 | 최종 판단 | **사람** | `docs/PRD.md` 원칙 — 확실한 근거만 자동, 나머지는 사람 |

## 도구 계획

| 도구 | 내부 구현 | 입력 스키마 | 출력 스키마 | 권한/위험도 |
|---|---|---|---|---|
| `run_check` | `checker/checks.py::run_checks()`가 실제 엔진이다 — `cli.py`의 `_run_one()`은 그걸 부르는 private wrapper(밑줄 시작, 외부 호출용 아님). **구현 시 이 함수를 감싸는 public wrapper를 새로 만들어야 한다**(예: `checker/api.py::check()`), 서브프로세스는 안 씀 | `{file_path, platform, kind, language}` | `{violations: [{rule_id, article, count, severity: "auto"\|"confirm", locations: [cue_index]}], passed: bool}` | 읽기 전용, 승인 불필요. 단 기본값으로 `.work/`에 중간 결과를 남긴다(원본은 안 건드림, 새 디렉터리일 뿐이라 저위험) |
| `run_fix` | 위 wrapper의 `--fix` 경로 | `{file_path, platform, kind, language}` | `{output_path, applied: [{rule_id, count}], still_violating: [{rule_id, reason}]}` | **원본 대신 새 파일 생성**(`CLAUDE.md` 규칙7 원본 불변) — 파일 시스템 쓰기이므로 실행 전 1회 사용자 동의 필요, 이후 같은 세션 내 반복 호출은 자동 승인 |
| `run_korean_review` | `checker --korean` (내부적으로 `korean-subtitle-corrector` 호출, `CLAUDE.md` 규칙0 — 별개 리포) | `{file_path}` | `{suggestions: [{cue_index, rule: "K01"\|"K02"}]}` (교정기가 없으면 스킵 표시, 에러 아님) | 읽기 전용 |
| `ask_human` | 웹앱 자체 기능(외부 API 아님) | `{question_id, cards: [{rule_id, article, cue_index}], options: ["승인","거부","직접수정"]}` | `{question_id, answers: [{cue_index, decision, edited_text?}]}` | 상태를 `waiting_for_user`로 저장, 답변 전 다음 단계로 진행하지 않음 |

**실패 처리 규칙**: `run_check`/`run_fix`가 예외를 던지면(파일 형식 오류 등)
에이전트는 재시도하지 않고 즉시 사람에게 오류를 보여준다 — 자동 재시도로
원인을 숨기지 않는다. `run_korean_review`는 교정기 미설치 시 "건너뜀"으로
표시하고 나머지 파이프라인은 계속한다(`docs/PRD.md`가 이미 정한 동작).

**위험한 도구 최소 권한**: 파일을 실제로 바꾸는 것은 `run_fix` 하나뿐이고, 항상
새 파일로 쓴다. 세션당 최초 1회는 "자동 교정을 적용해도 될까요?" 동의를 받고,
이후 반복 루프는 같은 세션 안에서만 자동 승인(다른 srt 파일·다른 세션에는
적용 안 됨).

## 사람 개입 지점

1. **세션 시작 시**: 발주처·SDH/번역·언어가 자동 감지 안 되면(`checker`의
   기존 `decisions.py` 결정표 동작과 동일) 반드시 사람이 고른다 — 기본값으로
   조용히 채우지 않는다.
2. **최초 자동교정 적용 전**: 1회 동의.
3. **`[확인]` 표시된 위반마다**: 카드로 묶어 승인/거부/직접수정 요청. 같은
   질문(`question_id`+`version`)에 이미 답했으면 다시 묻지 않는다.
4. **최종 산출물 확정 전**: 리포트 요약(고친 것 N건·남은 것 M건)을 보여주고
   사람이 "완료"를 눌러야 종료 처리한다 — 에이전트가 스스로 "끝났다"고 말하는
   것과 실제로 사람이 결과를 받아들이는 것을 분리한다. **이 "완료"는 QC 감수를
   대신하지 않는다** — 이 자동 루프가 더 할 일이 없다는 뜻일 뿐, 산출물은
   여전히 QC 담당자의 최종 감수를 거쳐야 납품 가능하다.

## 핵심 기능(MVP)과 화면 구성

**MVP 범위**: 넷플릭스 SDH·번역, 한국어 1개 프로파일부터. 확장은
`docs/SE_WORKFLOW_AUTOMATION.md`의 기존 우선순위표(1.TC 수렴 2.재분할 제안
3.줄바꿈 제안)를 그대로 따른다 — 이 문서가 이미 "다음에 뭘 자동화할지"를 정해
놨으므로 새로 만들지 않는다.

화면(웹, 로컬 서버 + 브라우저):

| 화면 | 내용 |
|---|---|
| 업로드 | srt 파일, 발주처·종류·언어 선택(자동 감지 실패 시 필수 입력) |
| 진행 상황 | 현재 루프 회차, 실행 로그 토글(도구 호출·소요 시간), 상태(`running`/`waiting_for_user`/`done`) |
| 확인 카드 | 위반 조항 인용, 위치, 승인/거부/직접수정 버튼 — **대사 원문은 로컬 파일에서 직접 렌더링**(에이전트 프롬프트 경유 아님) |
| 결과 | 수정 srt 다운로드, 리포트(고친 것/남은 것/걸린 시간·회차 수) |

새로고침해도 `waiting_for_user` 카드가 남아 있어야 한다(카드뉴스 예시와 동일
요구사항) — 상태는 세션 종료까지 서버에 저장.

## 에이전트 루프 & 오케스트레이션

- 실행 엔진: Claude Agent SDK(Python) — 이 저장소가 이미 Python이고, `checker`를
  같은 프로세스에서 라이브러리로 직접 호출할 수 있어 서브프로세스 오버헤드가
  없다.
- 상태 저장: 프로젝트당 `{file_id, platform/kind/lang, loop_count, last_check_result,
  pending_question, session_id}` — 서버 재시작 시 `pending_question`이 있으면
  중단 화면을 보여주고 저장된 맥락으로 재개(SDK 콜백 자체는 재시작하면 사라지므로
  질문·답변은 반드시 별도 저장, MQ4 예시와 동일 패턴).
- 종료 조건: 위반 0건 도달, 또는 같은 회차에서 위반 건수가 줄지 않으면(수렴
  실패) 최대 **5회 루프**에서 강제 종료하고 "더 이상 자동으로 못 줄임" 상태로
  사람에게 넘긴다. 시간 한도는 `checker` 자체가 로컬 연산이라 초 단위로 끝나므로
  별도 타임아웃 불필요.
- 다중 에이전트: 이 MVP는 단일 에이전트(오케스트레이터)로 충분 — 카드뉴스
  예시의 "조사→작성→검토" 같은 역할 분리가 필요한 만큼 판단이 무겁지 않다
  (판단이 전부 규칙 기반 카운트 비교라서).
- 테스트: 이 저장소는 커밋 전 `tools/hooks/pre-commit`이 `tests/run_tests.py`를
  돌려 실패하면 커밋을 막는다(`CLAUDE.md` 규칙9). 새 에이전트 서버 코드도
  이 훅 대상이다 — 최소한 `run_check`/`run_fix` wrapper와 `ask_human` 상태
  전이(waiting_for_user ↔ running)에 대한 테스트를 `tests/`에 추가한다.

## 관찰 가능성

- 실행 로그: 도구 호출마다 `{tool, input, output_summary, duration_ms}` — 대사
  원문은 로그에도 남기지 않는다(요약 카운트만).
- 회차별 리포트: `.work/`에 이미 있는 관례(`docs/PRD.md`)를 따라 회차마다
  `NN-check.json`/`NN-fix.json`으로 남긴다 — 몇 회차에서 병목이었는지 사후에
  확인 가능.
- 비용: 이 에이전트는 대사 본문을 LLM에 보내지 않으므로 토큰 사용량이 파일
  크기와 무관하게 작다 — "회차 수 × 판단 호출 1회"가 비용의 전부. 이 자체가
  비교 지표가 된다(회차가 늘수록 비용 증가 — 수렴 안 되는 프로파일을 찾는 신호).

## 실험 & 평가

- 평가셋: `examples/`의 샘플 srt + 정답지 대조가 이미 있는 코퍼스
  (`docs/corpus_status.yaml`)에서 발주처·장르별로 5~10편 추출.
- 지표: 완료까지 걸린 루프 회차, 사람에게 넘어간 확인 카드 수(적을수록
  좋음 — 단 자동 교정 오답이 늘면 안 됨), `run_fix` 후 새로 생긴 위반 수(0이어야
  함, `docs/PRD.md`의 "자동 교정 오답 0" 기준 그대로 상속).
- 실패 사례 기록: 5회 루프 강제 종료(수렴 실패), 잘못된 파일 형식, 확인 카드에
  사람이 응답했는데 반영이 안 된 경우 — `docs/BACKLOG.md`와 같은 형식으로 남긴다.

## 배포 계획 — 공개 URL 요구와 로컬 전용 원칙의 절충

MQ4는 "다른 사람이 링크만으로 접속해 실행"을 요구한다. `CLAUDE.md` 규칙6·
`docs/COMMERCIALIZATION.md` 1.5절의 로컬 전용 원칙은 **고객의 미공개 대사**를
겨냥한 것이지, 데모용 공개 샘플까지 막는 규칙이 아니다 — 이 구분을 명시하면
둘은 부딪히지 않는다.

1. **에이전트 오케스트레이션 서버는 공개 URL로 배포**(웹앱). 대사 원문을
   다루지 않고 도구 호출·상태만 관리한다.
2. **데모는 srt 텍스트 파일 업로드를 받는다**(영상·음성 업로드는 없음).
   `run_check`/`run_fix`만 쓰므로 whisper·GPU 같은 무거운 자원이 필요
   없어 서버에서 가볍게 돈다. 처리 후 파일은 세션 종료 시 즉시 폐기하고,
   화면에 "비밀 유지가 필요한 실제 납품 파일은 올리지 마세요 — 이 데모는
   공개 가능한 자막에만 쓰세요"를 명시한다. **실제 업무(진짜 고객 자막)는
   여전히 로컬에서만 처리**하며, 그 경로는 이 데모 서버를 거치지 않는다
   (기존 CLI·GUI 그대로).
3. 이렇게 하면 처음 보는 사람도 자기 srt 파일을 넣어 검사→확인카드→교정
   전 과정을 실제로 체험할 수 있다 — "내 로컬 폴더가 있어야만 써진다"는
   구멍이 없다.

수용 기준: 대표 시나리오(넷플릭스 SDH 샘플, 위반 3종 이상 포함) 실행 시
사람 개입 없이 자동 교정 가능한 위반은 전부 처리되고, 확인 카드 응답만으로
완료까지 도달하는 것.

**2026-09-09, 실제 배포 URL로 재확인함** — `examples/ko-sdh-sample.srt`를
공개 URL(`https://patricia-astrology-missouri-offerings.trycloudflare.com`,
임시 터널)로 업로드 → 자동교정 승인 → 확인카드 8건 응답 → `done`(고친 것
10건, SE이관 2건, 3회차, $0.041). `/download`·`/bookmarks` 엔드포인트도
공개 URL로 200 확인. 로컬이 아니라 실제 인터넷을 거친 요청으로 전체
사이클이 도는 것까지 확인했다 — `EVALUATION.md` 참고.

## 범위 밖

- 자막 편집기 기능, 클라우드 전사·번역 — `docs/PRD.md`와 동일하게 범위 밖.
- 여러 에이전트로 역할을 나누는 구조 — MVP 판단이 가벼워 불필요, 확장 시
  검토(예: "위반 우선순위 판단"과 "카드 문구 작성"을 분리).
- ~~MCP 서버로 `checker`를 외부에 노출하는 것~~ 〔했음, 2026-09-09 — 확장 시도〕
  `agent/mcp_server.py`가 독립 stdio MCP 서버로 `run_check`/`run_fix`/
  `export_bookmarks`를 노출한다. Claude Desktop·Claude Code·다른 에이전트가
  설정 파일에 등록만 하면 이 저장소 밖에서도 자막 QC를 도구로 쓸 수 있다 —
  MCP 클라이언트로 실제 접속해 `check_subtitle`·`fix_subtitle` 호출까지
  확인함(README_MQ4.md 참고).
