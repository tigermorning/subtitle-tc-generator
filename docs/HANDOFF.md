# 엔지니어링 핸드오프 — 팀 합류자를 위한 문서

이 문서는 `docs/BACKLOG.md`(날짜순 작업 기록)를 대신하지 않는다. 여기는 **"왜
이렇게 만들었는가"와 "지금 무엇이 비어 있는가"만** 담는다. 과정을 시간순으로
쫓고 싶으면 `BACKLOG.md`를, 작업 규칙 자체를 보려면 `CLAUDE.md`를 본다.

**살아있는 문서다.** 결정이 바뀌거나 새 모델을 들이거나 오래된 미해결 항목이
풀리면 이 파일을 그때그때 고친다. 날짜 붙은 회의록이 아니라 **지금 상태의
지도**로 유지한다 — 오래된 항목은 지우거나 고치지, "2026-08-27에는 이랬음"
식으로 쌓아두지 않는다(그건 BACKLOG.md의 역할이다).

## 1. 무엇을 만드는가

방송·OTT용 자막(TC + SDH + 번역)을 사람 작업자 수준으로 초벌 생성하는 도구다.
납품 대상은 발주처(넷플릭스 등)마다 다른 정식 스타일 가이드를 따라야 하고,
결과물은 항상 **사람이 검수·수정하는 초안**이다 — 완전 자동화가 목표가
아니다(아래 4번 "설계 철학" 참고).

두 축이 있다.
- **TC(타임코드) 작업자**: 말소리를 화면 단위로 끊고 인점·아웃점을 맞춘다.
- **자막(SDH·번역) 작업자**: 그 화면 안에 들어갈 텍스트를 쓴다(원어 그대로
  받아쓰거나, 다른 언어로 옮긴다).

이 도구는 두 역할을 **하나의 파이프라인**으로 자동화하고, 발주처 규정 검사기
(`checker/`)로 사람이 볼 것만 추려낸다.

## 2. 파이프라인 — 순서가 설계의 전부다

```
①전사 -> ②대조(대본 있으면) -> ③번역 -> ④재분할 -> ⑤글자수·스포팅 규정
transcribe.py  align.py          translate.py  resplit.py  timing.py / checks.py
```

이 순서를 바꾸면 결과가 구조적으로 나빠진다(`CLAUDE.md` 규칙 1).

- **전사가 글자 수를 무시하는 이유**: whisper의 `max_len`으로 자르면 문장이
  중간에 끊긴 채 굳는다. 사람도 먼저 다 받아쓰고 나중에 나눈다.
- **재분할이 번역 뒤에 오는 이유**: 원어 기준으로 미리 끊으면 번역문(특히
  한국어)이 그 경계에 갇힌다. 언어마다 문장 구조가 달라 끊는 자리도 달라야
  한다.
- **글자 수가 맨 끝인 이유**: 발주처 규정(줄당 글자 수·읽기 속도)은 **번역문
  기준**이다. 원어 스크립트에 규정을 대는 것은 시간 낭비다.

## 3. 규정(rule) 3계층

```
rules/private/<플랫폼>/ 공식 문건에서 옮긴 값. source.official: true
rules/learned/<플랫폼>/ 정답 자막에서 관측한 값. source.origin: learned
코드 기본값             위 둘 다 없을 때만
```

**공식 규정층은 이 저장소에 없다**(2026-09-10). 넷플릭스·디즈니+·쿠팡플레이
규정과 그 근거 문서(`sources/`)는 파트너 전용 비공개 문서라 별도 비공개
저장소 `tigermorning/subtitle-tc-rules`로 떼어 냈고, `rules/private/`로 클론해
쓴다(`README.md`의 "발주처 공식 규정은 별도 비공개 저장소에 있다" 참고).
`.gitignore`가 그 자리를 막는다.

같은 날 히스토리도 재작성했다 — `git filter-repo`로 옛 커밋에서 네 경로를
지우고, GitHub 저장소를 지웠다 다시 만들어 올렸다. **저장소를 지운 이유는
force-push만으로는 안 지워지기 때문이다**: 병합된 PR 16개가 `refs/pull/N/head`로
옛 커밋을 붙들고 있었고(PR #11·#12·#13은 diff에 규정 파일이 그대로 보였다),
그 ref는 GitHub이 관리해서 사람이 못 지운다. 재작성 전 상태는
`D:ackup\subtitle-tc-generator\`에 남겼다(미러 + PR 기록 + 여는 법을 적은
README). 자세한 것은 `docs/BACKLOG.md` 0-S절.

**부르는 이름은 안 바뀌었다.** `checker/profile.py`의 `_roots()`가
`rules/private/`를 함께 뒤진다 — `-p netflix -l ko -k sdh`도
`extends: netflix/ko-sdh.yaml`도 그대로다. 클론이 없으면 그 세 발주처
프로파일만 로드 실패하고 나머지 검사는 돈다.

**학습값이 규정을 덮어쓰지 않는다.** 배운 값이 `rules/<플랫폼>/`를 고치는
일은 없다 — 관측이 공식 문건과 다르면 그건 승격이 아니라 "지적"으로 남긴다.
규정으로 승격하는 유일한 길은 사람이 확인하는 것이다(`CLAUDE.md` 규칙 11).

**`rules/genre/`가 플랫폼 규정 위에 얹힌다.** 예능·다큐처럼 장르가 요구하는
값(예: 병합 상한 2초)은 플랫폼과 별개 계층이다.

**`--lock-timecodes`**: 이미 TC 작업이 끝난 파일에 번역만 붙이는 실무 사례를
위한 모드. 재분할·스포팅을 전부 막고, `--fix-timing`과 같이 쓰면 오류를 낸다
(조용히 무시하지 않는다 — `CLAUDE.md` 규칙 8).

**정답지 학습은 두 층(A/B)이고, `genre_maturity`가 그 경계를 관리한다**
(`CLAUDE.md` 규칙 13, 2026-08-30):

```
A. 정답지-학습    정답 자막만 재서 통계·관행을 뽑는다. 영상·GPU 불필요,
                 비용 거의 0. 정답지 있는 영상은 예외 없이 이것부터 한다.
B. 정답지-대조-진단  --generate로 blind 초안을 만들고 --against로 대조해
                 checker/ 코드 자체의 구조 버그를 찾는다. whisper 전사가
                 필요해 비용이 크다.
```

A만으로 충분한지(=B를 계속 돌려야 하는지)는 `rules/private/corpus/corpus_status.yaml`의
`genre_maturity`에 발주처+장르별로 기록한다 — 연속 3편(`threshold`)을 B로
대조했는데 새 구조 버그가 0건(`clean_streak`)이면 "성숙" 후보이지만,
**사람이 확인해야만** `matured: true`가 된다(자동 전환 없음, 규칙 11 승격
경로와 같은 이유). 성숙 판정 후에도 새 이상이 보이면 `clean_streak`은 0으로
리셋된다. 절차는 `.claude/skills/정답지-학습/`·`.claude/skills/정답지-대조-진단/`에
코드화돼 있다.

## 4. 설계 철학 — 세 가지가 반복해서 나온다

1. **어느 쪽도 정답으로 두지 않는다.** 전사와 스크립트가 다르면 기계가
   판정하지 않고 표시만 한다. 표기 통일표를 어겨도 고치지 않고 표시한다 —
   문맥상 예외가 실제로 있다.
2. **확실한 근거와 추정을 구분한다.** 파일 안에서 검증 가능한 것(글자 수,
   중복 등)은 자동 교정한다. 영상에서 추정한 것(말소리 구간, 화자 분리,
   화면 캡션 위치)은 **제안만 하고 자동으로 고치지 않는다** — 틀렸을 때
   원인을 사람이 못 찾기 때문이다.
3. **원본을 절대 덮어쓰지 않는다.** 교정 결과는 항상 새 파일로 나간다.

## 5. 핵심 모델·도구와 선택 이유

전부 **로컬에서만 돈다** — 미공개 방송 전 영상·대본이 클라우드로 나가면 계약
위반이다. 이 제약이 모델 선택 전체를 관통한다(클라우드 API를 쓰면 더 쉬운
자리에서도 로컬 모델을 고른 이유).

| 용도 | 선택 | 이유 |
|---|---|---|
| 전사(STT) | faster-whisper(ctranslate2) `large-v3-turbo`가 1순위, 없거나 실패하면 ffmpeg 내장 whisper 필터(whisper.cpp) `ggml-large-v3-turbo`로 되돌아감 | 별도 설치 없이 이미 쓰는 ffmpeg으로도 되는 것이 원래 선택 이유였다. 다만 whisper 필터는 신뢰도(`avg_logprob`)를 안 줘서 환각을 못 걸러 2026-08-30에 faster-whisper를 1순위로 올렸다(§6 참고). large 모델이 GPU에서 base보다 오히려 빠르고(23배속 vs 8배속) 훨씬 정확해 품질 낮출 이유가 없었다 |
| 말소리 구간(VAD) | Silero VAD(ONNX, 2MB) | ffmpeg의 음량 기반 검출은 배경음악·잡음에서 무너진다(한 자료에서 30분을 구간 3개로 잘못 잡음). 모델 기반이 최악을 없앤다 |
| 번역(1차) | Ollama 로컬 LLM — 기본 `exaone3.5:7.8b`(한국어 품질 우수, 상업 라이선스 제약 있음), 대안 `qwen2.5:7b-instruct`(Apache-2.0) | 로컬 제약 + 라이선스 선택권을 같이 준다. 3060 Ti 8GB에서 4bit 양자화로 돈다 |
| 화자 분리 | `pyannote/speaker-diarization-community-1`(2026-08-27 도입) | whisper가 화자 구분을 안 준다 — 예능처럼 화자가 빠르게 바뀌는 자리를 `regroup.py`가 한 자막으로 뭉치는 버그의 근본 원인이었다. 4개 후보 중 3.1보다 화자 수 세기·전사 타이밍 정합이 낫다고 공식 문서가 밝힌 최신판을 선택 |
| 소리 분류(`--sfx-scan`) | `MIT/ast-finetuned-audioset-10-10-0.4593`(2026-08-31 도입) | whisper는 대사만 받아 적어 SDH의 순수 효과음 자막(`[groans]` 등)을 처음부터 못 만든다(영화B·영화A·드라마B 정답 대조로 실측 확인). AudioSet 527종 분류가 "무슨 소리 계열인가"까지는 짐작해 준다 — 분위기·구체 동작은 못 짚어(아래 의존성 상태 참고) 1단계(보고용)만 만듦 |
| `--against` 텍스트 유사도 | Ollama 임베딩, `paraphrase-multilingual`(2026-08-27 도입) | 글자 겹침(`SequenceMatcher`)은 의역을 못 잡는다("Mister Cho isn't that you?" vs "Aren't you Mr. Cho?" = 글자 유사도 0.18). 참/거짓 짝 10개로 4개 모델(bge-m3·paraphrase-multilingual·nomic-embed-text-v2-moe·qwen3-embedding:0.6b)을 실측 비교해 분리폭이 가장 큰 것을 골랐다(`checker/embed.py` 주석에 표 있음) |
| BLEU·ROUGE·perplexity | **안 씀** | 시도했다가 걷어냈다. ROUGE-L이 기존 글자 유사도와 값이 같았고, 한국어 특유의 짧은 어절 때문에 BLEU가 자주 0으로 무너짐. `번역-품질-지표` 스킬에 판단 근거 기록 |
| Whisper 파인튜닝 | **안 함(의도적)** | 자막은 대사의 축약본이지 전사가 아니다. 이것으로 ASR을 학습시키면 전사 자체가 축약을 배워버린다 — 전사는 모델 교체로만 개선한다 |
| 전사 모델 교체 후보 — Qwen3-ASR-1.7B | **기각(2026-09-10, 실측 후)** | 2편·2언어 대조에서 whisper가 이겼다. 무엇보다 TC를 스스로 내지 않는다(별도 forced aligner 필요, 5분 상한·11개 언어) — TC가 이 도구의 유일한 납품 축이라 그것만으로 후보에서 빠진다. 아래 절에 숫자와 방법 있음 |
| 번역 LoRA(PEFT) | **아직 안 함** | 최소선 5,000쌍(≈5편)에 아직 못 미침(2026-08-27 기준 1편, 1,453쌍). 문턱값 도달 시 자동 시작하지 않고 사용자에게 먼저 건의하기로 함. `corpus/pairs/`에 (초안, 정답) 짝을 미리 쌓는 중 |

### 전사 백엔드 교체 검토 — Qwen3-ASR-1.7B는 기각됐다 (2026-09-10 실측)

**다시 검토하기 전에 이 절부터 읽는다**(규칙 17). Qwen3-ASR은 2026-01-29에
Apache-2.0으로 공개된 Alibaba의 ASR 모델이고, 벤치마크 숫자와 "음악 섞인
장구간에서 whisper-large-v3가 무너진다"는 기술 보고서 주장 때문에 교체
후보로 올라올 만하다. 실제로 재 봤고, 우리 자료에서는 지지되지 않았다.

**방법**: TC를 whisper 것으로 고정하고 그 창(window) 안의 오디오만 Qwen에
다시 넣어 **텍스트만** 갈아 끼웠다. 두 결과의 TC가 완전히 같으므로
`--against` 대조에서 달라지는 변수가 ASR 텍스트 하나뿐이다. 둘 다 정답지를
기준으로만 쟀지 서로 비교하지 않았다(규칙 13 — 자체 대조 금지).

    자료                                          whisper   Qwen
    드라마B E02 (디즈니+, 한국어 SDH)      0.50     0.40
      └ Qwen이 한국어로 판정한 573창만              0.56     0.50
    영화A (아마존, 영어 SDH)                  0.78     0.73
      └ Qwen이 영어로 판정한 1505창만               0.78     0.73
      └ 정답지 ♪ 구간과 겹치는 71창                 0.35     0.32

(`--against`의 텍스트 유사도 중앙값. 마침표를 떼고 다시 재도 한국어 결과는
0.56 vs 0.50 그대로였다 — 구두점 표기 차이 탓이 아니다.)

**기각 사유는 점수보다 구조가 먼저다.**

1. **TC를 안 낸다.** 타임스탬프가 모델에 없고 별도 `Qwen3-ForcedAligner-0.6B`가
   필요하다(5분 상한, 11개 언어). TC가 유일한 납품 축이다(규칙 15).
2. **신뢰도가 없다.** `generate.py`의 환각 검사가 faster-whisper의 `avg_logprob`에
   기대는데 Qwen은 logprob 출력이 문서화돼 있지 않다.
3. **언어 힌트가 강제가 아니다.** `language="ko"`를 줘도 778창 중 205창을 한국어
   아님으로 판정했다(English 68, Chinese 22, Japanese 13). 시스템 프롬프트 한
   줄로 들어갈 뿐이다. 영어는 이 문제가 거의 없었다(1710창 중 오판 7).
4. **자기 강점 구간에서도 안 이겼다.** 노래(♪) 구간에서 0.35 vs 0.32.

**Qwen이 실제로 나은 것은 속도뿐** — 67분 영상 97초(RTX 3060 Ti, fp16).

**근거로 쓰지 않기로 한 관측**: 같은 문장 3회 이상 연속 반복이 whisper 8건·
3건인데 Qwen은 0건이었다. Qwen을 창 단위로 돌려 문맥 이월 자체가 불가능했으니
창 나눔이 만든 착시다 — 반복 환각 비교는 이 설계로는 못 잰다.

**한계(같이 남긴다)**: 창 경계를 whisper가 정했으므로 whisper에 유리한 설계다.
다만 그 유리함이 0.05~0.06 격차를 전부 설명하지는 못한다 — 한국어에서는 단어
수준 오류가 실제로 더 많았다(`젖병`→`적병`, `커서`→`거사`, 경상도 사투리·
고유명사에서 특히).

**재실험 방법**: `tools/asr_backend_compare.py`가 이 절차를 그대로 담고 있다
(`baseline`으로 whisper 기준선, `alt`로 후보 모델을 그 창에 넣는다). whisper를
다시 돌릴 필요도 없다 — `학습한 TC 및 자막 모음/_whisper_cache/`의 전사가 창을
주므로 `alt`만 돌리면 GPU 몇 분이면 끝난다. 이 설계가 **못 재는 것**(TC 품질,
반복 환각)은 그 도구의 docstring에 적어 뒀다.

### 개발 환경 특이사항 — 반드시 알아야 함

- **pyannote.audio는 시스템 파이썬에 직접 설치하면 안 된다.** torch>=2.8.0을
  요구하는데, 시스템 파이썬의 torch(2.6.0+cu124)는 torchvision 등 다른
  프로젝트가 물려 쓴다. 실제로 한 번 설치했다가 시스템 torch를 CPU 전용
  2.13.0으로 바꿔버려 되돌린 적이 있다(2026-08-27). **`.venv-diarize/`에
  격리 설치한다.**
- Windows에서 개발, 사용자는 Windows에서 실행. WSL은 Ollama 원격 호출에만
  쓴다(GPU가 Windows 쪽에 있어서).
- ffmpeg 필터 옵션에 절대 경로를 못 쓴다(`C:`의 콜론이 옵션 구분자로 읽힘) —
  작업 폴더 기준 상대 경로만 쓴다.
- ffmpeg 출력은 반드시 `encoding="utf-8"`로 디코드한다. Windows 파이썬 기본은
  cp949라 non-ASCII가 섞이면 스트림이 깨진다.
- 배치 파일 본문은 ASCII만 — cmd가 콘솔 코드페이지로 바이트 단위 파싱을 한다.

### 화면 캡션 인식(`--ocr-scan`) 의존성 상태 (2026-08-30 신설, 같은 날 스모크 테스트+고침 끝)

`.venv-diarize`와 같은 이유로 `.venv-ocr/`에 EasyOCR을 격리 설치했다 —
`requirements-ocr.txt` 참고. 설치·실측·수정·재검증까지 한 세션에서 끝냈다
(2026-08-30, 예능A 19회 "Screwballs S02E19"로).

**1차 실측에서 구조적 문제 둘을 찾았고, 같은 날 바로 고쳤다:**

1. **기본 모드(`detect_bottom_text` 선필터)가 이 영상에서 캡션을 0개 찾았다.**
   원인을 프레임을 직접 열어 확인함(300초·900초·1500초 지점) — 이 예능의
   화면 캡션은 화면 **아래 25%가 아니라 인물 옆(중앙~오른쪽, 세로 중간)**에
   뜬다. `detect_bottom_text`는 이름 그대로 아래쪽만 본다 — 애초에 이 종류
   캡션 위치를 못 잡는 설계다. **고침**: `checker/ocr.py`의 `full_scan` 기본값을
   `True`로 뒤집었다(규칙12: 정확히 다 훑는 것이 빠른 것보다 우선). 옛 방식
   (선필터로 빠르게)은 `--ocr-fast`로 opt-in만 가능하다.
2. **`merge_frames`의 정확 일치 비교가 같은 캡션을 여러 조각으로 쪼갰다.**
   실측: 같은 캡션("우리 마님이 이런데 뭐..?")이 연속된 프레임에서 EasyOCR이
   매번 조금씩 다르게 읽었다("마남이"/"마넘이"/"마남이" — 압축·모션 블러로
   프레임마다 인식이 흔들린다). 완전 일치 비교라 한 캡션이 세 조각으로
   갈렸다. **고침**: `difflib.SequenceMatcher`(표준 라이브러리) 편집 유사도
   비교로 바꿨다(`--ocr-min-similarity`, 기본 0.6). 대표 텍스트는 합쳐진
   프레임 중 신뢰도가 가장 높았던 것을 쓴다.

**재검증**: 같은 40초 클립을 다시 돌려 캡션이 3조각 -> 1개로 병합됨을
확인(0~2000ms, "우리 마남이 / 이런데 뭐 ?", 신뢰도 0.56). `tests/run_tests.py`
에 편집 유사도 케이스 추가, 812건 전부 통과. 읽기 자체는 원래도 잘 됐다
(신뢰도 0.4~0.78, "마님"이 "마남"으로 한 글자만 틀림) — 문제는 위치 가정과
병합 로직이었지 EasyOCR 자체가 아니었다.

**전체 회차(37분)로 회귀 확인 — 세 번째 문제를 가정했다가 검증해서 절반만
맞았다(2026-08-31):** 전체를 돌려보니 캡션 하나가 167초까지 늘어난 사례가
나와, "전이적 드리프트"(직전 캡션의 대표 텍스트와 비교하면 A~B~C~D로 비교
기준이 계속 옮겨가며 서로 다른 캡션이 섞여 이어 붙는 것)로 가정했다.

- **비교 기준을 anchor(그 캡션의 첫 프레임)로 바꾼 것은 맞는 고침이었다** —
  합성 테스트로 실제 드리프트 시나리오를 재현해 확인함(`tests/run_tests.py`).
- **처음에 같이 넣은 `max_duration_ms`(8000ms) 상한은 검증 없이 넣었다가
  되돌렸다.** 167초 구간을 다시 잘라 돌려 보니 여전히 하나로 합쳐졌고(anchor
  비교만으로 이미 정상), 315초·340초 프레임을 직접 열어 확인하니 "Sebastiam/
  Tomy/Scarlet" **이름 캡션이 25초 넘게 픽셀 단위로 그대로**였다 — 167초짜리는
  버그가 아니라 **사실**이었다(정지 화면 코멘터리·이름표 캡션은 실제로 이만큼
  오래 떠 있는다). 상한 기본값을 없앴다(`None`, `--ocr-max-duration`으로 사람이
  켜야만 적용). 증거 없이 상한을 두면 실제로 오래 떠 있는 정상 캡션을 인위적으로
  쪼갠다 — 이 결정 자체가 규칙14(맥락 부패 확인 — 이전 생성물의 추정을 검증
  없이 다음 고침에 그대로 반영하지 않는다)의 사례다.
- **두 번째 독립 근거(311~370초 구간, 상한 되돌리기 전 코드로 돌았지만
  텍스트 자체를 보면 충분함)**: "아무리 그래도 그게 뛰냐"가 62.5초 내내
  검출된 8조각 전부에서 **글자 하나 안 바뀌고** 그대로였다. 이름 캡션
  사례와 별개로, 정적 텍스트가 오래 떠 있는 것이 이 영상에서 드문 일이
  아님을 다시 확인했다.

`tests/run_tests.py` 817건 전부 통과. `checker/ocr.py`의 `merge_frames`
독스트링에 이 판단 과정을 그대로 남겨 뒀다 — 다음에 "지속시간이 이상하다"는
지적이 다시 나오면 여기부터 읽는다.

**"제대로 구축됐는지 확인" 요청으로 실제 서브프로세스 경로를 처음 돌려 보니
구멍 셋을 찾았다(2026-08-31, 합성 영상으로 진짜 `.venv-ocr` 서브프로세스
실행해 재현·검증까지 끝냄 — 유닛 테스트는 `merge_frames` 같은 순수 함수만
잰다는 것을 이번에 알았다, `engine` 목업이 실제 서브프로세스를 완전히
건너뛴다):**

1. **첫 실행이 100% 크래시했다.** EasyOCR이 영어 인식 모델을 처음 내려받을
   때 tqdm 진행바가 U+2588(전각 블록)을 찍는데, `_ocr_worker.py`가 별도
   프로세스라 `cli.py`의 `_fix_console_encoding()`이 안 미쳐 Windows 기본
   콘솔 인코딩(cp949)으로 `UnicodeEncodeError`가 났다. 실측: `~/.EasyOCR/
   model/temp.zip`이 반쯤 받다 멈춘 채 남아 있었다 — 기본 언어(en)로 한
   번도 성공한 적이 없는 상태. **고침**: 같은 파일에 `reconfigure(encoding
   ="utf-8", errors="replace")` 추가(`cli.py`와 같은 처방). 재검증: 다운로드
   부터 재현, 18.7초에 정상 완주, 캡션 정확히 검출.
2. **프레임 하나가 손상되면 전체 스캔이 죽었다.** `reader.readtext()`가
   못 읽는 프레임(끊긴 ffmpeg 출력 등)에서 `OSError`를 던지는데(가짜 PNG로
   재현) 안 잡고 있었다 — 결과를 끝에 한 번에 쓰는 구조라 프레임 한 장이
   그때까지 처리한 것 전부를 날린다. **고침**: 프레임별 예외를 잡아
   "글자 없음"(빈 텍스트·신뢰도 0)으로 남기고 계속한다.
3. **7~8시간짜리 스캔(`--ocr-hardsub`, 71분 영상 기준)이 끊기면 처음부터
   다시 돌아야 했다.** 결과를 마지막에 한 번에만 디스크에 썼다 — 이건
   재현은 못 했다(몇 시간대 작업이라), 코드 동작 분석만으로 찾은 구조적
   위험이었다(사용자 확인 후 착수). **고침**: `_ocr_worker.py`가 프레임마다
   처리 직후 결과 전체를 임시파일+`os.replace()`로 원자적으로 다시 쓰고,
   시작할 때 기존 결과가 있으면 그 시각(ms)들은 다시 안 돈다.
   `detect_onscreen_captions(checkpoint_path=...)`가 영상·언어·fps·band·
   full_scan 지문으로 이전 체크포인트를 믿어도 되는지 판단해서(안 맞으면
   지우고 새로 시작) `--ocr-hardsub`에 자동으로 물렸다(`<out>.ocr-checkpoint
   .json`). 스캔이 끝까지 성공하면 체크포인트는 치운다. 재검증: 앞 10개
   프레임에 가짜 결과("STALE")를 미리 심고 이어받기를 돌리니 그 10개는
   그대로(재실행 안 됨), 나머지만 새로 OCR — 성공 후 체크포인트 자동 삭제
   확인. 지문 검증 로직(순수 함수)은 유닛 테스트 11건 추가.

`tests/run_tests.py` 905건 전부 통과. 셋 다 `checker/_ocr_worker.py`·
`checker/ocr.py` 주석에 실측 근거를 그대로 남겼다.

Smart App Control이 켜져 있으면 `.venv-diarize`의 torch DLL이 막힌 적이
있다(아래 항목) — `.venv-ocr`는 이번엔 문제없이 돌았다.

**2단계(`--generate --ocr`, 2026-08-31)**: 위 1단계(`--ocr-scan`, 보고용)
위에 실제 자막 병합을 얹었다. 자세한 내용은 `docs/BACKLOG.md` T12 참고 —
요약하면 `position.is_forced_narrative()`가 이미 텍스트 마커만으로 화면자막을
판정하고 있어서, 필요했던 건 마커를 입히는 함수 하나뿐이었다.

**3단계(`--ocr-hardsub`, 2026-08-31, 같은 날)**: T12까지는 whisper 대사 위에
얹는 *부가* 캡션이었다 — `[습작] SDH+번역/하드섭(자막필요)/`처럼 자막이 화면
전체에 타 있고 임베디드 트랙이 아예 없는 영상엔 안 맞았다. `tools/corpus_build.py:
90-91`이 "OCR을 거친 것은 정답 자료로 쓰지 않는다"고 이미 못박아 둬서,
`--ocr-hardsub`는 **초안(.ocr-draft.srt)만** 낸다 — 사람이 영상과 대조해
고친 뒤에야 `학습한 TC 및 자막 모음/`에 들어간다(`docs/BACKLOG.md` T13,
`.claude/skills/정답지-학습/SKILL.md`). 실측 중 구조 버그를 하나 더 찾음:
전체 프레임을 읽으면 좌상단 작품명 워터마크·배경 간판 글자가 대사와 섞여
정작 실제 대사가 결과에서 사라졌다 — `_extract_frames()`에 화면 아래
`band`(기본 0.25) 자르기를 추가해 고침(`--ocr-scan`/`--ocr`은 캡션 위치가
안 정해져 있어 여전히 `band=None`).

**TC 정밀도(같은 날 후속)**: "굵게 훑고 경계만 따로 촘촘히 재확인"을 세 번
고치다가 접근 자체를 걷어냈다 — ffmpeg이 짧은 구간을 독립적으로 seek하면
실제 내용과 다른 프레임을 준다는 게 원인이었다(같은 시각인데 연속 디코딩
프레임엔 자막이 보이고 독립 seek 프레임엔 안 보임, 프레임을 직접 저장해
확인). `refine_caption_boundaries` 등 정밀화 층을 전부 빼고, 이미 정확하다고
확인된 연속 디코딩을 그대로 촘촘한 fps로 쓴다 — `--ocr-hardsub`의
`sample_fps` 기본값을 12로 올렸다(2fps 대비 6배 느림, 71분 영상 기준
7~8시간 — 시간보다 정확도 우선). 자세한 시행착오는 `checker/ocr.py` 모듈
독스트링과 `docs/BACKLOG.md` T13 참고.

### 소리 분류(`--sfx-scan`) 의존성 상태 (2026-08-31 신설)

**격리 venv가 필요 없었다.** `.venv-ocr`·`.venv-diarize`와 달리, 이 컴퓨터
시스템 파이썬에 이미 `transformers`(5.14.1)·`torch`(2.6.0+cu124)가 깔려
있어 버전 충돌 없이 바로 됐다(직접 확인, 2026-08-31). 모델은
`MIT/ast-finetuned-audioset-10-10-0.4593`(Audio Spectrogram Transformer,
AudioSet 527종 라벨, 허깅페이스에서 첫 실행 시 자동 다운로드).

**`transformers`의 `pipeline("audio-classification", ...)` 편의 함수를
안 쓴다.** 파일 경로든 numpy 배열이든 내부적으로 `torchcodec`을 무조건
import하는데, 이 컴퓨터에서 `torchcodec`의 네이티브 라이브러리 로드가
`OSError: [WinError 127]`로 죽는다(직접 재현, 2026-08-31 — Smart App
Control이나 DLL 버전 문제로 보이나 근본 원인은 안 팠다). 대신
`AutoFeatureExtractor`·`ASTForAudioClassification`을 직접 불러 쓴다
(`checker/sfx.py`) — `torchcodec`을 아예 안 거친다. 오디오는 ffmpeg으로
16kHz 모노 wav를 뽑고 `scipy.io.wavfile`로 읽는다(`soundfile`은 이
컴퓨터에 없어서 최소 의존성 있는 `scipy`를 골랐다).

**1단계(짐작만, `checker/sfx.py`)만 됐다.** `--ocr-scan`과 같은 위치 —
자막 Event로 만들거나 `--generate`에 자동으로 얹지 않는다(규칙4, 소리
검출은 추정). 대사 없는 구간(VAD 기준)마다 top-1 라벨을 보고, 확신 높은
일부만 `rules/lexicon/ko-sdh-effects.yaml`의 가장 일반형 한국어 후보로
매핑한다("Music"→"[음악이 흐른다]" 등) — 분위기("[슬픈 음악]" 등)나
화면을 봐야 아는 구체 동작("[신발을 쓱 벗는다]" 등)은 오디오만으로 못
정해서 애초에 매핑표에 안 넣었다(`docs/BACKLOG.md` §0-C가 이미 이 한계를
지적해 뒀다). 영화B로 스모크 테스트: 대사 없는 639구간 중
505곳에서 확신 있는 라벨을 냄 — 대부분 "Music"(적절해 보임), 몇몇은
"Speech"(VAD가 못 잡은 자리에 실제 말소리가 있을 가능성 — 이번 세션
`has_vad_support()` 정정과 같은 부류의 신호, 자세히는 안 팠다).

**2단계(`--generate --sfx`, 같은 날 이어서)**: `sound_events_to_draft_events()`·
`merge_sound_events()`로 실제 자막에 얹는다. `candidate`가 있는 것만,
얹은 자리는 예외 없이 "확인 필요" 노트(신뢰도 안 가림 — OCR과 다름, 위
설명 참고). 드라마B E01로 통합 테스트하며 구조 버그를 하나 찾아 바로 고침:
**플랫폼별 음악 효과음 ♪ 표기(DP12류)를 안 맞췄다** — 디즈니는
`[♪ 음악이 흐른다]`를 원하는데 고정 문구 `[음악이 흐른다]`를 그대로
얹어 DP12 위반 126건을 만듦(이 검사는 `checker/fixes.py`에 자동 교정이
없어 사람이 매번 손으로 고쳐야 했다). `_apply_music_marker()`로
`profile.get("music", {}).get("note_inside_bracket")`를 생성 시점에
반영하도록 고침 — 재검증: 지적 840→693건, DP12 0건.

**범위를 일부러 여기서 접었다(2026-08-31, 사용자 확인)**: 대사와 겹치는
배경음(대화 도중 흐르는 음악 등)은 다루지 않는다 — 지금은 "대사 없는
구간"만 본다. 미완성이 아니라 **의도적 결정**이다 — 실무 SDH는 대사가
있는 화면엔 보통 효과음 자막을 안 겹쳐 넣어서(화면에 이미 자막이 있음)
확장해도 실익이 적다고 판단했다. 다시 검토할 계기가 생기면(예: 대사
사이 짧은 배경음 전환을 SDH가 실제로 표시하는 사례를 발견하면) 여기부터
읽는다.

### 화자 분리(`--diarize`) 의존성 상태

`.venv-diarize/`에 pyannote.audio + torch 2.13.0+cu126을 격리 설치해
뒀다(시스템 파이썬과 왜 분리했는지는 위 항목 참고). 모델은
`pyannote/speaker-diarization-community-1`(게이팅됨, HF 계정으로 이용조건
동의 + read 권한 토큰 필요 — `checker/diarize.py` 참고, 토큰은
`HF_TOKEN` 환경변수). Windows 사용자 환경변수는 이미 떠 있는 프로세스에
소급 적용 안 된다 — 등록 후 새 셸이 필요하다.

**2026-08-28부터 이 컴퓨터에서 `--diarize`가 막혀 있다 — 코드 문제가
아니다.** `DLL load failed ... 애플리케이션 제어 정책` 오류가 난다.
원인은 Windows 11 **Smart App Control**이 켜져 있어서다(`Get-CimInstance
-Namespace root\Microsoft\Windows\Defender -ClassName
MSFT_MpComputerStatus`의 `SmartAppControlState`가 `On`) — 서명 안 되거나
평판 정보 없는 실행 파일·DLL을 자동 차단하는 기능인데, `.venv-diarize`의
torch DLL이 걸린다. 어젯밤(2026-08-27)엔 됐다가 다음날 안 됐다 — 평판
재평가가 나중에 이뤄졌을 가능성이 크다.

**끄면 해결되지만 사용자가 껐다 켤 수 없는 기능이다**(마이크로소프트가
의도적으로 단방향으로 설계 — 다시 켜려면 윈도우 초기화가 필요하다).
그래서 **사용자 결정(2026-08-28): 끄지 않는다. `--diarize` 없이
진행한다.** 이 컴퓨터에서 화자 분리가 다시 필요해지면 이 상태부터
확인한다 — DLL 경로나 코드 문제로 오판해서 시간 낭비하지 않는다. 다른
컴퓨터(예: 노트북)로 옮기면 될 수도 있다(그쪽 Smart App Control 상태에
달렸다).

## 6. 그동안 찾아 고친 구조적 버그 (재발 방지용 요약)

전부 `checker/`에 남아 자동으로 다음 영상부터 적용된다. 상세 재현 과정은
`docs/BACKLOG.md`에 있다 — 여기는 "무엇을, 왜"만.

- **whisper 세그먼트가 직전 말의 꼬리에 걸쳐 시작하면 인점이 그 앞 구간에
  붙었다**(2026-09-11, `timing.suggest_spotting`). 세그먼트가 실제 말보다
  몇백 ms~1초 일찍 시작해 이전 화자의 VAD 구간 끝에 걸치면 `min(s)`가 그 앞
  구간의 시작을 잡고 직전 아웃점에 걸려 멈췄다 — 진짜 온셋은 뒤 구간에 있는데.
  드라마B E02·E03 진짜 짝 370개 실측: VAD 온셋은 정답 인점 +80ms 안팎에
  있었는데 우리 인점은 -373/-475ms 일렀다. 자막 밖으로 더 많이 뻗은 앞자락·
  뒷자락 구간을 이웃 말로 보고 빼도록 고쳤다(하나뿐인 구간은 안 뺀다). 같은
  캐시로 다시 대조: 인점 |중앙| 248→79ms(E02)·228→68ms(E03), 100ms 안
  38→55%·38→58%.
- **재분할 경계를 글자 수 비례로 놓고 whisper 단어 시각을 버리고 있었다**
  (2026-09-11, `resplit._allocate_by_words`·`transcribe.Segment.words`).
  faster-whisper가 `word_timestamps=True`로 이미 단어 시각을 내는데 환각
  검사 켜는 용도로만 쓰고 버렸다. 이제 `Segment.words`에 싣고 캐시 옆
  `<캐시>.words.json`에 남기며, 재분할 조각 경계를 조각이 끝난 단어와 다음
  단어 사이 한가운데에 놓는다. 단어 열 글자 수가 텍스트와 10% 넘게 다르면
  (번역·대본 경로) 비례로 돌아간다. 실측: 재분할 경계 인점 |중앙| 73~90ms,
  100ms 안 52~55%. 옛 캐시(단어 없음)로도 생성은 된다 — 비례로.

- **"몇 개 중 몇 개를 봤나"를 셀 수 없었다**(2026-09-08,
  `rules/private/sources/작업자-자료/정독-기록.yaml` + `tools/reading_progress.py`).
  규칙 12는 "안 다룬 범위를 분명히 남긴다"고 정하는데, 기록이 산문
  (`이미지-판정.md`·`이미지-정독.md`)에만 있어서 셈이 불가능했다. 그래서
  `이미지-정독.md` 머리말이 오래도록 **"127장 전수 정독"**이라고 적혀 있었다 —
  세어 보니 `작업 기본 원칙.docx` 48장 중 **30장이 어느 기록에도 없다**(머리말은
  2026-09-08에 고쳤다).
  - 형식은 산문 기록을 대신하지 않는다. 저기에는 "무엇을 읽었나"가, 여기에는
    "몇 개 중 몇 개를 어느 상태로 처리했나"만 있다. 상태는 다섯 — 반영함·정독함·
    그룹판정("나머지 62장은 교정기 몫"처럼 묶음 판정)·안읽음(보고 규칙이 아니라고
    판정)·보류.
  - **미확인은 "안 봤다"가 아니라 "봤다는 기록이 없다"다**(규칙 3). 도구가 그
    수만 센다.
  - id를 매긴 자료는 id로, 안 매긴 자료(SubtitleEdit 15장)는 개수로 센다. 섞어
    적어도 된다.
  - `--strict`를 주면 미확인이 있을 때 1로 끝난다 — "다 봤다"고 주장하기 전에
    스스로 확인하는 자리다. 기본은 보고서(0).
- **`lanes_check`가 한글 경로를 통째로 못 봤다**(2026-09-08, 붙인 당일 발견).
  git은 기본값(`core.quotepath=true`)에서 한글 경로를 8진수로 감싸 내놓는다 —
  `"rules/private/sources/ì..."`. 그대로 받으면 `rules/private/sources/`로 시작하지
  않게 되어 이 저장소의 한글 폴더가 전부 갈래 판정에서 빠진다. 실제로 정독 기록
  형식을 넣은 커밋(문서 정독 + 코드, 두 갈래)이 조용히 지나갔다. `-z`와
  `core.quotepath=false`를 함께 주고, 그 파싱을 `parse_z_output()`으로 떼어 시험이
  한글 경로로 직접 확인한다.
- **규칙 12(갈래를 동시에 벌이지 않는다)가 글로만 있었다**(2026-09-08,
  `tools/lanes_check.py`). 커밋 직전에 staged 파일을 네 갈래(코퍼스·학습 / 문서
  정독 / 규정 / 코드)로 나눠, 둘 이상 섞였으면 어느 파일이 어느 갈래인지 짚어
  준다. `docs/`와 최상위 `*.md`는 갈래로 안 센다 — 어느 갈래든 기록은 함께
  남기기 때문이다.
  - 덤으로 규칙 12의 증거 기준도 짚는다: 학습값 파일의 `from.작품`이 한 편뿐인데
    같은 커밋이 `checker/`를 고치고 있으면 "한 작품의 습관을 코드로 굳히는
    중일 수 있다"고 말한다.
  - **막지 않는다**(종료 코드 언제나 0). 커밋 단위는 세션 단위가 아니라
    근사치이고, 코드 고침이 학습값 갱신을 정말로 함께 요구할 때도 있다.
  - 잔소리가 되지 않는지 먼저 쟀다 — 최근 80커밋 중 학습 자료와 코드가 섞인 것은
    1건, 정독 자료와 코드가 섞인 것은 0건이다. 드물게 뜨므로 뜨면 볼 값이 있다.
- **규칙 15(TC 먼저, 텍스트 나중)가 글로만 있었다**(2026-09-08). `--against`가
  TC 지표와 "텍스트가 가장 안 맞는 자막" 목록을 한 화면에 함께 내놓아서, 자막
  단위가 정답과 어긋난 상태에서도 텍스트부터 손보게 됐다. 실측(2026-08-27,
  예능A 15회 영어 번역 1453개 전수)에서 **텍스트가 안 맞아 보인 자리의
  상당수가 사실 `regroup.py`가 화자 교체를 못 보고 이어 붙인 TC 문제**였다 —
  TC를 나중에 고치면 경계가 새로 생겨 텍스트 교정을 두 번 한다.
  - 이제 텍스트 목록은 `--text-diff`를 줘야 나온다. 기본값은 TC 쪽만 낸다
    (유사도 가운데값은 그대로 낸다 — 숫자 하나는 거리만 알려 줄 뿐 자막을
    하나씩 손보게 만들지 않는다).
  - `--text-diff`를 주면 원장(`rules/private/corpus/corpus_status.yaml`)의 `tc_verified`를 찾아
    아직 '끝남'이 아니면 경고한다. 원장에 없으면 아무 말도 안 한다 — 모른다와
    안 끝났다는 다르다(규칙 3).
  - **막지는 않는다.** "TC가 충분히 맞았다"를 기계가 문턱값으로 정하지 않는다
    (규칙 4). 순서를 지키게 할 뿐, 넘어갈 때는 사람이 정한다.
- **단위 환산이 대사와 화면자막에서 갈린다**(2026-09-08, 작업자 자료 WORK-015·029
  반영. `non_metric_unit` 구현 — T12의 세 검사 중 하나였고 미구현이었다).
  자료가 두 자리에서 서로 다른 것을 말하는데 **모순이 아니라 대상이 다르다**:
  - 대사: "화폐를 제외한 모든 단위는 환산하지 않고 화자가 말한 대로 적는다"
    (`작업 기본 원칙`, `이미지-정독.md` 837행)
  - 화면자막: 반복 화면자막은 KNP `반복` 탭에 환산해 적어 둔다 — `O miles from OO`
    → `OO로부터 O km`, `OF` → `영하 OO도`(WORK-015·047). 표기 자료도 "미터법으로
    변환하되 전문 분야가 비법정 단위를 쓰면 그대로 둔다"고 적는다(653행)
  - 그래서 **화면자막으로 판별된 자막에만** 확인을 건다. 대사 줄은 안 본다.
  - **숫자가 앞에 붙은 것만 본다** — `노트`(공책)·`인치`(단위 이름 자체)처럼 단위가
    아닌 뜻으로 쓰인 자리를 지적하면 오답이 쏟아진다.
  - **자동 교정 안 함**(`auto: false`). 항공·컨테이너 `피트`, 미식축구·골프 `야드`,
    해양 `해리`·`노트`는 그대로 쓰는데 어느 분야인지는 영상을 봐야 안다(규칙 4).
    지적 문구가 그 사실을 함께 말한다.
  - 통화는 안 본다 — 환산 **금지** 대상이라 성격이 반대이고 `currency_converted`
    (C03, 여전히 미구현)의 몫이다.
  - 표식이 미정(`ask`)이면 무엇이 화면자막인지 알 수 없어 검사하지 않는다.
- **존반이 인물 × 인물 행렬이 됐다**(2026-09-08, 작업자 자료 WORK-048 반영).
  실무 KNP의 `존반` 탭은 가로·세로에 인물을 놓고 칸마다 `I`/`F`를 적으며 **방향을
  갖는다**(피비→로스와 로스→피비가 다를 수 있다). 우리 캐릭터 시트는 인물마다
  말투 한 칸(`declared_tone`)뿐이라 그 방향을 담을 수 없었다.
  - 시트에 `상대별 말투` 칸을 더했다(`로스=반말; 모니카=합니다체` 꼴 — KNP 행렬의
    **한 행**이 이 칸이다). `Character.tone_to`로 읽고 쓴다.
  - T17이 이 값을 먼저 본다. **상대는 자막 안에서 확인될 때만 가려낸다**(규칙 4) —
    대사 안에 다른 인물 이름이 나오는 줄만. 이름이 둘 이상 나오면 찍지 않고,
    대괄호 안(효과음·화자명)은 대사가 아니라 세지 않는다.
  - 상대를 못 가리는 줄에서는 **예전 그대로** 인물별 한 칸을 쓴다. 자막에 상대가
    표시되지 않는 것이 이 검사가 오래 미구현이던 이유이고, 그 사실은 안 변했다.
  - **행렬을 받아 놓고 한 번도 못 썼으면 그렇게 말한다**(`skipped`에 남긴다) —
    조용히 사람별 값으로만 돌면 사용자는 행렬이 걸린 줄 안다(규칙 3).
  - 배선은 `cast`와 **따로** 뒀다(`cast_pairs`). 옛 호출부(SE 플러그인·GUI)는
    `cast` 하나만 넘기므로 동작이 그대로다.
  - **감수 프롬프트에는 안 넣었다.** 프롬프트에 상대를 주면 모델이 관계를 추정하게
    되는데, 상대를 못 가리는 줄이 대부분이다 — 검사 쪽에서만 쓴다.
- **WORK-007(장면전환 fps별 표)은 이미 끝난 대조다 — 2026-09-08에 이것을 "확인
  대기"로 다시 올렸다가 되돌렸다.** 2026-08-31에 SE 공식 소스
  (`BeautifyTimeCodesSettings.cs`의 `Preset.Netflix`)로 대조해 **코드 변경 불필요**로
  결론이 나 있었다(근거는 `rules/private/sources/작업자-자료/이미지-정독.md`의 "장면전환 —
  SE '프로필 편집' 프레임 표" 절과 `checker/timing.py` 상수 옆 주석 양쪽에).
  - `최대 오프셋`·`안전 영역`은 SE의 Red/Green Zone이다 — 연결된 자막·체이닝
    우선순위를 가르는 **SE만의 2단계 UI 개념**이고 넷플릭스 공식 문서엔 그 구분이
    없다. SE 소스도 이 정수를 fps에 따라 바꾸지 않는다(프리셋은 fps 무관 고정값).
    이미지의 fps별 표는 SE 기능이 아니라 작성자가 `round(fps*0.5)`·`floor(fps*0.25)`로
    손계산해 둔 참고표다(모든 행이 그 식과 일치함을 그때 확인).
  - 반영해도 얻는 게 없다는 것도 확인했다(2026-09-08): red zone 7프레임은 24fps에서
    약 292ms라 우리 `SHOT_CLEARANCE_MS=500` 안에 이미 들어간다 — 그 구간 큐는 전부
    이미 스냅된 뒤다. fps별 환산도 `round(fps*0.5)`프레임 = 500ms라 지금과 같은 값이다.
  - **교훈은 규칙 17 그대로다.** `timing.py`의 그 상수 바로 옆에 결론이 적혀 있었는데
    안 보고 "대응 코드가 없다"고 새로 판단했다. 정독 기록을 새로 쓸 때도 기존 절
    (같은 파일 1173행)을 먼저 찾지 않아 같은 표를 두 번 적었다.
- **문서가 코드보다 낡아 끝난 일을 다시 할 뻔했다**(2026-09-08,
  `tools/docs_check.py`). 이 파일 7절이 `grep -rn "learned" checker/*.py` 0건이라
  <!-- docs-check: 무시 (그때 그렇게 적혀 있었다는 기록이다) -->
  적어 두고 "다음 세션은 여기부터"라고 붙들고 있었는데, 그 경로는 일주일 전
  (2026-09-01, `19141e2`)에 이미 만들어져 있었다. 규칙 17이 "손대기 전에 먼저
  열어 보라"고 지정한 파일이 낡으면 체크리스트가 오히려 함정이 된다.
  - 커밋 직전에 훅이 **문서가 스스로 증명할 수 있는 주장만** 확인한다: 백틱으로
    가리킨 저장소 경로가 실제로 있는가, "`grep ...` N건"이 지금도 N건인가,
    등록된 "지금 상태" 숫자(훅이 도는 시험 수 등)가 맞는가.
  - **과거 기록은 안 건드린다.** `BACKLOG.md`·`AGENT_INCIDENTS.md`는 그때 그랬다는
    기록이라 낡는 것이 정상이라 경로만 본다. "지금 상태" 숫자도 문장을 보고
    짐작하지 않고 `LIVE_COUNTS`에 손으로 등록한 것만 본다(규칙 4).
  - 시험을 보태면 그 숫자가 곧 낡으므로 `--fix-counts`로 한 줄에 맞춘다 — 막기만
    하고 고칠 길이 없으면 사람이 검사를 꺼 버린다.
  - 도망갈 구멍: 그 줄에 `docs-check: 무시`. 옆 리포(교정기) 경로는 애초에 안 센다.
- **문턱값 상수 셋의 근거가 없었다 — 처음 실측했다**(2026-09-08,
  `tools/vad_sweep.py`). `vad.detect_speech()`의 `threshold=0.5`·
  `min_speech_ms=120`·`min_silence_ms=250`은 VAD를 들인 첫 커밋(`3e49433`,
  2026-08-11)에 근거 없이 들어왔다 — 그 커밋은 "음량 검출 대 Silero VAD"는
  4편으로 재서 남겼지만 이 세 값 자체는 무엇과 견줬는지 적지 않았다.
  `resplit._snap_to_silence`의 `tolerance_ms=400`도 같다(`c49a4cc`).
  - **실측 방법**: 확률 열(`vad._probabilities`)을 한 번만 구해 놓고 값만 바꿔
    가며 정답 자막의 인점·아웃점과 견준다(추론 36~60초, 이후 스윕은 거의 공짜).
  - **결과 — 방향이 작품마다 뒤집힌다.** 드라마B(한국어 실사 드라마)는
    더 느슨한 쪽(0.3/80/100)이 낫고(E02 인 158→136ms, E03 117→102ms),
    영화A(영어 애니, 음악이 계속 깔림)는 더 빡빡한 쪽(0.7)이 두 배
    낫다(인 1481→729ms). 한 값으로 둘 다 맞출 수 없다.
  - **문턱값은 그래서 안 바꿨다**(규칙 12 — 일관되게 가리키는 것은 한 작품뿐이고
    다른 작품은 반대를 가리킨다). 진짜 답은 새 고정값 하나가 아니라 **장르·언어별로
    다른 값**일 가능성이 크다(`rules/genre/`가 이미 하는 일). **`min_silence_ms`만
    2026-09-11에 250 → 100으로 바꿨다** — 세 자료 전부 100을 골랐고 Seoul Corpus
    240파일이 네 번째로 같은 방향을 가리켰다(8절 "Seoul Corpus").
  - **`tolerance_ms=400`**: 정답 경계가 침묵 한가운데에서 떨어진 거리는 중앙값
    582~1683ms였고, 400ms 안에 드는 경계는 20~36%뿐이다 — 지금 폭은 경계의
    3분의 1 정도만 당긴다. 넓히면 더 당기지만 글자 수 비례 위치에서 멀어지는
    대가를 치른다. 어느 쪽이 정답에 가까운지는 `--against`로 최종 TC를 견줘야
    안다(이 숫자만으로는 모른다).
- **규칙 13(정답지가 있으면 학습이 먼저)이 글로만 있었다**(2026-09-08,
  `checker/answerkey.py`). 두 번 어겼다 — 정답지 있는 회차의 초안을 3차 윤문까지
  입혀 "완성본"으로 전달(`docs/AGENT_INCIDENTS.md` 5번), `[습작] SDH+번역/`의 새
  영상을 "정답지 없다"고 단정하고 5회차를 whisper로 생성(규칙 17 다섯 번째 사고).
  두 번째는 `media.list_subtitle_streams`(영상 **안**의 자막 트랙)로 막았고, 이번에
  나머지 절반을 막는다 — 영상 **밖**에 우리가 이미 추출해 둔 정답지. `--generate`가
  영상을 열기 전에 `rules/private/corpus/corpus_status.yaml`(`video:` 경로가 그대로 적혀 있어 가장
  확실하다. `truth:`와 이미 끝난 단계까지 함께 안다)과 `학습한 TC 및 자막 모음/`
  (`<플랫폼>_<제목>/E<회차>_...srt` — 제목·회차를 이름에서 맞춘다)을 본다. CLI와
  GUI 둘 다에 붙였다. **막지는 않는다** — 제목 맞추기는 파일 이름에서 하는 추정이라
  이름만 비슷한 다른 작품을 막으면 안 된다(규칙 4). 같은 회차가 없고 다른 회차만
  있으면 그렇게 말하고, 상관없는 영상에는 아무 말도 하지 않는다(경고가 흔해지면
  아무도 안 본다).
- **경고가 CLI에만 있었다 — 플러그인·GUI는 잘못 고른 프로파일을 몰랐다**
  (2026-09-08). `detect.mismatch_warning`("쿠팡 표기인데 넷플릭스로 검사했다")이
  `cli.py`에만 있어서, SE 플러그인과 GUI는 같은 파이프라인을 쓰면서도 이 경고를
  한 번도 못 냈다. `pipeline.correct_and_check`로 옮겨 세 입구가 같은 경고를
  받는다. **자동 교정 전 `events`로 본다** — 교정 뒤 자막으로 보면 방금 고른
  프로파일 쪽으로 표기를 바꿔 놓은 자기 출력물을 되읽는 꼴이라(점 셋 -> … 교정이
  그대로 넷플릭스 근거가 된다) 경고가 원리적으로 안 뜬다. `cli.py`가 실제로 그
  상태였다. 함께 고친 것: SE 플러그인의 `applyFixes` 기본값을 `false`로
  (규칙 7 — 전에는 설정을 한 번도 안 건드린 작업자의 자막이 넷플릭스 기본
  프로파일로 **실제로 고쳐졌다**), `platform`·`kind`를 정한 적이 없으면 그 사실을
  메시지에 붙이고(SE가 설정을 왕복시켜 주므로 "고른 칸 이름"을 `_chosenKeys`로
  함께 왕복시켜야 두 번째 실행부터도 구분된다), `config.json`이 깨져 무시된 것도
  말한다. GUI는 규정 파일을 하나도 못 찾았을 때 `["netflix"]`를 끼워 넣던 것을
  없애고(없는 것을 있는 것처럼 보였다), OCR 언어를 정한 적 없으면 영어로 읽는다고
  알린다.
- **기본값이 결정인 척했다**(2026-09-08, `checker/decisions.py`). `-p`를 안
  주면 argparse가 `netflix`를, `-k`를 안 주면 `translation`을 조용히 채웠고,
  화면에 찍히는 `프로파일: netflix ko translation` 한 줄은 **사람이 고른
  것과 기계가 채운 것을 구분하지 못했다.** 규칙 1의 ⓪(발주처 확인이 가장
  먼저)이 도구 안에서 사라지는 자리였다. `--generate`에서는 더 나빴다 —
  `detect.mismatch_warning`이 유일한 방어막인데, 생성 경로에서는 넷플릭스
  프로파일로 자동 교정해 쓴 **자기 출력물**을 되읽으므로 경고가 원리적으로
  안 뜬다(순환). 고침은 값을 바꾸지 않는다(예전 명령이 그대로 돌아야 한다)
  — `decisions.resolve()`가 똑같이 채우되 출처(user/default/file/none)를
  남기고, `--generate`·`--dry-run` 앞머리에 결정표를 찍고, 리포트에도
  `decisions`로 남긴다. 규칙 11이 `origin: learned`를 지우지 않는 것과 같은
  방식이다. 함께 보이게 한 것: 장르 미지정(리포트에 한 줄 — `--genre variety`를
  두 세션 연속 빠뜨린 규칙 17 사고 ③), `--translate`인데 캐스트 시트가 없어
  인물 관계를 모른 채 존댓말로 통일되는 것(규칙 16). **`preflight.py`와 층이
  다르다** — 저기는 환경(ffmpeg·모델·디스크)이 준비됐는지 보고, 여기는
  결정이 내려졌는지 본다.

- **whisper 무음 환각**: 완전한 침묵에서도 whisper가 자신 있게 글자를
  지어낸다. 말소리 구간(VAD)과 전혀 안 겹치는 전사 조각을 걸러낸다. 이
  필터를 만들다가 **VAD 자체가 도중에 멎는** 경우(컨테이너 손상 등)를
  놓쳐 실제 대사 6분을 통째로 지운 회귀를 자체 발견·수정했다 — "검출기가
  못 찾았다"와 "검출기가 멎었다"는 다른 사건이다.
- **다중 화자 병합 폭주**: `regroup.py`가 whisper 조각을 합칠 때 화자
  교체를 못 봐서, 빠른 대화 주고받기를 한 자막에 뭉쳤다. 스포팅 아웃점이
  거기서 수백~수천ms 밀렸다. 1차로 `max_shift_ms` 상한(2026-08-27)으로
  막았고, 근본 원인은 화자 분리 도입(5번 표)으로 해결 중.
- **재분할이 문장 경계를 안 봄**: 글자 수가 남으면 서로 다른 발화 여러 개를
  한 자막에 묶어 냈다. `force_sentence_split`으로 문장이 끝나는 자리는
  글자 수와 무관하게 가른다.
- **번역 출력에 형식 오염**: 모델이 번역문 전체를 마크다운(`**...**`)으로
  감싸거나, `원래 번역 → 고친 번역` 형식의 자기수정을 그대로 남기거나,
  `**참고:**` 같은 설명을 덧붙였다. 셋 다 프롬프트에 이미 있던 형식(용어집
  힌트가 `A → B`를 씀)을 모델이 따라 하다 생긴 오염으로 보인다. 전부 후처리
  단계에서 벗겨낸다(`checker/translate.py`).
- **검사기가 미구현 항목을 조용히 통과시킴**: `--check` 모드는 미구현 검사
  목록을 보여줬는데 `--generate` 모드는 그 줄 자체가 없었다 — 검사 안 한
  것이 "위반 없음"처럼 보였다. 두 경로가 같은 정보를 내도록 맞췄다.
- **`--against`의 가짜 짝짓기**: 시간이 가장 가까운 것끼리 기계적으로
  짝짓다 보니, 자막 개수가 다르면(캡션을 우리가 못 만드는 등) 전혀 다른
  대사끼리 짝지어져 통계가 왜곡됐다. 캡션 제외 + 유사도 하한 + 길이 하한으로
  거르는 절차를 세웠다(`정답지-대조-진단` 스킬).
- **`--generate --passes`가 조용히 1차만 하고 2차·3차를 건너뜀
  (2026-08-27 밤)**: `stage_revise` 호출이 cli.py의 다른 분기("받은 TC에
  번역만 얹는" 경로, `--generate` 없이 `--translate`만 켠 경우)에만 있었다.
  `--generate --translate --passes 3`를 켜면 그 조건이 거짓이 되어 2차·3차
  전체가 에러 없이 스킵됐다 — 로그에 "2차"·"3차" 진행 표시가 아예 없는데도
  아무 경고가 안 났다. `generate()` 함수 자체에 `passes`를 붙여 어느 CLI
  분기를 타든 같은 경로를 쓰게 고쳤다.
- **2차·3차 결과에 `[pass 2]`·`**Notes:**` 같은 모델 메타 설명이 그대로
  남음(2026-08-27 밤, 위 버그를 고친 뒤 실전 검증 중 발견)**: 원인 둘.
  (1) `revise.py`의 표지 제거 정규식이 문자열 **맨 앞**만 봤는데,
  `_parse_numbered`가 번호 없는 줄을 앞 번호에 이어 붙이므로 표지가 합쳐진
  문자열의 **중간 줄**에서 시작할 수 있었다 — 줄마다 보게 고쳤다.
  (2) `revise.py`에는 1차용 흘림 걸러내기(`_strip_trailing_notes`,
  `_strip_markdown_wrap`)가 아예 없었다 — 표지 제거 정규식 하나뿐이었다.
  2차·3차도 같은 모델이 내는 같은 흘림을 그대로 겪는다는 걸 놓쳤다. 재검증
  (v12)에서 흘림 0건 확인.
- **`revision_report`가 여러 회차를 첫 회차 이름 하나로 합산**: "2차에서
  1207개를 고쳤습니다"처럼 나왔는데, 2차 대상 자막이 1114개뿐이라 애초에
  2차 혼자 낼 수 없는 숫자였다 — 2차+3차 합산값을 `revisions[0].stage`
  이름으로만 세고 있었다. 회차별로 나눠서 센다.
- **드라마 병합 상한이 예능 기준(2초)을 상속 안 받고 기본값(4초)에 머묾
  (2026-08-30, 드라마B E01)**: `rules/genre/variety.yaml`은 590행
  근거로 `timecode.merge_max_ms: 2000`을 이미 걸어 뒀는데 `drama.yaml`엔
  이 키가 없었다 — 드라마 권장(2~3초)이 `spotting.recommended_ms`에
  적혀만 있고 병합 단계엔 안 이어졌다. 표시 시간이 정답의 거의 2배(2900~
  4400ms vs 1794~2127ms)로 나온 원인이었다. `merge_max_ms: 3000`(권장
  상한)을 넣었다.
- **whisper 필터가 신뢰도를 안 줘서 환각을 못 걺**: ffmpeg 내장 whisper
  필터는 srt·json 어느 출력에도 `avg_logprob` 같은 신뢰도 값이 없다(직접
  테스트로 확인). 전사 백엔드를 faster-whisper(ctranslate2)로 바꿔 신뢰도를
  받게 했다. 신뢰도만으론 부족했다 — 언어를 강제 지정(`--whisper-lang
  ko`)하면 정확히 옮긴 외국어 대사도 신뢰도가 낮게 나온다. **신뢰도 낮음 +
  글자 밀도(3 CPS) 낮음이 같이 있을 때만** 환각으로 본다(정밀도 위해
  재현율을 일부 포기하는 절충).
- **외국어 대사가 번역 안 되고 그대로 남음**: 정답 SDH는 일본어 대사를
  전부 한국어로 옮기고 `[남자가 일어로]`처럼 언어 표시를 붙이는데, 우리는
  전사만 하고 안 옮겼다(disney/ko-sdh.yaml의 `translate_if_plot_pertinent_
  and_foreign`이 이미 있었는데 화면자막용인 줄 알고 무시했었다 — 실제로는
  대사에도 적용됨을 정답 대조로 확인). `checker/foreign_dialogue.py`를
  새로 만들어 로컬 번역기로 옮기되, 화자 이름은 지어내지 않고(`[일본어]`만
  표시) 옮긴 자리마다 notes에 "확인 필요"를 남긴다. 번역 결과에 한글이
  하나도 없으면(모델이 지시를 무시하고 원문/다른 외국어를 돌려준 경우)
  번역 실패로 보고 원문을 그대로 둔다.
- **문맥 불일치 검사(`--check-context`)가 이미 있었는데 안 쓰고 있었음**:
  whisper가 비슷한 소리를 다른 말로 잘못 듣어도 문법은 멀쩡한 문장을 낸다
  — `context_check.py`가 로컬 LLM으로 앞뒤 문맥과 맞는지 보는 기능인데
  CLI 플래그(`--check-context`)를 아무도 안 켜고 있었다. 켜니 E01에서
  83곳 검출 — 신뢰도·글자밀도 기반 환각 검사(63곳)와 거의 안 겹친다.
  둘은 서로 다른 오류를 잡는다: 통계 검사는 "시간 대비 글자가 적다", 문맥
  검사는 "문법은 되는데 뜻이 안 통한다".
- **whisper 전사가 같은 영상·같은 명령에도 매번 다르게 나옴**: faster-whisper
  전환 후 실측(같은 명령 5번) — 세그먼트 수 290·705·709·747·756개.
  4번은 700대에 몰렸는데 1번(290)만 확 튀었고, 그때가 우리 자체 VAD(결정적)가
  찾은 말소리 구간(660개)보다도 적었다. "세그먼트가 말소리 구간의 90%
  미만이면 실패로 의심"해 한 번 더 전사하고, 매 실행 결과를
  `whisper_retry_log.jsonl`에 남긴다(표본이 적어 임계값은 가설 — 데이터
  쌓이는 대로 재검토 예정).
- **"VAD가 못 잡으면 whisper가 지어낸 것"이라는 전제가 배경음악 있는
  구간에서 깨짐(2026-08-31, 영화B en-sdh 정답 대조)**: VAD 안
  겹치는 조각을 조용히 지우던 필터가 배경음악 깔린 65초·18초 구간에서
  whisper가 정확히 받아 적은 진짜 대사("Hello!"·"Where is the fuel?"·
  "Wait." 등)까지 함께 지웠다. 게다가 그 자리 주석은 "지우지 않고 알린다"고
  적혀 있었는데 실제 코드는 지우고 있었다(규칙 4 위반). `generate.py`의
  `has_vad_support()`로 분리해 **지우지 않고 표시만** 하도록 고쳤다 —
  기존 환각 의심(밀도·신뢰도) 검사와 별개 목록으로 알린다. 같은 whisper
  캐시로 고치기 전/후 재생성해 확인: 짝지음 1198→1503개(54.3%→68.1%),
  세 예시 구간 중 둘은 대사가 살아났고 나머지 하나(220~240초, "Red
  leather, yellow leather.")는 원인이 달랐다 — whisper 자체가 그 대사를
  옆 문장에 통째로 삼켜 애초에 전사를 안 했다(VAD와 무관, 다른 갈래의
  한계). 부작용: "군더더기"(정답에 없는 우리 자막) 166→345개로 늘었다 —
  전엔 지워서 안 보이던 진짜 환각이 이제 표시만 되어 최종 자막에 남기
  때문이다(사람이 편집 중 지워야 한다).
- **whisper의 "유명한" 침묵 환각 문구는 텍스트만으로도 판정 가능해서
  자동으로 지운다(2026-08-31, 위 항목 부작용을 영화A로 검증하다
  발견)**: 위 VAD 고침 뒤 영화A 오프닝에서 "Transcribed by ESO,
  translated by —"·대시(`—`)만 있는 조각이 수십 개 드러났다 —
  whisper.cpp·openai-whisper 커뮤니티에 널리 보고된, 유튜브 자동자막 학습
  흔적으로 보이는 환각이다. VAD 안 겹침과 달리 이건 **오디오 추정이 아니라
  텍스트 자체로 확인되는 사실**이라(규칙 4) `_is_known_hallucination()`으로
  자동으로 지운다(문구 사전 매칭 + 글자다운 글자가 하나도 없는 조각).
  영화A에서 117개 지움. **주의**: 이 고침 뒤 `--against` 짝지음
  숫자가 1187→1117개로 오히려 줄었다 — 퇴보가 아니라 스킬 4번이 경고한
  "가짜 짝짓기"가 걷힌 것이다(직접 확인: 지워진 문구 다수가 근처 진짜
  정답 대사와 시간만 가까워 억지로 짝지어져 있었다. 그래서 유사도 중앙값은
  오히려 올랐다, 0.46→0.50). **짝지음 개수만 보고 판단하지 않는다** —
  군더더기 개수(111→94)·유사도 중앙값을 같이 봐야 한다.
- **whisper가 20~30초씩 통째로 환각 루프에 빠져 그 구간 실제 대사를
  전부 잃는 사례(2026-08-31, 영화B·영화A 정답 대조에서
  총 3번 확인)**: 위 두 항목으로는 못 막았다 — 텍스트가 진짜(영화B
  "Red leather, yellow leather.")거나 유명 문구라 지워지긴 해도(영화A
  "Transcribed by —" 두 곳, 27~30초) 그 밑에 깔린 실제 대사 자체를
  whisper가 애초에 안 만들었기 때문이다. `checker/transcribe.py`의
  `_faster_whisper_transcribe()`에 `condition_on_previous_text=False`
  (직전 텍스트로 다음 조각을 조건화하지 않음 — 한 번 헛것을 뱉으면 다음
  조각도 그 문맥을 물고 환각을 반복하는 전이를 끊는다)와
  `hallucination_silence_threshold=2.0`(`word_timestamps=True`가 있어야
  작동, faster-whisper 자체 기능 — 무음에서 환각이 이어지면 건너뜀) 둘을
  추가했다. 영화A로 검증(같은 영상, 새로 전사): 두 환각 루프 구간
  모두 실제 대사로 복원 확인(정답 15개 중 11개, 13개 중 13개 커버).
  전체 지표도 같이 좋아졌다 — 짝지음 1117→1456(51.4%→67.0%), 유사도
  중앙값 0.50→0.77, 인점 편향 +240ms→+60ms, 아웃점 +771ms→+86ms(앞서
  "짧은 리액션 컷 과다연장"으로 보였던 것도 이 환각 루프가 근본 원인이었다
  — 같이 사라짐). **주의**: faster-whisper 로드가 Smart App Control로
  간헐적으로 막힌 적 있다(`DLL load failed`) — 실패하면 ffmpeg 내장
  whisper로 조용히 대체되는데, 그쪽엔 이 두 옵션이 없다. 로그에
  "(faster-whisper)" 표시가 있는지 확인하고 결과를 판단할 것.
  **영화B로 재검증(2026-08-31, 같은 날)**: 원래 문제였던 세
  구간(125~190초·220~240초·378~396초) 전부 실제 대사로 복원 확인 — 고침
  자체는 두 작품 모두에서 검증됨. 다만 **전체 지표는 오히려 살짝
  내려갔다**(짝지음 68.1%→64.3%, 인점 편향 +90→+135ms) — 새 버그가
  아니라 두 가지가 겹친 결과로 판단: ① whisper 전사는 같은 영상도 실행마다
  달라진다(이미 위에 기록된 특성), ② `condition_on_previous_text=False`가
  문맥 참고를 끊은 대가로 애매한 대사를 더 잘못 듣는 사례를 실측함
  ("This is first contact!"를 "this is cursed contact"로). 8초 넘는 긴
  세그먼트가 35개 생겼는데(그레이스의 긴 혼잣말 — 영화B는 예능처럼
  화자 하나가 오래 말을 잇는 장면이 많다) `resplit_all`이 이미 잘 쪼개고
  있어 이건 구조 문제가 아님(직접 확인, 13개 대 정답 10개로 잘 나뉨).
  **결론**: 대사 통째 손실이라는 원래 문제는 고쳐졌고, 아깝지만 감수하는
  대가(가끔 오청)가 있다 — 유리하게만 보고하지 않는다(규칙11 보고 방식과
  같은 정신). 더 깊이 팔 신호는 안 보인다.
- **한국어 팬섭 크레딧·유튜브 끝인사도 whisper의 "유명한" 환각 문구다
  (2026-08-31, 드라마B E02~05 개별 조사 — E01만 깊이 보고
  나머지는 안 봤던 것을 이번에 채움)**: "한글자막 by 한효정"이 4회차
  전부, "다음 영상에서 만나요."가 3회차(E02·E04·E05)에서 그대로
  반복됨 — 영어 문구(위 항목)와 같은 부류, `KNOWN_HALLUCINATION_PHRASES`에
  추가. 최종 수치: E02 73.7%, E03 80.8%, E04 73.8%, E05 78.0% 짝지음(정답
  대비) — E01(59.5~73.7%)보다 오히려 조금씩 높다. **E01 결론(코드 버그
  8개)이 나머지 4개 회차에도 통한다는 가정은 대체로 맞았다** — 새로 나온
  구조 버그는 이 문구 하나뿐이고, 나머지 어긋남은 사투리 오청(부산
  사투리 "우예 됐노"→"왜 됐나" 등)·SFX-only 자막·타이틀 카드("메이드 인
  코리아")처럼 이미 아는 범주였다.
- **장면전환 스냅이 번역 자막에도 걸리고 있었다(2026-08-31)**: 2026-08-28에
  넷플릭스 공식 문서("applicable to all timed text files")를 근거로 `--generate`가
  SDH·번역 구분 없이 장면전환 스냅을 걸도록 고친 적이 있는데, 실무는 그렇지
  않다는 사용자 확인을 받았다 — TC 작업 뒤 **SDH를 만들 때만** SE에서 장면전환을
  지정하고, 번역 자막은 그 단계 없이 바로 번역에 들어간다. `checker/cli.py`·
  `generate.py`가 이제 `shot_change.applied`와 `kind == "sdh"`를 함께 본다.
  `rules/*/common.yaml`의 공식 인용문 자체는 안 고쳤다 — 문서 범위와 이 도구의
  적용 범위가 다른 것뿐이다(규칙 3). 같은 회차에 SE 공식 GitHub 소스
  (`BeautifyTimeCodesSettings.cs`)로 대조해, 기존 `SHOT_CLEARANCE_MS=500`·
  `SHOT_OUT_LEAD_FRAMES=2` 상수가 SE 넷플릭스 프리셋과 이미 정확히 같다는 것도
  확인했다(`rules/private/sources/작업자-자료/이미지-정독.md` 참고).
- **번역기가 목표 언어에 한국어를 섞어 낼 때가 있다(2026-08-31, 영화A·
  영화B 번역 B층 심화)**: 지금까지 8개·6개 언어 번역이 1라운드만
  확인됐던 것을 대표 4개(영화A fr·sv, 영화B ja·de)로 다시 대조했다.
  영화B 일본어에서 1434개 중 93개(6.5%)가 한국어 오염 — "(참고: ...)"
  한국어 메타 설명을 덧붙이거나("Jag kommer att övervaka dig nog 좋습니다"),
  단어 하나가 통째로 한국어로 남는 경우("유도 혼수상태にありました") 둘 다
  나왔다. fr/sv/de도 3~16개씩. `translate_events()`에 `target_lang != "ko"`인데
  결과에 한글이 있으면 표시하는 검사를 추가했다(한국어만 잘라내면 문장이
  깨지므로 고치지 않는다, `foreign_dialogue.py`와 같은 논리).
  **나머지 어긋남은 대부분 구조 버그가 아니라 번역 스타일 차이였다** —
  예: 영화A 스웨덴어 정답은 "Star Command" 무전 장면을 "Rymd…"/"…centralen."
  처럼 라디오 잡음 효과로 단어를 쪼개 넣는데(이미 원어 SDH 조사에서 이
  장면 자체가 ASR로 어려운 오디오 처리 효과가 있다고 확인됨), 우리는 그냥
  글자 그대로("Stjärna." "C-Kommando.") 옮겼다 — 화면·소리 맥락을 알아야
  하는 창작적 선택이라 코드로 못 고친다.

**커밋 이력 메모(버그 아님, 2026-08-31)**: `git log`로 `CLAUDE.md`·
`rules/private/sources/작업자-자료/반영-계획.md`·`이미지-정독.md`·`이미지-판정.md`·
`작업 준비 및 기본 규칙.docx`/`.txt`의 이력을 찾으면 커밋 `cf69783`("SFX
2단계... + CLAUDE.md 출처 갱신")에 잡힌다 — 이 커밋은 실제로는 SFX
작업과 무관한, 5번째 구글독스(작업 준비 및 기본 규칙) 이미지 14장 정독
작업이 다른 동시 세션의 SFX 커밋에 함께 묶여 들어간 것이다. 내용은
멀쩡하다(diff 확인함). 이미 push된 공유 브랜치라 히스토리를 다시 쓰지
않기로 했다(위험 대비 이득이 적음, 사용자 확인) — 커밋 메시지만 보고
"SFX와 상관없는 파일이 왜 여기?"라고 헷갈리면 이 문단을 본 것으로 안다.

**같은 부류 두 번째 사례(2026-08-31, 같은 날)**: `checker/cli.py`의
`--sfx` 가드 수정(`-k sdh` 확인 추가)과 `tests/run_tests.py`의 SFX
관련 시험 추가분이, 커밋 안 하고 작업 중이던 사이에 다른 동시 세션의
하드섭 커밋(`e49bc97` "하드섭 TC 정밀화 재작업")에 함께 묶여 들어갔다
— 아마 그쪽이 `git add -A`류로 넓게 스테이징했을 것이다. 내용은
멀쩡하다(테스트 894건 통과 확인, `checker/generate.py`·`checker/sfx.py`는
정상적으로 내 커밋 `8c76a6f`에 남음). 이미 push됨 — 위와 같은 이유로
히스토리를 다시 쓰지 않는다. **여러 세션이 같은 작업 폴더를 동시에 쓸 때
반복되는 위험 패턴이다** — 커밋 전에 `git status`로 남의 변경이 섞여
있는지 확인하는 습관이 필요하다(이 세션은 이후로 커밋 직전마다 그렇게
했다).

## 7. 미해결 — 다음 사람이 볼 것

- **일본어 2차·3차 검수 〔2026-09-08 만듦〕. 그 외 언어는 아직 없다.**
  `checker/revise.py`는 한국어·영어뿐이었다. 2026-08-31 조사에서 **프랑스어는 손댈
  필요가 없고**(중앙값 31자 대 정답 33자) **일본어는 심각하다**(중앙값 24자 대 정답
  13자, 81%가 30% 이상 김)는 것이 나왔는데, 그때는 1편 표본이라 보류했다.
  - **2026-09-08에 정답 자막 4작품 21,888개로 다시 쟀다**(넷플릭스 예능B 예능
    13,323 / 디즈니 드라마C 드라마 4,399 / 디즈니 다큐A 심판의 날 다큐 2,558 /
    블루레이 영화B 영화 1,608). 발주처 넷·장르 넷이 같은 방향이다
    (규칙 12 충족).

        항목                   예능   드라마   다큐   영화
        한 자막 글자수 중앙값     9      9      15     7
        한 줄 글자수 중앙값       7      8       9     7
        한 줄짜리 비율          63%    81%     45%    84%
        です/ます 종결           9%    17%      0%     1%
        「。」「、」 포함          0%     0%      0%     0%

  - 길이만의 문제가 아니었다 — **정답은 です/ます를 거의 안 쓰고 구두점을 아예 안
    쓴다.** 그래서 2차는 상체(常体)를 기본으로 잡고, 3차가 문말 です·ます 제거·조사
    생략·구두점 금지·한 화면 7~15자를 본다.
  - **1차 프롬프트도 함께 고쳤다.** 전에는 "관계를 모르면 정중체로 통일"이었는데,
    그러면 1차가 만든 です/ます를 2·3차가 전부 되돌려야 한다 — 애초에 만들지 않는
    쪽이 싸다.
  - **아직 안 한 것**: 실제 영상으로 돌려 정답과 대조하지 않았다(프롬프트만 넣었다).
    다음에 일본어 정답지가 있는 영상으로 `--against`를 돌려 중앙값이 실제로 정답
    쪽으로 움직이는지 확인해야 한다. 그 전까지는 **근거 있는 프롬프트**이지 검증된
    개선이 아니다.
  - 나머지 언어(프랑스어 등)는 여전히 2·3차가 없다. 프랑스어는 실측상 필요도가
    낮았고, 다른 언어는 아직 안 쟀다 — 언어별로 다르므로 뭉뚱그리지 않는다.
  - 같은 대조에서 본 별개의 버그는 그대로다: 일본어 번역 하나가 영어·일본어·키릴
    문자가 뒤섞인 완전히 깨진 문장을 냈다("idiots why everyone **ああ、...
    ゴールドилиックゾーン..."). 지금의 한글 오염 검사로는 못 잡는다(한글이 없어서).
    1건뿐이라 코드로 고치지 않았다.
- **영상 파일 자체 손상은 복구 불가.** 예능A 15·16회처럼 컨테이너가
  손상된 자료는 whisper가 그 지점에서 짧게 결손을 낸다(46~103개 지점,
  9~10% 대사 손실). 코드로 못 고친다 — 근거만 `rules/learned/`에 남기고
  받아들인다.
- ~~whisper 반복 환각(hallucination loop)에 대한 코드 차원 대응이 없다~~
  〔2026-08-31 절반 풀림〕. 2026-08-27 예능A 15회에서 "같은 짧은
  구절이 비정상적으로 여러 번 반복 전사되는" 사례를 처음 봤을 때는
  다중 화자 병합 버그로 오판했다가(`--diarize`로 확인해 보니 pyannote가
  애초에 한 명이 계속 말하는 구간으로 봄 — 화자 교체가 아니라 whisper
  자체의 반복 환각), 최소 2편 근거가 없어(규칙 12) 코드 버그로 승격은
  안 했었다. **이번 세션(2026-08-31)에서 영화B·영화A로 정확히
  같은 부류(문맥 전이로 인한 반복 환각)를 3번 재현·확인하고 고쳤다** —
  `_faster_whisper_transcribe()`에 `condition_on_previous_text=False`+
  `hallucination_silence_threshold=2.0`(6절 참고). "절반"인 이유: 예능A
  15회 영상은 이미 삭제돼(규칙 13) 그 특정 사례로 재검증은 못 한다 —
  다만 기전(문맥 전이로 반복 환각이 퍼지는 것)이 같은 부류라 이 고침이
  일반적으로 커버할 것으로 본다(확인이 아니라 추정, 규칙 4 — 다음에
  비슷한 증상이 다시 보이면 이 고침 뒤에도 남는지 확인할 것).
- **LoRA(PEFT) 미시작.** 위 5번 표 참고. `corpus/pairs/`에 쌓는 중이고
  아직 학습 스크립트·평가 방법 자체가 없다 — 이건 데이터가 아니라 인프라가
  없는 상태다.
- **`--diarize`는 실전 검증 끝났고 정상 작동한다(2026-08-27).** 다만 검증
  과정에서 진짜 버그 하나를 더 찾아 고쳤다 — `merge_cues()`가 화자를
  비교할 때 `previous.end_ms`와 `event.start_ms`를 봤는데, 간격이 0인
  병합(whisper 조각이 딱 붙은 가장 흔한 경우)에서는 이 둘이 같은 시각이라
  비교가 항상 무의미했다. 각 조각 자체의 중간 지점을 비교하도록 고친 뒤
  실제로 병합 개수가 바뀌는 것을 확인했다(예능A 15회: 1103→1114).
  **교훈**: 유닛 시험은 통과했지만(간격 50ms로 짜서 이 사례를 못 잡았다)
  실전 데이터로 돌려 보고서야 드러났다 — 화자 정보처럼 실제 값의 분포가
  중요한 로직은 합성 시험만으로 부족할 수 있다.
- **화자 이름을 못 채운다(의도적 설계, 버그 아님).** 화자 분리는 "1번·2번
  화자가 다른 사람"까지만 안다. 실제 이름은 대본이 있어야 하고, 없으면
  사람이 영상을 보고 채운다 — 이름을 추측해서 채우지 않는다(규칙 3).

## 8. 데이터 — 전부 로컬 전용, 이유는 저작권

| 폴더 | 내용 | git |
|---|---|---|
| `[습작] SDH+번역/` | 학습용 원본 영상(정식 발매) | 제외 |
| `학습한 TC 및 자막 모음/` | 정답 자막(정식 번역문) 추출본 | 제외, 별도 비공개 백업 |
| `corpus/pairs/` | LoRA용 (우리 초안, 정답) 짝 | 제외 |
| `rules/learned/` | 정답에서 관측한 **통계값**(원문 아님) | **포함** — 저작권 있는 원문이 아니라 숫자이므로 |

`학습한 TC 및 자막 모음/`은 사람이 확정한 값만 옮긴다 — 기계가 재구성한 값을
섞지 않는다(형식이 이상한 자료는 그냥 학습하지 않고 넘어간다, 규칙 11).

### 검토했고 안 쓰기로 한 외부 데이터셋

다음 세션이 같은 것을 또 검토하지 않도록 남긴다(규칙 17). **여기 있는 것은
"살펴봤더니 안 맞더라"이지 "쓸모없다"가 아니다** — 판정 근거를 함께 적어 두는
이유는, 우리 쓰임새가 달라지면 결론도 달라질 수 있어서다.

| 자료 | 검토일 | 판정 | 근거 |
|---|---|---|---|
| AI Hub `71699` 한국어 텍스트-비디오-사운드 (50만 건, 3.29TB) | 2026-09-10 | **안 쓴다** | `start`/`end`가 말소리 경계가 아니다 (아래) |

AI Hub 71699을 본 이유는 `checker/vad.py`의 문턱값 셋이 **한국어 실사 쪽 근거가
한 작품(드라마B)뿐**이라 규칙 12의 "최소 2편"에 못 닿아 있기 때문이다.
한국어 실사 영상 + 초 단위 `start`/`end`가 대량이라 그 빈칸에 맞아 보였다.

샘플(경량) 30편을 받아 `tools/timestamp_origin.py`로 재 봤더니 **말소리 경계가
아니었다.** 전사 텍스트를 문장 단위로 자른 **분할점**을 시간축에 옮긴 것이다.

- 구간 사이 간격의 **95%가 정확히 +10ms** (114/120). 사람이 찍으면 이렇게 안 나온다
- 첫 구간이 정확히 `0.000`에서 시작: **30/30**. 마지막 `end` == 클립 길이: **25/30**
- 즉 구간이 클립을 빈틈없이 덮는다 = 침묵이 어디에도 안 적혀 있다

영상으로 대 봤다(`ffmpeg silencedetect`, -32dB/0.3s). `KNJ_03518`은 JSON이
12.056에서 끊고 12.066에서 다시 시작하는데 실측 무음은 11.051~13.241이라 실제
말은 13.241에야 재개된다(**1.2초** 어긋남). `MYQ_13422`는 구간이 16.230까지인데
말은 14.287에 끝난다(**1.9초**를 물고 있다).

`checker/vad.py`의 현재 인점 오차가 117~158ms다. 이걸 정답으로 삼으면 **10배 나쁜
기준에 맞춰** 튜닝하게 된다. 그래서 안 쓴다.

**남는 것**: 판별 도구 `tools/timestamp_origin.py`. 외부 라벨 자료를 정답으로
쓰기 전에 먼저 돌린다 — `tools/vad_sweep.py`가 "이 값이 정답과 얼마나 가까운가"를
재기 **전에**, "이걸 정답으로 써도 되는가"를 묻는 자리다. 이 도구의 첫 판은
`end[i] == start[i+1]`(간격 0)만 봐서 이 자료를 **놓쳤다** — 간격이 0이 아니라
10ms로 *일정*했기 때문이다. 0인지가 아니라 한 값에 쏠렸는지를 보도록 고쳤다.

**아직 빈 것**: `checker/vad.py`의 **문턱값**(0.5) 한국어 실사 근거. 메이드 인
코리아 한 작품뿐이고, Seoul Corpus는 문턱값에 대해선 차이가 작아 방향을 못 준다.
`MIN_SILENCE_MS`는 아래 Seoul Corpus 절에서 채워졌다.

### 검토했고 쓸 수 있다고 본 외부 데이터셋 — Seoul Corpus (2026-09-10)

| 자료 | 검토일 | 판정 | 근거 |
|---|---|---|---|
| Seoul Corpus (OpenSLR 113, CC BY-NC 2.0) — 한국어 즉흥 인터뷰 40명·27시간, Praat TextGrid | 2026-09-10 | **VAD 정답 후보로 쓴다** | 손보정 경계. 자동 지문 0 (아래) |

- `tools/seoul_corpus_to_json.py`로 `utt.ortho.` 티어를 발화 덩어리 JSON으로
  바꾼 뒤 `tools/timestamp_origin.py`로 240파일·57,499구간 전부 쟀다:
  이어붙임 **0.0%**(71699은 95%), 첫 구간 0 시작 **0/240**(71699은 30/30),
  10/20ms·프레임 격자 쏠림 없음(전부 0.25), 길이 상한 봉우리 없음, 시각값
  90%가 소수 6자리(Praat 손보정 흔적).
- 5클립(`s01m16f1` `s10f17f1` `s20f24f1` `s30f38m1` `s40f43f1`)을 ffmpeg
  `silencedetect`(-32dB/0.3s)로 대 봤다. 순수 `<SIL>`(≥0.4s) 구간은 전부 무음
  검출과 겹쳤고(93/93), 라벨 경계와 검출 경계 차이 중앙값이 발화끝 -18~-85ms·
  다음 발화 시작 +8~+130ms — 71699의 1.2~1.9초와 자리가 다르다. 부호가 일정한
  것(라벨이 dB 문턱보다 안쪽)은 어말 자음·기식이 -32dB 아래로 떨어지는 탓으로
  본다. **표본이 작다**(순수 `<SIL>` 구간이 클립당 3~37개) — 확정이 아니라
  "쓸 수 있다"까지다.
- **쓸 때 조심할 것**: `<IVER>`(인터뷰어 발화)는 소리는 있는데 라벨이 태그뿐이라
  VAD 평가에서 **제외 구간**으로 다룬다 — 변환기가 `exclude`로 내고
  `vad_sweep.py --corpus`가 읽어서 뺀다. 인점·아웃점 오차는 **옆에 0.4초 이상
  비발화가 붙은 경계**(`in_points`/`out_points`)에서만 잰다: 인터뷰어 "네"가
  침묵 없이 붙는 자리는 라벨은 끊지만 소리는 이어져 VAD가 못 끊는 것이
  정상이고, 거기까지 세면 아웃점 오차가 초 단위로 부풀어 아무것도 못 읽는다
  (2파일 실측 아웃점 중앙 1.6초). 방송이 아니라 인터뷰라 음악·효과음 아래
  말소리 경계는 못 잰다. NC 라이선스라 상업화 시 재확인(`docs/COMMERCIALIZATION.md`).
- **스윕 결과(2026-09-11, 240파일 전부, 잰 인점 27,983·아웃점 28,316, 10분 40초)**:
  결정 변수는 `침묵최소` 하나였다. 문턱 0.5·말최소 120 기준으로 침묵최소 100이면
  인점 20ms/95% 172ms·아웃 72ms/95% 365ms, 250(당시 값)이면 중앙은 같은데 95%가
  2,172/2,171ms, 400이면 7,477/8,053ms. 침묵 속 숨·입소리에 확률이 잠깐 튀고
  그 조각이 다음 말과 이어 붙는 탓이다. 구간 수는 100ms 설정에서 57,733 ≈ 정답
  57,499(과분할 아님). 문턱값은 0.3~0.7 사이 인점 중앙 20→27ms 차이뿐. 기존 세
  자료(드라마B E02·E03, 영화A)도 전부 침묵최소 100을 골랐으므로, 규칙 12의
  "최소 2편"이 이 값에 한해 채워져 **`MIN_SILENCE_MS`를 250 → 100으로 바꿨다**
  (사용자 확인). 문턱값은 그대로(장르 따라 방향이 갈린다 — `checker/vad.py`
  주석). 다시 재려면: `python tools/seoul_corpus_to_json.py <TextGrid 폴더> <flac 폴더>`
  → `python tools/vad_sweep.py --corpus <flac 폴더>`.
- **정답지로 확인(2026-09-11, 메이드 인 코리아 E02·E03, 전사 캐시 재사용)**:
  진짜 짝만 보면 인점 절대중앙 228~248ms(100ms 안 38~39%), 아웃점 가운데
  -128~-162ms로 우리가 일관되게 이르다. 값 변경 전 라운드는 흩어짐을 안 남겨
  "좋아졌다"고 말할 수 없다(규칙 13 — 자체 대조 안 함). 더 중요한 발견: 1500ms
  넘는 이상치는 전부 **분할 단위 차이**고, 경계는 `resplit._allocate`의 글자 수
  비례 위치에 놓인다 — VAD 스냅은 그중 3분의 1만 당긴다(`SNAP_TOLERANCE_MS`
  주석). faster-whisper가 이미 내는 **단어 시각을 버리고 있다**
  (`transcribe.py`, `word_timestamps=True`를 환각 검사용으로만 켠다). 인점을
  100ms 안으로 넣으려면 VAD 문턱값이 아니라 **단어 시각을 재분할 경계로 쓰는
  것**(그다음 MFA 같은 forced aligner)이 지렛대다 — 다음 갈래 후보.
  아웃점이 일관되게 이른 것은 SDH 아웃점 여유 관행일 가능성(2편 같은 방향) —
  `rules/learned/` 후보이지 코드 버그가 아니다.
- **같은 날 두 가지를 고친 뒤**(6절 — 이웃 말 꼬리 제외, 단어 시각 재분할)
  같은 캐시로 다시 대조: 진짜 짝 인점 |중앙| E02 79ms(100ms 안 55%)·E03 68ms
  (58%), 가운데 +10/+5ms. 아웃점은 그대로 가운데 -115/-113ms(100ms 안
  33~37%) — `timing.LEADS["vad"]["out"]=(3,4)`프레임이 작업자 자료의
  "보이스 끝난 후 0.2~0.3초(6~9프레임)"보다 작다. 2편이 같은 방향(약 +3프레임)
  을 가리키지만 값 변경은 사람 확인 뒤(규칙 11) — 다음 갈래 후보.
- 자료 자체는 저장소에 안 넣는다(2.5GB). 다시 받는 곳: `https://www.openslr.org/113/`
  (`label.tgz` 57MB, `sound.tgz` 2.5GB).

## 9. 테스트·커밋

```bash
python3 tests/run_tests.py    # 801건(2026-08-30 기준), GUI 제외
```

**테스트 통과가 커밋의 전제조건이다** — `tools/hooks/pre-commit`이 커밋 직전에
강제로 돌린다(새 클론에서 `git config core.hooksPath tools/hooks` 한 번
필요). `app/`(PySide6 GUI)을 건드렸으면 별도 venv로 도는 776건도 사람이 직접
확인한다 — 훅이 여기까진 안 잡는다.

## 10. 새로 합류하면 먼저 볼 것

1. `CLAUDE.md` — 작업 규칙 0~17번(18개), 전부 실제로 한 번씩 틀렸다가 정정된 것들이다.
2. 이 문서.
3. `checker/pipeline.py`의 독스트링 — 단계 순서의 코드상 근거.
4. `docs/BACKLOG.md` — 지금 막힌 것과 진행 중인 것의 상세.
