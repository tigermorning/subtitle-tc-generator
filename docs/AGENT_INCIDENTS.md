# 에이전트(Claude) 작업 오류 기록

이 문서는 코드 버그(`docs/HANDOFF.md` 6절)가 아니라 **작업 과정에서 에이전트가
반복한 실수**를 기록한다. 목적은 같은 팀(사람이든 다른 에이전트든)이 같은 자리에서
같은 실수를 또 하지 않는 것이다. 사용자가 지적한 순간·정확한 지적 내용·실제로
어떻게 고쳤는지(파일·메커니즘)를 남긴다 — "다음엔 조심하겠다"는 다짐이 아니라
**구조적으로 재발을 막은 지점**을 남기는 게 핵심이다.

새 항목을 추가할 때는 이 형식을 따른다: 무엇을 지적받았나 → 원인 → 실제 고침
(파일 경로 명시) → 재발 방지 메커니즘.

---

## 2026-08-29

### 1. 이미 고친 버그를 "안 고쳐졌다"고 다시 진단함

**무엇을 지적받았나**: 예능A 15회 SDH 초안이 정답과 구조적으로 안 맞는
문제(화자 바뀌어도 계속 이어붙임)를 처음부터 재진단했다 — whisper 재전사
2회, gap/duration 스윕 스크립트를 새로 짬. 그런데 `rules/genre/variety.yaml`에
**2026-08-27에 이미** `merge_max_ms: 2000`으로 고쳐져 있었고, 이유는 그
초안이 `--genre variety` 플래그 없이 생성됐기 때문이었다. 코드 버그가
아니라 플래그 누락이었다.

**원인**: 진단을 시작하기 전에 `docs/HANDOFF.md` 6절("그동안 찾아 고친
구조적 버그")과 `rules/genre/`를 먼저 확인하지 않았다. 근거가 이미 파일에
있었는데 안 보고 새로 판단했다.

**실제로 어떻게 고쳤나**:
- `.claude/skills/정답지-대조-진단/SKILL.md`에 "0. 시작 전에" 절 추가 —
  전사·스윕부터 손대지 않고 `docs/HANDOFF.md` 6절·`rules/genre`·
  `rules/learned`를 먼저 grep하도록 못박음
- `docs/corpus_status.yaml` 신설 — 작품/회차별 파이프라인 진행 상태를
  한 파일에 기록(아래 3번과 공유하는 인프라)

**재발 방지 메커니즘**: `CLAUDE.md` 규칙 17(아래 참고)이 이 스킬 문구를
프로젝트 전체 규칙으로 승격했다.

---

### 2. 이미 완성돼 전달한 파일을 "미완성 초안"으로 오판함

**무엇을 지적받았나**: 세션 시작 시점에 `.tmp/최종초안/예능A_15회_...초안.srt`를
"손교정 안 된 초안"이라고 답했다. 실제로는 어젯밤(2026-08-28) 세션이 3차
윤문 + 용어 고정("토크의 신")까지 끝내 `SendUserFile`로 이미 전달한
완성본(`polished.srt`)과 **바이트 단위로 동일한 파일**이었다 — `diff` 결과
차이 0.

**원인**: 파일 하나만 보고 그 파일의 이력(어느 세션이 언제 무슨 처리를
했는지)을 확인하지 않은 채 "생성기 원본 그대로"라고 단정했다. 세션 내
메모리(`project_learning_workflow.md`)의 오래된 문구만 보고, 그 뒤에 실제로
일어난 일(같은 세션 안에서 사용자가 "손교정"의 정의를 재정의하고 3차 윤문을
"손교정"으로 인정한 것)은 확인하지 않았다.

**실제로 어떻게 고쳤나**:
- `mcp__ccd_session_mgmt__search_session_transcripts`·`list_events`로 어젯밤
  세션의 실제 대화·명령을 직접 열어 확인 — 짐작이 아니라 원문 대조로 검증함
- `docs/corpus_status.yaml`의 `pipeline.text_polished`·`delivered` 필드로
  "이 작품/회차가 어느 단계까지 끝났는지"를 파일 하나에 명시적으로 기록

**재발 방지 메커니즘**: 규칙 17의 확인 목록 2번("`docs/corpus_status.yaml`에서
해당 작품/회차 — 이미 끝난 단계인가")이 이 케이스를 정확히 겨냥한다.

---

### 3. `--genre variety` 플래그를 두 세션 연속 빠뜨림

**무엇을 지적받았나**: 직접적인 사용자 지적은 아니었지만, 세션 기록을
대조하다가 발견함 — 어젯밤(2026-08-28) 세션도, 오늘 세션도 예능A
15·16회를 생성할 때 `--genre variety`를 빠뜨렸다. "다음엔 안 잊는다"는
다짐이 세션 종료 후 사라지기 때문에 같은 실수가 반복됐다.

**원인**: 플래그를 사람(에이전트)이 매번 손으로 타이핑하는 절차 자체가
실수하기 쉬운 구조였다. "기억력" 문제가 아니라 "매번 손으로 조립한다"는
프로세스의 결함이었다.

**실제로 어떻게 고쳤나**:
- `tools/gen_from_ledger.py` 신설 — `docs/corpus_status.yaml`에서
  platform·lang·kind·genre를 읽어 `--generate` 명령을 **자동 조립**한다.
  사람이 플래그를 다시 타이핑할 일이 없다.

**재발 방지 메커니즘**: 플래그 누락이라는 실수 **종류 자체**가 이 도구를
쓰는 한 구조적으로 불가능해진다 — "기억하기" 대신 "자동화"로 막음.

---

### 4. 사용자가 말하지 않은 내용을 "사용자 지시"로 기억하고 있었음

**무엇을 지적받았나**: 사용자 직접 인용(2026-08-29): "가끔 보면 내가
말하지 않은데요? 내가 지시하지 않은 내용을 네가 내가 지시했다고 기억하고
있더라고. 이런 환각 현상을 없애야 해. 내가 지시한 내용은 정확하게 기억하고
네가 지어내는 내용은 없어야 해." — `CLAUDE.md`와 메모 곳곳의 "(사용자
지시, 날짜)" 인용이 실제 발화가 아니라 과거 세션의 요약·추론이 사실처럼
굳어졌을 위험을 지적함.

**원인**: 근거의 출처(사용자 발화 자체)를 검증 없이 사실로 적어 넣는 습관 —
규칙 14의 poisoning(오염)과 같은 구조지만, 여기선 출처까지 지어내는 더
심각한 경우.

**실제로 어떻게 고쳤나**:
- 메모리 `feedback_no_fabricated_user_attribution.md` 신설(에이전트 개인
  메모리 — 이 리포와 별도로 세션 간 유지됨) — "사용자 지시"라고 적을 땐
  직접인용 위주로, 불확실하면 먼저 확인부터 받는다는 원칙을 남김
- 기존 `feedback_check_sources_before_claiming_gap.md`를 "규정 공백"뿐
  아니라 "코드 버그"·범위로 확장(1번 사고와 같은 결)

**재발 방지 메커니즘**: 완전한 감사(과거 12건의 "(사용자 지시, ...)" 인용
전부 원문 대조)는 아직 안 끝났다 — 이번 세션엔 표본(규칙 16의 한 건)만
직접 세션 기록으로 검증했고 실제 발화와 일치함을 확인했다. 나머지는 필요할
때마다 같은 방식(`search_session_transcripts`→`list_events`)으로 검증한다.

---

### 5. 정답지가 있는 영상에 "손교정·완성본 전달"을 계속함 — 애초에 필요 없는 일이었음

**무엇을 지적받았나**: 사용자 질문(2026-08-30): "정답지 있는 상황에서
손교정이라는 게 의미가 있어? 손교정이라는 것은 정답지 없는 상황에서...
억지로 학습하기 위해서 하려는 건데, 예능A 15·16회는 SRT 파일로 영어
번역도 있고 한국어 SDH도 있는데 이걸 굳이 왜 손교정을 해야 하는 거야?"

**원인**: 규칙 13이 이미 "정답지 대조는 도구(`checker/`) 검증용이지 이
영상 자체의 완성본을 만드는 게 목적이 아니다"라고 정의해 뒀는데, 실제
작업(2026-08-28 밤 + 2026-08-29 오늘)은 예능A 15·16회 초안을 3차
윤문까지 입혀 "완성본"으로 `SendUserFile`로 전달했다. **정답지가 이미
있는데** 그보다 못한 근사치를 만들어 전달한 것 — 목적(도구 검증)과 산출물
(영상별 완성 자막 전달)을 혼동했다. 다른 사고들과 달리 이건 "근거를 안
봐서"가 아니라 **작업의 목적 자체를 잘못 설정**한 경우다.

**실제로 어떻게 고쳤나**:
- `CLAUDE.md` 규칙 13에 문단 추가 — "정답지 있는 영상은 완성본을 만들어
  전달할 필요가 없다. 정답지 자체가 이미 완성본이다." 앞으로 정답지가
  이미 있는 언어·방향에는 생성·윤문·전달을 하지 않고, `--generate`+
  `--against`로 도구를 검증·수정하는 것까지만 함
- 이미 전달한 15·16회 "완성본" 파일은 되돌리지 않음(전달 자체를 취소할
  수 없고, 코드 검증에 실제로 쓰인 산출물이라 완전히 무의미하진 않았음) —
  다만 앞으로 같은 상황에서 반복하지 않음

**재발 방지 메커니즘**: `docs/corpus_status.yaml`에 `text_polished`·
`delivered` 단계가 아직 남아 있다 — 이 필드들은 **정답지가 없는 영상**
전용으로 좁혀 써야 한다(스키마 자체를 아직 안 고쳤음, 다음에 이
원장을 다시 쓸 때 이 구분을 필드로 명시할 것).

---

### 6. "할 일 없다"고 확언했는데 확인 안 한 것이었음 — 원장 스키마 자체가 불완전했음

**무엇을 지적받았나**: 5번 사고 직후 "예능A 15·16회는 이제 할 일
없다"고 말했다. 사용자가 바로 반박: "확인해 보니까 영어 번역 자막이
안 들어가 있고 한국어 SDH만 되어있던데 할 일이 없다는 건 거짓말 아니야?"
— 맞았다. `docs/corpus_status.yaml`을 만들 때 `kind: sdh` 하나만
모델링해서, 이미 정답지가 있던 영어 번역(`E15_영어_번역.srt`·
`E16_영어_번역.srt`) 자체가 원장에 없었다 — 원장을 봐도 "번역은 어떻게
됐나"를 물을 방법조차 없었다.

**같은 자리에서 사용자가 더 큰 규칙을 못박음**(직접 인용): "확인되지
않은 사실 또는 네가 잘 알지 못하는 사실에 대해서 가볍게 확언하지 마.
나는 아주 철저한 파트너를 원해." → 메모리 `feedback_no_casual_unverified_claims.md`로
저장.

**원인**: 원장(1번 사고에서 만든 도구)을 설계할 때 "이 작품에 정답지
kind가 여러 개일 수 있다"는 걸 검토 안 하고 하나만 가정했다 — 새 도구를
만들면서 **그 도구 자체에 검증되지 않은 가정을 심었다.**

**실제로 어떻게 고쳤나 — 확인하다가 실제 코드 버그 두 개를 더 찾음**:
- `docs/corpus_status.yaml`을 `schema_version: 2`로 바꿔 `episodes.<회차>.kinds.{sdh,translation}`
  구조로 재작성. `source_lang`(발화 언어)과 kind별 `lang`(결과물 언어)을
  분리 — 번역 kind는 이 둘이 다르다는 걸 처음엔 놓쳤다가 `tools/gen_from_ledger.py`를
  고치면서 발견해 바로잡음
- `tools/gen_from_ledger.py`에 `kind` 위치 인자 추가, `--translate` 자동
  포함, `--whisper-lang`엔 `source_lang`(발화 언어)을 쓰도록 수정 — 원래
  코드는 결과물 언어(`en`)를 whisper 언어로 잘못 넘길 뻔했다
- `tools/calibrate_regroup.py`를 새 스키마로 고쳐 실제로 돌려본 결과,
  번역 kind에서 `genuine_pairs()`(오늘 만든 함수)가 **한글이 없으면
  화면자막으로 오판**해서 영어 정답을 전부 걸러내는 버그를 발견 —
  `checker/evaluate.py`의 판정을 "한글도 영어 소문자도 없으면 캡션"으로
  일반화해 고침
- 고친 뒤 재실행하니 이번엔 "genuine=3"으로 나왔는데, 이것도 **가짜
  신호**였다(원시 한국어 전사와 영어 정답을 직접 비교한 거라 우연한
  코드스위칭 영어 단어 몇 개만 우연히 걸린 것) — `calibrate_regroup.py`가
  발화 언어와 정답 언어가 다른 kind는 아예 건너뛰도록 다시 고침(번역
  품질 검증은 이 가벼운 도구의 범위 밖이라고 명시)

**재발 방지 메커니즘**: 새 스키마 자체가 "kind 여러 개" 가능성을 구조로
표현하므로, 다음 작품을 등록할 때 kind 하나만 적어 넣는 실수가 나면 원장
읽는 사람(에이전트든 사람이든)이 다른 kind가 있는지부터 의심하게 된다.
근본적으로는 [[feedback_no_casual_unverified_claims]] — 도구를 "다
됐다"고 말하기 전에 실제로 다른 kind·다른 언어로 한 번 돌려보는 습관이
이 사고를 막는다.

---

### 7. 정답지 대조에 불필요한 자체 비교(ablation)를 끌어들임

**무엇을 지적받았나**: 예능A 15회 영어 번역을 오늘 코드(`--genre variety`)로
재생성한 뒤, "variety가 도움이 됐는지" 판단하려고 오늘 결과를 **2026-08-28
v12(12차 반복한 결과)와 비교**했다. 다듬은 횟수가 다른 두 결과를 대놓고
비교했으니 답이 안 나오는 게 당연했다. 사용자 지적(직접 인용): "네가 비교해야
할 것은 옛날에 내가 작업한 것과 오늘에 내가 작업한 것이 아니야. 너는 정답지를
보고 학습되는 것이기 때문에 네가 비교할 것은 네가 오늘 생성한 것과 정답지야."
그리고: "앞으로 이런 간단하고 당연한 지시 안 하고 싶어. 이런 대화는 시간
낭비야. 명확하게 어디에다 적어놔."

**원인**: 규칙 13(정답지 대조 = 도구 검증)의 핵심 절차를 다시 안 읽고 즉흥적으로
"공정 비교"라는 ML 실험 설계식 사고(A/B 테스트, 변수 통제)를 끌어왔다 — 이
프로젝트에서 정답지 대조는 원래 훨씬 단순하다: **오늘 만든 것을 정답지와
바로 대조하고, 어긋난 자리를 본다.** 그게 전부다.

**실제로 어떻게 고쳤나**:
- `CLAUDE.md` 규칙 13에 문단 추가 — "대조 대상은 항상 오늘 생성한 것 vs
  정답지다. 과거 버전·ablation 비교를 끌어들이지 않는다"를 사용자 원문 그대로
  인용해 못박음
- `.claude/skills/정답지-대조-진단/SKILL.md`의 "0. 시작 전에" 절 바로 뒤에
  같은 문구 추가 — 이 스킬을 쓸 때마다 보임

**재발 방지 메커니즘**: 사용자가 명시적으로 "다시 설명 안 해도 되게 적어놔"라고
요구한 사고다 — `CLAUDE.md`(모든 세션이 자동으로 읽음)와 스킬 파일 둘 다에
넣어서, 다음에 정답지 대조를 시작하는 순간 바로 보이게 했다.

---

### 8. `gen_from_ledger.py`가 오래 걸리는 작업 도중 사람의 손 수정을 덮어씀

**무엇이 있었나**: `tools/gen_from_ledger.py`가 시작할 때 `docs/corpus_status.yaml`을
메모리에 읽어두고, `--generate`(수 분 걸림) 끝난 뒤 **그 오래된 스냅샷을 통째로**
다시 저장했다. 그사이(생성이 도는 동안) 다른 회차·kind의 `tc_verified`·
`known_issues`를 손으로 고쳐 저장해 놨었는데, 도구가 끝나면서 그 수정이 전부
사라졌다(2026-08-30, 예능A 15회 sdh 재생성 도중 16회 tc_verified와 양쪽
translation known_issues가 지워짐 — 다음 턴에 원장을 다시 열어보고 발견함).

**원인**: 전형적인 "읽고-오래 기다리고-쓰기" 경합(stale read, lost update) —
오늘 만든 다른 도구들과 달리 이 도구만 실행 시간이 길어서(whisper 전사+번역,
수 분) 이 문제가 실제로 드러났다.

**실제로 어떻게 고쳤나**: 저장 직전에 원장을 **다시 읽어서**, 이 도구가 실제로
건드리는 자리(`kinds.<kind>.pipeline.tc_generated`)만 최신 원장 위에 얹어 저장
하도록 고침. 지워졌던 내용(16회 sdh tc_verified, 15·16회 translation
known_issues)은 기억을 더듬어 복구함.

**재발 방지 메커니즘**: 코드 수정 자체가 재발을 막는다 — 다음에 이 도구가 오래
걸려도 그사이의 손 수정을 안 지운다. 일반화하면: **원장을 자동으로 갱신하는
도구는 전부 "저장 직전 재로드" 패턴을 써야 한다.**

---

---

### 9. 멈추는 기준 없이 "한 라운드만 더"를 반복해 하루짜리 일이 며칠이 됨

**무엇을 지적받았나**: 사용자 지적(2026-08-30, 직접 인용): "지금 예능A
15 하나만 가지고 4일, 5일 이렇게 하는 게 문제인 거지. 그리고 그렇게
하면서도 발전이 없는 게 문제고. 또 네가 계속 예전에 한 거 또 해놓고, 아
예전에 했네? 이렇게 번복하고 그러는 게 문제라는 거지." 예능A 15·16회
정답지 대조가 2026-08-26부터 여러 세션에 걸쳐 계속됐고, 오늘 밤도 라운드를
찾을 때마다 "한 라운드만 더 돌릴까요"를 반복해서 물었다.

**원인**: `정답지-대조-진단` 스킬에 "언제 멈추는지"에 대한 기준이 아예
없었다 — 버그를 찾으면 다음 라운드를 돌지 말지 **매번 사용자에게 물었다.**
2026-08-30 3라운드째(반응압축 문턱값 조정)에서 후보 3개 중 1개만 진짜
버그였고 2개는 오탐이었다 — 이게 수확체감 신호였는데, 그 자리에서 안
멈추고 또 "더 할까요"를 물었다.

**실제로 어떻게 고쳤나**: `.claude/skills/정답지-대조-진단/SKILL.md`에
"6. 멈추는 기준" 절 추가 — 진짜 버그 0개·오탐 다수·연속 두 라운드 무소득·
좁은 상수 조정만 반복될 때는 **사용자에게 묻지 않고 스스로 멈춘다.**

**재발 방지 메커니즘**: 이전 사고들(1~8번)은 "근거를 안 봐서" 또는
"목적을 안 물어서"였다면, 이건 **멈춰야 할 때를 판단하는 기준 자체가
없었던 것**이다. 다음에 이 스킬을 쓸 때 6절을 보면 "언제까지 계속해야
하나"를 다시 안 헤맨다.

---

### 10. `genre_maturity` 도입 중 두 가지를 확인 안 하고 적음 — 인용 출처와 과거 버그 개수

**무엇이 있었나**: `CLAUDE.md` 규칙 13에 장르 성숙 기준(N=3)을 새로 만들면서
두 가지를 검증 없이 적었다.

1. 기존 문구 "정답지에서 배울 걸 배웠으면 넘어간다"를 "장르 전체가 성숙했다는
   걸 정의한 적 없는 빈 자기참조"라고 적었다. 실제로 grep해 보니 그 문구는
   `.claude/skills/정답지-대조-진단/SKILL.md` 6절에 정의돼 있었다 — 다만
   **영상 한 편 안에서 라운드를 몇 번 더 돌릴지**의 기준이었지, 장르 전체
   기준이 아니었다. 사용자가 "구조 버그 다 잡혔는지는 뭘로 판단해?"라고
   물어서 다시 확인하다가 스스로 발견함.
2. `docs/corpus_status.yaml`의 `genre_maturity`에 예능A 15·16회의
   `new_structural_bugs`를 각각 0·1로 적었다. 실제로는 두 회차가
   2026-08-27부터 여러 세션에 걸쳐 구조 버그 다섯 개(위 ①·9번 사고 참고 —
   다중 화자 병합 폭주·diarize `merge_cues()` 버그·`REACTION_MAX_CHARS`·
   `collapse_internal_duplicates`·`compress_reaction_runs`)를 찾아낸
   발견원이었는데, **최신 라운드 하나만 보고** 0·1로 적었다. 사용자가
   "예능A 15·16회 학습 상태 업데이트해줘"라고 요청해서 다시 열어보다가
   발견함.

**원인**: 둘 다 1번 사고와 같은 패턴이다 — **근거가 이미 파일에 있는데
(스킬 파일·`docs/HANDOFF.md` 6절) 확인하지 않고 새로 판단해서 적었다.**
1번은 규칙 17이 만들어진 지 하루 지난 뒤에도 같은 실수(이번엔 "인용의
정확한 정의 위치"·"과거 버그 총량")가 반복된 것 — 체크리스트가 있다고
저절로 지켜지진 않는다.

**실제로 어떻게 고쳤나**:
- `CLAUDE.md` 규칙 13에 정정 문구 추가 — 그 인용이 실제로 어디(스킬 6절)에
  정의돼 있고 어떤 층위(영상 한 편 vs 장르 전체)인지 명시
- `.claude/skills/정답지-대조-진단/SKILL.md` 6절 앞에 "이 절은 영상 한 편
  안에서의 기준이고, 장르 전체 기준은 7절이 따로 잰다"는 구분 문단 추가
- `docs/corpus_status.yaml`의 예능A 15·16회 `new_structural_bugs`를
  "미집계(최소 3건 이상)"로 고치고 `docs/HANDOFF.md` 6절을 근거로 남김 —
  정확한 총계를 새로 지어내지 않음(규칙 4)

**재발 방지 메커니즘**: 근본적으로는 규칙 17과 같은 종류라 새 메커니즘을
따로 만들지 않았다 — 대신 이번에 고친 두 자리(스킬 6절의 층위 구분 문단,
`genre_maturity`의 "미집계" 표기 자체)가 다음에 같은 질문을 받았을 때
바로 보이는 위치에 있다. **체크리스트가 있어도 매번 실제로 열어보지
않으면 소용없다**는 것이 1번과 이 사고의 공통 교훈이다.

---

## 2026-09-08

### 11. 이미 끝난 대조를 "확인 대기"로 다시 올림 — 결론이 그 상수 바로 옆에 있었다

`작업 기본 원칙.docx`의 미확인 30장을 정독하다 WORK-007(장면전환 SE 프로필,
fps별 최대 오프셋·안전 영역 표)을 보고 **"우리 코드에 대응하는 값이 없다 —
사람 확인 필요"**로 `docs/HANDOFF.md` 7절에 올렸다.

**이미 2026-08-31에 끝난 대조였다.** 결론은 두 곳에 있었다:

- `checker/timing.py`의 `SHOT_CLEARANCE_MS`·`SHOT_OUT_LEAD_FRAMES` **바로 위 주석** —
  SE 공식 소스(`BeautifyTimeCodesSettings.cs`의 `Preset.Netflix`)와 대조해 red/green
  zone은 SE만의 2단계 UI 개념이라 "여기 상수로 옮기지 않았다"고 명시.
- `rules/private/sources/작업자-자료/이미지-정독.md`의 "장면전환 — SE '프로필 편집' 프레임
  표" 절 — fps별 표가 SE 기능이 아니라 작성자의 손계산(`round(fps*0.5)`·
  `floor(fps*0.25)`)임까지 검증해 둠.

같은 이유로 정독 기록에 **같은 표를 두 번** 적었다(기존 절을 안 찾고 새 절을 씀).

사용자가 "반영해 달라"고 했을 때에야 그 주석을 읽었고, 반영해도 얻는 게 없다는
것까지 그 자리에서 확인됐다 — red zone 7프레임 ≈ 292ms(24fps)라 우리 500ms 안에
이미 들어가고, fps별 환산값도 500ms와 같다.

**왜 났나**: 규칙 17의 세 곳(HANDOFF 6절·corpus_status·메모리)은 봤지만, **고치려는
코드 자체의 주석**은 안 봤다. 정독처럼 "새 자료를 읽는" 작업이라 코드를 볼 일이
없다고 여긴 것이 함정이었다 — 새 자료의 결론을 적는 자리가 곧 코드였다.

**막는 장치**: 이번엔 글로만 남긴다. `tools/docs_check.py`가 문서의 경로·grep 주장·
숫자를 지키지만 "이 주제가 이미 코드 주석에 결론이 있는가"는 기계가 못 센다.
다만 값이 든 자료를 정독하면 **그 값이 들어갈 코드 파일을 먼저 grep**하는 것이
이 사고를 막는 최소 동작이다(`grep -rn "SHOT_CLEARANCE" checker/`면 3초다).

## 2026-09-10

### 12. 확인은 두 번 받았는데, 그 저장소가 무엇에 쓰이는지는 안 물었다

발주처 규정을 저장소에서 빼는 작업 중, 병합된 PR이 `refs/pull/N/head`로 옛 커밋을
붙들고 있어 force-push로는 규정 파일이 안 지워진다는 것을 실측으로 확인했다. 그래서
"저장소 삭제 후 재생성"을 제안했고 **사용자 확인을 두 번 받아** 실행했다. 실행 직후
사용자가 말했다 — **아직 평가받지 않은 과제 제출물이 그 저장소**라 지우면 안 된다고.

절차는 지켰다. 백업도 미리 떴다(미러 + PR 기록 JSON). 커밋 347개·날짜·작성자는
그대로 살아 있다. 그런데도 **병합된 PR 16개와 저장소 생성일은 못 돌린다.**

**빠뜨린 질문은 "지울까요?"가 아니라 "이 저장소가 지금 무엇에 쓰이고 있나요?"였다.**
되돌릴 수 없는 외부 자원 삭제는 동의만으로 안전해지지 않는다 — 그 자원이 **저장소
밖에서 맡고 있는 역할**(제출물, 배포 대상, 남이 보고 있는 링크)은 저장소 안을 아무리
들여다봐도 안 보이고, 사용자도 묻지 않으면 떠올리지 못한다. 실제로 사용자는 선택지를
두 번 고르는 동안 과제 이야기를 하지 않았다.

**다음부터**: 저장소·브랜치·릴리스처럼 밖에서 참조되는 것을 지우기 전에, 동의를 받는
질문과 **별도로** 용도를 묻는다. "삭제해도 되나요"와 "이걸 지금 누가·무엇에 쓰고
있나요"는 다른 질문이다.

## 공통점

1~4·6·10번은 원인이 같다: **근거가 이미 어딘가에 있는데(코드·이전 세션·
사용자 발화·다른 kind·스킬 파일의 실제 정의 위치) 확인하지 않고 새로
판단했다.** 그래서 `CLAUDE.md` 규칙 17로 "손대기 전에 셋을 먼저 본다"
(`docs/HANDOFF.md` 6절 · `docs/corpus_status.yaml` · 프로젝트 메모리)는
체크리스트를 만들었고, 6번을 계기로 "가볍게 확언하지 않는다"는 더 포괄적인
규칙이 하나 더 생겼다. **10번은 그 체크리스트가 생긴 뒤에도 같은 패턴이
반복될 수 있음을 보여준다** — 규칙이 있다고 저절로 지켜지는 게 아니라
매번 실제로 열어봐야 한다.

5번은 다른 결이다 — 근거를 안 본 게 아니라 **애초에 이 작업이 왜
필요한지(목적)를 정의한 규칙(13)을 매번 다시 안 물었다.** "정답지가
있으니 대조해서 도구를 고치자"까지는 맞았는데, 거기서 "그러니 이 영상의
완성본도 만들자"로 슬쩍 넘어간 게 문제였다.

8번은 또 다른 결이다 — 판단력이 아니라 **동시성 버그**다. 새로 만든 도구를
"한 번 돌려보고 끝"이 아니라 "오래 걸리는 동안 다른 걸 건드리면 어떻게 되나"
까지 검증했어야 했다.

9번은 다섯 번째 결이다 — **멈춰야 할 때를 스스로 판단하는 기준이 아예
없었다.** 개별 사고 하나하나는 다 고쳐지고 있었는데, "총합이 너무 오래
걸린다"는 더 큰 그림은 아무도 안 보고 있었다.

11번은 1·2·4·6·10과 같은 결이되 **본 곳이 어긋난** 경우다 — 규칙 17이 지정한 세
곳은 봤는데 정작 결론이 있던 자리(고치려는 코드의 주석)를 안 봤다. 자료를 읽는
작업이라 코드는 볼 일이 없다고 여긴 것이 함정이었다.

이 문서는 다섯 종류의 실수를 구분해서 남긴다 — 근거 확인 누락(1·2·4·6·10·11),
목적 재확인 누락(5), 플래그 자동화가 필요한 반복 조작 실수(3), 새 도구의
동시성 결함(8), 멈추는 기준 부재(9).
