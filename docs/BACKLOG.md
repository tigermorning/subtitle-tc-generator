# 백로그 — 다음 작업자(사람이든 다른 AI든)를 위한 인수 문서

이 파일은 **작업을 이어받기 위해** 있다. `CLAUDE.md`가 "이미 틀렸다가 바로잡은 규칙"을
담고, 이 파일은 **아직 안 한 것과 왜 그 순서인지**를 담는다.

마지막 갱신: **2026-08-26** · 시험 755건 통과(시스템 파이썬) · 작업 트리에 커밋 대기 중인
변경 있음(`checker/timing.py`, `rules/SCHEMA.md`, `CLAUDE.md`, 문서·스킬 신설 — 아래
§0-C)

## 0-Z. 2026-08-14 마감 — 이날 끝낸 것과 남긴 것

**끝낸 것** (자세한 것은 커밋 메시지에)

- 폴더 이름을 저장소 이름에 맞췄다(`subtitle-editor` → `subtitle-tc-generator`).
  데스크톱 바로가기·문서 경로까지. 옛 이름은 SE 플러그인과 교정기가 대체 경로로 계속 본다
- **pre-commit 훅**(`tools/hooks/pre-commit`) — 시험이 실패하면 커밋이 안 만들어진다.
  규칙 9가 글에서 기계로 옮겨졌다. 새 클론에서 `git config core.hooksPath tools/hooks`
- **README 전면 재작성.** 08-12 15:03에서 멈춰 있었다(GUI·생성·번역·`.work/`·장르·
  캐릭터가 통째로 빠져 있었고, 방향도 "SE보다 나은 편집기"로 낡아 있었다)
- 편집기 시절 문서 셋에 현재 방향을 붙였다(브리프·SE 코드 분석·상용화 목록)
- **디즈니·쿠팡 번역 아이콘 넷**을 만들었다. 프로파일은 08-11부터 있었는데 쓸 수단이 없었다
- **작업자 자료 정독** — 이미지 127장과 본문 2,076행 전부. 값을 옮긴 문서:
  `rules/sources/작업자-자료/이미지-정독.md`
- 그 결과 반영: 겹침 처리 기본값(디즈니 `dialogue_only`·쿠팡 `keep_both`),
  말줄임표 전략 검사(DC35·CC35·S35), 번역 프롬프트의 지양 목록(2차·3차)
- **교정기와의 계약을 기계로 고정했다.** `corrector_info()`가 교정기의
  `tools/check_public_api.py`를 실행하고, 계약이 깨지면 이쪽 시험이 실패한다 → 훅이 막는다

**남긴 것은 아래 §0-A와 §3·§4에 그대로 있다.** 가장 위는 여전히 멈춤(AppHangB1) 확인이다.

### Notion 카드에 방향 전환 반영 — 갱신본은 만들었다, 원본은 손으로 붙인다

사용자 요청(2026-08-14): Notion의 **도메인 문제·전략 카드**를 지금 방향에 맞게 고치는 것.

**원본 페이지는 쓸 수 없다.** 그 워크스페이스(`액티브러닝캠프 - 데이터분석`)에 사용자가
**게스트**라 Notion MCP가 연결을 거부한다("게스트 자격으로는 연결할 수 없습니다"). 커넥터는
개인 워크스페이스에만 붙는다. 원본을 내보내(Markdown & CSV) 칸 구조와 카드 여섯 장을 읽었다.

    원본    <비공개 노션 링크>   (읽기만, 게스트)
    갱신본  <비공개 노션 링크>   (개인 워크스페이스)

**갱신본은 칸별로 복사해 붙일 수 있는 형태로 써 뒀다.** 남은 일은 사람이 원본 카드에
붙여넣는 것뿐이다.

**주의 하나** — 원본 카드 여섯 장은 **본문이 전부 템플릿 그대로다**("태깅 프롬프트 3버전
작성 → 녹취 2건 적용"). 자막과 아무 상관 없다. 속성만 고치지 말고 실행 계획·시도 기록도
갈아야 한다.

**카드를 프로젝트 셋으로 재편했다.**

| 기존 카드 | 접히는 곳 |
|---|---|
| 한국어 맞춤법·띄어쓰기 교정 · 기존 맞춤법 검사기의 오류 개선 | 한국어 교정기 |
| 영상 TC 자동화 · SDH 자동 인식 · "자막 편집 + 한국어 교정" 툴 설계 | 자막 및 타임코드 생성기 |
| 전문용어·배경지식 DB 구축 | **n차 영-한 번역과 말맛 윤문** (원래 카드에 없던 것) |

세 번째 카드에는 2026-08-14에 정한 구조 결정이 함께 들어갔다 →
[`TRANSLATION_ARCHITECTURE.md`](TRANSLATION_ARCHITECTURE.md)

한 줄 요약: **"SE를 흉내 내는 편집기" → "자막·타임라인 생성기"**

| 갈래 | 옛것 | 지금 |
|---|---|---|
| 문제 | SE의 편집 기능이 부족하다 | **초벌 만드는 시간이 가장 비싸다** — TC는 분량의 1.5배, 번역은 영상 10분당 1시간, 프로가 40분 물에 이틀 |
| 문제 | 규정을 사람이 외워서 적용한다 | 그대로 유효. 다만 **자동 교정 오답 0**이 recall보다 먼저다 |
| 문제 | — | **한국어 규범 판정은 자막 도구의 일이 아니다**(교정기를 갈라낸 이유) |
| 전략 | SE를 대체하는 편집기를 만든다 | **SE 앞단을 채우고 세부는 SE로 넘긴다** — 전사·대조·번역·재분할·스포팅까지가 우리 몫 |
| 전략 | 편집 기능을 늘린다 | **SE 흉내는 만들지 않는다**(2026-08-12 결정). 대신 SE 안에서 우리 검사를 부르는 플러그인을 둔다 |
| 전략 | — | **규정은 데이터로 둔다** — 프로파일 상속(플랫폼 → 발주처), 코드에 박지 않는다 |
| 전략 | — | **미공개 자료는 로컬에서만** — 전사·번역 전부 이 컴퓨터 안에서 |
| 범위 밖 | — | 편집기 UI 흉내 · 포맷 지원 확대(SE가 408종) · 읽기 속도·줄당 글자 수를 교정기에 두는 것(생성기로 옮겼다) · **띄어쓰기 교정에 AI 얹기**(MQ2 실측으로 기각) |
| 지표 | — | 자동 교정 **오답 0** > 플래그 감소 > 선택적 recall. recall 100%는 사전 커버리지가 상한이라 도달 목표로 세우지 않는다 |

---

## 0-A. 지금 막혀 있는 것 — **여기부터**

### 실사용 멈춤 (AppHangB1) — 고쳤다고 보지만 확인 못 했다

**증상.** 영상에서 자막을 만들면 생성은 끝까지 가는데(자막 208개까지 나온다) **그 뒤
UI가 멈춘다.** Windows 이벤트 로그가 `AppHangB1`을 남긴다 — 크래시가 아니라 UI 실이
이벤트를 못 돌린 것이고, 파이썬 예외 기록은 없다. 두 번 재현했다(22:26, 22:36).

**밝혀낸 것.**

- 생성 자체는 문제가 없다. `.work/01-generate.srt`에 자막 208개가 남는다.
- Qt 쪽은 빠르다. 같은 규모(자막 210개·지적 118건)를 mpv 없이 재현해 재 봤다:
  `model.replace` 8.9ms · 파형 10.0ms · 지적표 9.0ms · `_refresh_stages` 1.0ms.
- 남는 것은 **UI 실에서 도는 동기 mpv 왕복**이다. `python-mpv`의 `command()`는 답을
  기다린다.
- ONNX(VAD)는 이미 단일 스레드다(`inter_op=1, intra_op=1`) — CPU 포화 가설은 죽었다.

**한 조치 둘.**

1. `fit_subtitle_scale()`을 100ms 시계에서 빼고 `resizeEvent`로 옮겼다. mpv 왕복을
   초당 20번 UI 실에서 기다리던 것이다(그 함수 독스트링이 "창 크기가 바뀔 때마다"라고
   적고 있었다). 멈춰 있으면 시계를 500ms로 늦춘다. → 커밋 `25ce0d3`
2. `set_subtitles`/`reload_subtitles`를 `command_async`로 바꿨다. 생성 직후 자막 208개를
   mpv에 **처음 얹는** 자리가 `sub-add`이고 그것이 가장 유력하다. → **아직 커밋 전이면
   `app/player.py`를 확인**

**다음 사람이 할 일.**

- exe를 재빌드하고 같은 영상으로 다시 돌린다. **그것이 유일한 시험이다.**
- 여전히 멈추면 다음 후보는 `_sync_position`의 `position_ms`(속성 읽기, 100/500ms마다)와
  `player.seek`(파형 클릭). 같은 방식으로 비동기화하거나, 재생기 조작을 전부 한 곳으로
  모아 UI 실에서 mpv를 만지지 않게 한다.
- **막힌 지점을 증명하지는 못했다.** 스택을 뜨려면 procdump 같은 도구가 필요하다.

**재빌드가 막힐 수 있다.** 멈춘 `자막생성기.exe`가 `dist` 파일을 잠근다. 작업 관리자로
끝낸 뒤 빌드한다(자막은 `.work/`에 있어 잃는 것이 없다).

```
cmd.exe /c "..\korean-subtitle-corrector\.venv\Scripts\python.exe -m PyInstaller \
  --noconfirm --distpath dist --workpath .tmp\build 자막생성기.spec"
```

`tools/build-exe.bat`은 끝에 `pause`가 있어 자동 실행에서 멈춘다. 위처럼 직접 부른다.

### 실사용 자료가 여기 있다 (평가·재현용)

```
/mnt/c/Users/user/Desktop/바른번역 글밥아카데미/영어영상 입회 테스트 [2026.04.05]/
    Sub_en-ko.work/01-generate.srt    자막 208개 — 대본 123 / 전사 62 / 소리 없음 24
    Sub_en-ko.work/01-source.json     번호별 원문
    Sub_en-ko.work/manifest.json      단계 기록
```

**T4(다큐 합니다체 임계값)를 재려면 완성본 자막이 필요하다.** 이 초벌과 사용자의
완성본을 견주면 임계값도, 오역 검증의 헛플래그율도 실측할 수 있다.

---

## 0-C. 2026-08-25·26 — 학습 계층 실가동 + 정답지 대조 진단 첫 실행

**§0-B의 4번(관측값을 별도 층에 둔다)이 실제로 됐다.** `rules/learned/` 아래
10개 발주처 버킷(netflix·disney·coupang·amazon·appletv·hbo·viki·viu·bluray·
unknown)에 학습값이 쌓였다. `.claude/skills/정답지-학습/`이 이 절차를 코드화한다.
**§0-B의 1·2번(`--all-langs` 쌍 만들기, 폴더 배치 진입점)은 여전히 안 됐다** —
`tools/corpus_build.py:270`의 `--all-langs`는 지금도 "쌍은 만들지 않는다"고
docstring에 그대로 적혀 있고, `main()`엔 폴더 인자가 없다. 착각하지 말 것(이전에
"거의 다 됐다"고 잘못 판단한 적 있어 직접 코드를 읽어 확인함, 2026-08-26).

**정답지 대조 진단(`--generate` + `--against`)을 처음 실전에서 돌렸다** — 예능A
시즌2 15·16회로 시작해 `checker/timing.py`의 실제 버그를 찾았다: `suggest_spotting()`이
말소리 구간 전체 끝까지 아웃점을 늘려서, 여러 사람이 끊김 없이 겹쳐 말하는 예능·
토크쇼에서 근처 배경 잡담·박수까지 아웃점이 끌려갔다(자막이 정확히 7000ms·4000ms
플랫폼 상한에 걸려서야 멈춘 사례로 확인). **`max_shift_ms`(기본 2000ms) 상한을
추가해 고쳤다 — 현재 커밋 대기 중, 테스트 755건 통과 확인함.**

이어서 영화D(2026, 영어 실사 영화)·영화F(2026, 영어 애니메이션)
두 편을 더 돌려 같은 방식으로 검증했다. **더 큰 원인이 남아 있다**: 노래·박수·
비언어 발성(`♪ lyrics ♪`, `(growling)`, `(speaking Minionese)` 류)을 whisper가
대사처럼 문자 그대로 전사해, 아웃점이 실제 대사 끝이 아니라 그 소리가 끝나는
자리까지 늘어난다. 세 작품·두 언어·세 장르에서 공통 확인됐다(한국어 예능, 영어
실사, 영어 애니). `max_shift_ms` 상한은 가장 심한 과확장만 막을 뿐 근본 원인은
아니다.

**다음 작업자에게 남기는 것**: "이 오디오가 대사인지 아닌지"를 판별하는 기능이
없다. 정답지의 대괄호 표기(`[음악]`·`(growling)` 등)와 우리 초안을 시간대별로
대조하면 whisper가 어디서 착각했는지 좌표를 모을 수 있다 — 다만 표본이 아직
세 작품뿐이라 규칙 12(몇 편 몇 개 기준)에 따르면 패턴 확정에는 이르다. 오디오
특징(피치 안정성 등)으로 "대사 아닐 확률" 신호를 만들 수는 있어도, `[제작진의
웃음]`처럼 화면을 봐야 아는 구체적 내용은 오디오만으로 못 만든다 — 이 한계를
잊지 말 것.

`rules/SCHEMA.md`도 이 기간에 갱신됨: `collision.policy`는 08-14 정독(WORK-030
이미지)에서 이미 값이 나왔다는 걸 재확인해 명시(미확보 아님), `forced_narrative.marker`는
자료 자체가 "업체·작업마다 다르다"고 명시한 걸 확인해 **확인된 부재**로 문서화함
(추측 안 함).

### 2026-08-27 — 침묵 환각 필터 + 번역 방향 매개변수화 + 손 교정 1회차

**침묵에서 whisper가 지어내는 반복 환각을 고쳤다** — `checker/generate.py`가
말소리 검출(`find_speech`)을 전사보다 먼저 하도록 순서를 바꾸고, 말소리와 전혀
안 겹치는 전사 조각을 뺀다. 영화D·영화F(2026) 두 영화의
오프닝 로고 구간에서 밀리초까지 같은 타임스탬프에 반복 헛소리가 나온 것으로
발견·검증함(커밋 `089e053`).

**`checker/translate.py`가 목표 언어를 매개변수로 받게 됨** — 예전엔 무조건
한국어로만 번역했다. 이제 `-l` 프로파일 언어(`ko`/`en`)를 그대로 받아 그 언어로
번역한다. 지금은 **1차(오역만 보는 단계)만** 됨 — 2차·3차(`checker/revise.py`,
감수·윤문)는 한국어 문법 전용 로직(존댓말/반말, 한국어 어순, 한국어 최상급
비문 등)이라 그대로 건드리지 않았다. 아래 손 교정에서 정확히 왜 2차·3차가
필요한지 증거가 나왔다.

**예능A 15회, 한국어→영어 번역 초안을 정답(`E15_영어_번역.srt`)과
손으로 대조함(정답지 1076개 중 약 180개 표본 — 앞부분 20개, 700~790번대 90개
가량. 전체 대조는 아직 안 됐다, 규칙 12 — 안 본 범위를 남긴다).** 발견한 것:

1. **화면 텍스트(온스크린 캡션)를 통째로 못 만든다.** 정답엔 예능 자막 관행대로
   대문자 캡션(`PRESENTED BY SCREWBALLS`, `ROUND 3 VIEWERS CHOOSE THE GOD OF
   TALK` 등)이 곳곳에 있는데, whisper는 소리만 듣지 화면 글자를 못 읽는다.
   노래·효과음 오인(위 §0-C 첫 항목)과는 **다른, 세 번째 갈래의 한계**다 — OCR 없이는
   못 고친다. 이전 `--against` 대조에서 나온 "빠뜨림" 개수 상당수가 이 원인일
   가능성이 큼(재검산 안 함, 다음에 확인할 것).
2. **1차가 수사적 표현을 과하게 직역해 쪼갠다.** 예: 정답 "Is there anybody
   who's blind out there?"(하나의 수사 의문문) vs 초안 "Anyone blind? Where are
   you?"(두 조각으로 쪼개져 어조가 사라짐). **1차 설계 자체가 "오역만 본다,
   다듬기는 2차·3차"이므로 이건 버그가 아니라 2차·3차가 없어서 생기는 빈자리다.**
3. **반복되는 구어체 필러를 그대로 옮긴다.** 정답은 "-Something painful?
   -Yes, painful."로 깔끔한데 초안은 "a bit of pain, I have pain… pain, pain."
   처럼 반복을 그대로 번역함 — 사람 편집자는 자연스러운 반복을 정리하지만
   1차는 안 한다(역시 2차·3차의 몫).
4. **문맥 없이 튀어나온 문장 하나 발견**("Alright, good, good" — 정답 쪽에
   대응하는 말이 없음). `CLAUDE.md` 규칙 14(맥락 부패 우선 의심) 적용: 이
   번역기는 배치마다 직전 3개 번역을 맥락으로 같이 보내는 구조(`translate.py`
   `context=3`)라 poisoning(앞선 배치의 오류가 다음 배치에 사실처럼 전달)이
   구조적으로 가능하다 — **의심만 하고 확정하지 못함**, 더 볼 필요 있음.
5. **인명 로마자 표기가 흔들림**("Cho" 정답 vs "Jo" 초안) — 코드 버그가 아니라
   워크플로 누락이다. `--terms`로 KNP 시트를 먼저 만들고 `--glossary`로 먹여야
   하는데 이번 진단 실행에는 안 줬다. 실제 작업에서는 번역 전에 KNP부터 만드는
   것이 순서다.

**다음 작업자에게 남기는 것**: (1) 예능A E15 나머지 구간(180개 넘는 부분)
손 교정 이어서 하기, (2) 위 2번·3번 패턴이 English 2차·3차(`revise.py`의 영어판
— 아직 없음, 영어 문체 규정 조사부터 필요, 규칙 9)로 얼마나 해결되는지 확인,
(3) 4번(맥락 오염 의심)을 더 작은 사례로 재현해 진짜인지 가르기, (4) 화면 캡션
누락이 이전 `--against` 통계에서 차지하는 비중 재확인.

**정답지 개수 정정**: 앞서(§0-C) "예능A E15 정답 1076개"라고 적었던 건
**한국어 SDH 쪽 개수다.** 영어 번역 정답(`E15_영어_번역.srt`)은 실제로
**1453개**(크레딧 포함, 끝까지 셈)다 — 같은 회차라도 자막 종류마다 나뉘는
방식이 달라 개수가 다르다. 앞으로 이 둘을 같은 숫자로 혼동하지 않는다.

**심각한 버그를 하나 냈다가 같은 날 잡음 — 기록으로 남긴다.** 위 침묵 환각
필터(`checker/generate.py`, 커밋 `089e053`)를 예능A 15회로 검증하다가,
초안이 00:51:50에서 끊기고 정답은 00:57:58까지 있는 것을 발견했다. 원인:
**VAD의 오디오 읽기 자체가 3113초에서 멎었다**(영상은 3479초) — whisper 전사는
3478초까지 멀쩡했는데, 필터가 "말소리와 안 겹치면 지어낸 것"이라고 판단해서
VAD가 못 미친 마지막 6분(진짜 대사)을 통째로 지워 버렸다. 막으려던 문제(침묵
환각, 몇 초)보다 이 회귀(6분 삭제)가 훨씬 컸다. `checker/generate.py`
(커밋 `1b85af6`)에서 VAD 커버 구간과 영상 길이를 비교해, 30초 넘게 벌어지면
그 뒤는 걸러내지 않도록 고쳤다. **교훈**: "검출기가 못 찾았다"와 "검출기가
멎었다"는 다르다 — 필터·자동 삭제 로직을 만들 때 검출기 자체의 커버리지를
항상 같이 확인해야 한다.

**예능A 15회 영상 자체도 손상돼 있다.** VAD 실패를 진단하다가
`ffmpeg -i ... -map 0:a:0 -f null -`로 오디오 전체 디코드를 해 보니, 파일
전체에 걸쳐 "invalid EBML number" 오류가 수십 곳 나왔다 — E16·E19·Money
Heist와 같은 손상 패턴이다. 그런데 `tools/corpus_build.py`의 자막 트랙 추출
(`-c:s copy`, 오디오·비디오 디코드 없음)은 지금까지 문제없이 통과했다.
**이미 커밋된 학습값(`rules/learned/netflix/ko-sdh.yaml`,
`en-translation.yaml`의 예능A 15·16회분)이 정말 온전한지 재확인이
필요하다** — 자막 트랙 추출이 손상을 안 만났을 뿐인지, 아니면 진짜 온전한
것인지 아직 모른다. 다음 작업자가 자막 트랙만 따로 전체 디코드 검증
(`-map 0:s:N -f null -` 같은 방식이 되는지 확인)까지 해서 가려야 한다.

**→ 재확인 완료(15회만, 2026-08-27).** 재추출해서 기존 커밋 파일과 바이트
단위로 완전히 동일함을 확인(추출 자체는 안정적). 그런데 영어 번역 트랙과
대조하니 한국어 SDH의 4초 넘는 빈 구간 135개 중 106개에서 영어엔 내용이
있고(그중 100개는 화면 캡션이 아니라 실제 대사) — **이 회차 한국어 SDH에
실제 대사 캡션이 최소 100곳 넘는 구간에 걸쳐 빠져 있다.** 손상이 "한 지점
이후 전부 망가짐"이 아니라 46개 손상 지점마다 ffmpeg가 다시 튕겨 나와
(resync) 영상 끝까지는 커버하는 구조라, 빠진 자리는 손상 지점 근처에만
흩어져 있고 나머지는 정상으로 본다.

값은 고치지 않았다(`rules/learned/netflix/ko-sdh.yaml`에 근거만 적었다) —
빠진 자리는 통계에 애초에 안 들렸을 뿐, 있는 자막은 여전히 정답 트랙에서
그대로 옮긴 확실한 값이다. 손상 원인이 대사 길이·속도와 상관없는 파일 바이트
오류라 표본이 한쪽으로 쏠렸을 가능성도 낮게 본다. **다만 이건 발주처 대응용
근거가 아니라 생성기 보정(스포팅 LEADS 값 등)에 쓰는 학습 자료의 신뢰도
문제로 다룬다**(사용자 정정, 2026-08-27 — 이 프로젝트의 정답 대조는 순수
학습·도구 검증 목적이다).

**→ 16회도 재확인 완료(2026-08-27).** 같은 방법: 재추출은 기존 커밋과
바이트 단위로 동일, 손상 지점 103곳(15회 46곳보다 많음). 영어 번역과 대조해
4초 넘는 빈 구간 138개 중 71개(67개가 실제 대사)에서 한국어 SDH만 빔 —
652큐 중 10.3%, 15회(9.3%)와 비슷한 비율. 같은 결론: 값 안 고침, 있는
자막만 확실한 값으로 유지. **예능A 15·16회 모두 재확인 끝났다** — 이
항목은 닫는다.

---

## 0-B. 정답 자막으로 생성기를 학습시킨다 — **다음에 이어서**

**사용자 요청(2026-08-14).** 상용 릴리스에 들어 있는 공식 자막을 정답 자료로 모아
생성기를 고친다. **추출과 분석에서 멈추지 않는다 — 되는 곳은 실제로 학습시킨다.**
영화B로 시험해 통했고, 통하면 **훨씬 큰 자료를 주겠다**고 했다.

사용자가 못 박은 방침:

- **타임코드를 정답으로 둔다.**
- **뽑을 수 있는 자막 트랙을 전부 쓴다.** 한둘만 고르지 않는다. 영상마다 언어 수가 다르다.
- 한국어·다국어 번역·SDH 모두 **공식 검수를 거친 사람 자막**이므로 그대로 쓴다.
- **글자에 오류가 있어도 자료를 버리지 않는다.** 찾으면 표시만 한다.

### 확인된 것 (영화B, 2026-08-14)

`영화B` 폴더의 블루레이 두 판과 웹립 한 판으로 확인했다.

- **OCR이 필요 없다.** 블루레이 mkv에 텍스트 자막 트랙이 37개 들어 있다(kor 포함).
  뽑은 것이 옆에 있던 `.srt`와 **타임코드까지 완전히 일치**했다 — 무손실이다.
- **원어-한국어 쌍 1107개.** 그중 **343개(31%)가 합쳐진 묶음**이다. 영어 자막 여럿이
  한국어 하나로 합쳐진다.
- **한국어에만 있는 자막 80개는 전부 화면자막이다**(`'행운을 빌어요!'`,
  `'지구까지 113.8년'`). 영어 트랙은 화면 글자를 옮기지 않는다.
- 사람 자막 실측(한국어 1692개, 언어 가중치 적용): 노출 중앙값 **1752ms**,
  줄당 **7.5자**(95% 10.5), **4.9자/초**, 한 줄 944 / 두 줄 748 / **세 줄 0**.
- 파일럿 대조(10분 클립, `ggml-base`): 인점 **+704ms 늦음 · 흩어짐 ±819ms**,
  표시 시간 **2750ms**(사람 1752), 줄당 **41자**(사람 7.5), 자막 149개(정답 202).
  → **우리는 너무 길게 띄우고 한 자막에 너무 많이 담는다.**

### 지금 있는 것 — **지우지 마라**

    tools/corpus_build.py       f8f82bb에서 넣고 99daf2c에서 고쳤다
    .tmp/corpus/phm_bluray/     pairs.jsonl · stats.json · flags.jsonl   1.9MB
    .tmp/gt/                    파일럿 자료 한 벌                        262MB

`.tmp/`는 git이 무시하고 **빌드 작업 폴더이기도 하다**(`--workpath .tmp\build`).
청소하다 같이 날아가기 쉬우므로 **`.tmp/gt`와 `.tmp/corpus`는 남긴다**(사용자 지시,
2026-08-14 — 다음 세션에서 large-v3-turbo로 같은 클립을 다시 재기 위해서다).

날아갔으면 되살린다. **키프레임 시각을 그대로 써야 한다** — 임의 지점에서 자르면
`-c copy`가 키프레임으로 스냅해 정답과 수백 ms 어긋난 채 시작하고, 그러면 재려는 값과
오차가 같은 크기가 된다.

    영화: ..\영화B\영화B_릴리스\
          영화B_릴리스.mkv
    트랙: 0:2 영어 · 0:3 영어 SDH · 0:23 한국어
    클립: -ss 4198.778 -t 600 -c copy      (70~80분. 대사가 가장 빽빽한 10분)
    정답: 뽑은 자막을 4198.778초만큼 당기고 0~600초에 완전히 들어가는 큐만 남긴다
          -> clip_en 202큐 · clip_en_sdh 226큐 · clip_ko 191큐

이 폴더의 `out_en.srt`와 `eval_en.json`은 **`ggml-base`로 낸 첫 회차**다. 다음 회차와
견주려고 남긴 것이므로 덮어쓰지 말고 새 이름으로 낸다.

### 다음에 할 일 — 이 순서로

1. **`--all-langs`가 쌍까지 만들게 한다.** 지금은 뽑기만 하고 쌍은 원어-목표 하나만
   만든다. 사용자 방침은 **트랙 전부**다. 짓다가 되돌렸으니 처음부터 하면 된다.
   (원어를 축으로 나머지 언어 전부와 묶고, 같은 언어끼리는 번역 쌍이 아니므로 뺀다)
2. **폴더 통째로 도는 배치 진입점.** 큰 자료가 오면 그것부터 필요하다.
3. **large-v3-turbo를 받아 파일럿을 다시 잰다.** 지금 흩어짐 ±819ms가 **모델 탓인지
   방법 탓인지 가르지 못한다.** 두 회차의 차이가 모델 몫이고 남는 것이 방법 몫이다.
4. ~~**관측값을 별도 층에 둔다.**~~ **됐다 — §0-C 참고(2026-08-25·26).**
   ~~프로파일 값을 실측으로 교체한다~~ — 이렇게 적었던
   것을 사용자 지시로 고쳤다(2026-08-14). **학습이 `rules/`를 건드리지 않는다**
   (`CLAUDE.md` 규칙 11). 관측값은 `origin: learned`와 출처 편수를 달아 따로 두고,
   **규정이 비워 둔 자리에만** 들어간다. 규정이 값을 정한 항목에서는 아예 보지 않는다.

   그 빈자리가 어디인지는 파일럿이 이미 가리켰다 — 넷플릭스 한국어는 노출을
   833~7000ms로만 정한다. 그 사이에서 우리는 2750ms를, 사람은 1752ms를 골랐다.
   규정 위반이 아니라 **규정이 말하지 않은 자리**다.

   화면자막 표식과 겹침 처리도 자료에서 읽히지만(이 릴리스는 작은따옴표 92개,
   `{\an8}` 43개) 그것은 **프로파일을 고칠 근거가 아니라 이 릴리스의 관측**이다.
   관측값은 `rules/learned/`에 두고, 규정이 비워 둔 자리만 채운다.

   **규정으로 굳는 길은 열려 있다.** 다만 기계가 옮기지 않는다 — 새 규정으로 보이는
   것을 발견하면 **사람에게 보고하고 확인을 받는다**(규칙 11). 보고에는 관측값,
   몇 편 몇 개에서 나왔는지, **어긋나는 사례까지** 함께 낸다. 한 작품에서만 나온 것은
   규정이 아니라 그 작품의 습관이다.
5. **줄 나눔·합치기 학습.** 여기가 첫 실제 학습이다. 자료가 작아도 되는 작은 모델이고
   CPU로 돈다. 파일럿의 가장 큰 결함(덜 나눈다)을 정면으로 친다.
6. **번역·말맛 LoRA.** 본 게임이지만 **자료가 더 있어야 한다.**

### 하면 안 되는 것 — Whisper 파인튜닝

**자막은 축약본이지 전사가 아니다.** 영어 자막 1768개에 한국어 1692개인데 실제 대사는
그보다 많다. 이것을 ASR 정답으로 학습시키면 **전사가 축약을 배운다** — 규칙 1(전사는
글자 수를 무시한다)을 모델 안에 위반해 넣는 것이다. 전사는 학습이 아니라 **모델 교체**로
개선한다.

### 함정 넷 — 이미 밟아 본 것들

1. **판본을 섞지 마라.** 23.976 판과 24.000 판은 비율이 정확히 1.001이라 **끝에서
   9.05초** 어긋난다. 시작에서는 0.06초라 아무도 못 느낀다. 영화B 폴더의
   `...타사 릴리스.srt`가 실제로 잘못 짝지어져 있다(블루레이 한국어와 바이트까지 같은 파일).
   → 자막은 **그 영상에서 뽑은 것만** 쓴다.
2. **글자 수 규정은 언어마다 다르다.** 한국어 줄당 16자(CJK 1.0·그 외 **0.5**),
   영어 줄당 42자(전부 1.0). 가중치 없이 세면 한국어 최대가 19자로 부풀어 사람 자막을
   열두 번 잘못 지적한다. **`checker/text.py`를 쓴다. 직접 세지 않는다.**
3. **발주처를 모르면 규정을 대지 않는다.** 블루레이는 넷플릭스 납품물이 아니다.
   넷플릭스 잣대를 대면 영어의 12%가 지적된다 — 오류가 아니라 잣대가 다른 것이다.
   `-p`를 안 주면 그 트랙 자체 분포의 상·하위 1%만 이상치로 낸다.
4. **프레임 간격을 위반으로 세지 마라.** 넷플릭스가 2020-07-24에 삭제한 규정이다.
   세었더니 원어 자막의 61%가 지적됐다.

### 규모와 하드웨어

    RTX 3060 Ti 8GB · 디스크 93GB · Ollama에 exaone3.5:7.8b, qwen2.5:7b-instruct

8GB로 7~8B QLoRA(4bit)는 **된다** — 자막은 한 쌍이 짧아 시퀀스가 짧다.

    영화 1편 ≈ 1,100쌍
    LoRA 최소선    5,000쌍  ≈  5편
    쓸 만한 수준  20,000쌍  ≈ 18편
    말맛까지      50,000쌍+ ≈ 45편+

### 사용자에게 받아야 할 것

- **큰 자료의 폴더 위치.** 편수와 언어 구성이 6번의 가능 여부를 정한다.
- **large-v3-turbo를 받아도 되는지**(1.6GB, `models/`에 넣으면 자동으로 잡힌다).
- 한국어·영어 밖 언어의 **공식 규정 문건**이 있는지. 있으면 프로파일로 넣는다.
  없으면 그 언어에는 규정을 대지 않는다(추측해서 만들지 않는다).

---

## 0. 먼저 읽을 것

1. `CLAUDE.md` — 작업 규칙 13개. 새 기능을 붙이기 전에 반드시 읽는다.
2. `checker/pipeline.py` 독스트링 — 단계 순서를 왜 이렇게 정했는지.
3. 이 파일.

**시험을 먼저 돌려 기준선을 잡는다.**

```bash
python3 tests/run_tests.py                                    # 758건 (GUI 제외)
cmd.exe /c "..\korean-subtitle-corrector\.venv\Scripts\python.exe tests\run_tests.py"   # PySide6 포함(더 많다)
```

시스템 `python3`에는 PySide6가 없다. GUI를 건드리면 반드시 venv 쪽으로도 돌린다.

---

## 1. 지금까지 정해진 큰 그림

### 최상위 목표

**전문가가 신뢰하고 쓸 수 있으며 전문가의 시간 낭비를 줄이는 도구.**
우선순위: **자동 교정 오답 0 > 플래그 건수 감소 > 선택적 recall.**
recall 100%는 사전 커버리지가 상한이라 원리적으로 불가능하다.

이 우선순위가 설계 결정을 여러 번 뒤집었다. 예: 다큐 `요` 검사를 줄 단위로 하지
않는 이유가 이것이다(허용되는 것을 위반으로 부르면 오답).

### 프로젝트 둘

| 저장소 | 하는 일 |
|---|---|
| `korean-subtitle-corrector` | 규칙 + 국립국어원 API로 한국어를 교정한다. **AI 안 쓴다**(MQ2에서 실측으로 "쓰지 말라"는 답이 나왔다) |
| `subtitle-tc-generator` (이 저장소) | 전사·번역·타임코드. 교정기를 라이브러리로 부른다(`checker/korean.py`) |

로컬 폴더 이름도 `subtitle-tc-generator`로 맞췄다(2026-08-14). 옛 이름
`subtitle-editor`는 SE 플러그인(`Runner.cs`)과 교정기 `survey.py`가 **대체 경로로
계속 본다** — 옛 설치본을 깨지 않으려는 것이지 되돌린 것이 아니다.

### 단계 모델 (합의된 목표 그림)

```
번역 트랙    ① 생성·1차 번역     정확도 + 타임코드만
             └ 오역 검증          층1 전수 대조 + 층2 의심분 역번역
             ② 감수 (n차, 수렴)  맥락·용어·인물관계·장르 말투 + 한국어 교정기
             ③ 윤문·QA           말맛·압축·글자수·리딩스피드·줄바꿈 + 최종 역번역

한국어 트랙  ① 전사  ② 한국어 교정  ③ 자막 QA
```

---

## 2. 끝난 것 (되돌리지 말 것)

- **`pipeline.py`로 오케스트레이션 일원화.** 네 어댑터(CLI 검사·CLI 생성·GUI·SE
  플러그인)가 모두 `correct_and_check`를 부른다. 선언한 `STAGES`마다 `stage_` 함수가
  있는지 시험이 잰다.
- **검사는 맨 끝.** 원본만 검사하면 교정이 만든 새 위반을 못 본다. 실측 사례가
  시험에 박혀 있다(`3천 불` → `3천 달러`가 글자 수 한계를 넘긴다).
- **`formality.py`** — 말투를 센다. 확정(합니다체·해요체)만 세고 반말은 유보.
- **`characters.py`** — 캐릭터 분석 문서. 자막이 증명하는 것만 채우고 나머지는
  '확인 필요'로 둔다.
- **모델 기본값 한 곳**(`translate.DEFAULT_MODEL`).
- **회차 상한 없음**(`stage_revise(rounds=n)`).
- **`genre.py` + `rules/genre/`** — 장르를 플랫폼 프로파일 **위에 얹는다**. 프롬프트가
  아니라 프로파일 층이어야 검사기가 본다(자료 590행이 장르로 TC 길이를 가른다).
  다큐·드라마·예능 셋. `--genre`로 켠다.
- **`~씨` 검사(G01)** — 다큐·리얼리티에서만. 화자명·낱말(`씨앗`)·지시어(`그 씨`)는
  거른다. 극존칭(G02)은 자료에 정의가 없어 **미구현으로 밝힌다**(규칙 9).
- **권장 표시 시간·합니다체 비율을 리포트에 숫자로** 낸다. 위반 목록에 섞지 않는다 —
  권장은 규정이 아니다(규칙 4·5).
- **`AGENTS.md`** — opencode·Codex는 `AGENTS.md`를 자동으로 읽고 `CLAUDE.md`는 읽지
  않는다. 규칙이 도구에 따라 빠지지 않게 입구를 만들었다.
- **생성 결과를 `.work/`에 남긴다.** 화면에 넘기기 **전에** 남기므로 화면 쪽이 멈춰도
  결과는 살아 있다. 실사용에서 210개를 그렇게 잃었다.
- **UI 실에서 mpv 왕복을 덜어냈다.** `fit_subtitle_scale`을 100ms 시계에서 빼고
  `resizeEvent`로 옮겼다(초당 20번 → 창 크기 바뀔 때만). 멈춰 있으면 시계를 500ms로 늦춘다.
- **단계 메뉴(`단계(&S)`).** 단추만 있으면 키보드로 못 가고 무엇이 있는지 훑을 수도
  없다. `stage_actions`를 만들어 두고 아무 데도 붙이지 않았던 것을 붙였다.
- **단계를 줄기별로 갈라 그린다.** `Stage.track`으로 소재·번역·한국어·조사 네 줄.
  `[① 1차 번역] [② 번역 감수 (회차)] [③ 자막 윤문·QA]`가 눈에 보이고, 줄기마다 직전
  결과 요약이 붙는다. **회차 선택이 화면에 있다.**
- **`stage_polish`·`stage_characters`.** ③은 윤문 -> 한국어 교정 -> 규정 검사를
  **파이프라인이** 잇는다 — 어댑터가 순서를 가지면 CLI와 갈라진다.
- **`work.py` + `.work/` 폴더.** 단계마다 남긴다 — CLI와 GUI 둘 다. 회차마다 따로
  남기므로 회차 사이를 견줄 수 있다. 타임코드는 첫 단계에서 굳고 이후 옮기면 기록에
  경고로 남는다. **남기기가 실패해도 본 작업은 멈추지 않는다.**
- **누적 표류 가드.** `revise(baseline=...)`가 **1차 대비**로도 재므로, 회차마다
  1.4배씩 늘어 원문 대비 2배가 되는 일이 막힌다(실측 2.70배 -> 1.40배).
- **수렴 조건과 멈춘 이유.** `--max-passes`/`--settle-at`. **상한에 걸린 것은 아직 덜
  됐다는 뜻**이라 다 끝난 것과 문구를 갈라 적는다.
- **`backtranslate.py` + `stage_backtranslate` + `--backtranslate`.** 번역을 원어로
  되돌려 원문과 견준다. **임계값을 두지 않는다** — 점수 낮은 순 상위 N개를 낸다
  (판정이 아니라 순위). 줄임말을 펴서 `don't`/`do not`이 어긋나 보이지 않게 한다.
- **`mistranslation.py` 층 1 검증.** 1차 직후 원문↔번역 대조. **넷 중 둘은 확정이
  아니다**(부정·숫자는 추정) — 그래서 전부 플래그로만 내고 규정 위반에 섞지 않는다.
- **프롬프트 세 단계 재배치.** 1차=오역+구조만, 2차=맥락·용어·말투, 3차=문체·간결·부호.
  **규칙이 두 단계에 겹치면 시험이 잡는다.** `DOCUMENTARY_RULES`(죽은 코드)를 지우고
  장르 규칙을 프로파일에서 읽는다(`revise.genre_hint`).
- **캐릭터 시트가 감수 프롬프트에 물린다**(`revise.cast_hint`). 정하고, 쓰고, 검사하는
  것이 한 자료다 — 같은 시트가 T17도 돌린다.
- **T17 `formality_inconsistent` 구현.** 캐릭터 시트의 `말투 지정` 칸과 견준다
  (`--cast`). 시트가 없으면 **`skipped_checks`로 "돌지 못했다"를 밝힌다** — 구현이
  끝나면 미구현 목록에서 빠지므로, 밝히지 않으면 "검사했고 통과"로 읽힌다.
- **`characters.read_tsv()`** — 시트를 되읽는다. **칸 순서가 아니라 머리글로 찾는다**
  (사람이 엑셀에서 칸을 옮긴다). `확인 필요`·`정해 주세요`는 빈 값으로 읽는다.
- **`webchars.py` + `characters.research()` + `--characters`** — 캐릭터 분석 문서에
  성별·나이·직업·경력·성격을 채운다(항목 이름은 작업자 자료가 정했다). 나가는 것은
  **작품 제목과 인물 이름뿐**이고 무엇을 보냈는지 기록한다. 사진은 주소와 라이선스만
  가져오고 동봉 판단은 작업자가 한다.

---

## 3. 남은 작업 — 이 순서로

**T1~T3, T5~T11이 전부 끝났다.** 남은 것은 T4의 절반(임계값 실측)뿐이고, 그것은 자료를
기다리는 일이다. 그래서 **다음 할 일은 §0-A(멈춤 확인)와 §4(결정 대기)에 있다.**

순서에 이유가 있다. 뒤 항목이 앞 항목을 전제로 한다.

### ~~T1. 캐릭터 시트 외부 조사~~ 〔끝남〕

`characters.py`의 `traits`·`photo`·`origin`을 채우는 조사 기능.

- **사용자가 "조회까지 다 만든다"로 결정했다.** 내가 제기한 우려(미공개작 제목
  조회가 작업 사실을 노출, 사진 저작권)를 듣고 재확인한 결정이다.
- **지켜야 할 것**(규칙 6, 이건 사용자가 뒤집지 않았다):
  - 나가는 것은 **작품 제목뿐**. 대사·대본은 어떤 경우에도 나가지 않는다.
  - **무엇을 보냈는지 기록에 남긴다.**
  - 사진은 로컬에 두고 **출처·라이선스를 함께 적는다**. 납품 문서 동봉 여부는
    작업자가 판단한다.
- 참고: `terms.py`의 `research(..., web=True)`가 이미 같은 성격의 외부 조회를
  한다. 그 무늬를 따른다.

**짓다가 잡은 것 둘:**

1. **이름이 정확히 같은 문서를 먼저 고른다.** 검색 순위에 맡기면 `Walter White`를
   찾았는데 `Walter White Jr.`가 1위로 왔다(실측). 아들·아버지처럼 이름이 겹치는
   인물은 작품마다 있고, 잘못 집으면 성격과 말투를 통째로 틀린 사람에게 붙인다.
2. **`{{...}}`를 통째로 지우면 안 된다.** 목록 틀에 싸인 값이 사라져
   `affiliation = {{Plainlist|...}}`가 빈칸이 됐다 — 경력 정보를 통째로 버렸다.
   규칙: 맨 인수는 내용, `키=값` 인수는 메타정보.

**남은 것:** 위키를 사람이 지정해야 한다(`--wiki`). 슬러그를 짐작하지 않기로 한
결정이다 — 엉뚱한 위키를 읽으면 엉뚱한 인물 정보가 나오고, 그건 없는 것보다 나쁘다.

### ~~T2. T17 `formality_inconsistent` 구현~~ 〔끝남〕

프로파일에 규칙으로 선언돼 있고 **미구현**이다(`netflix/ko-translation.yaml` T17,
"같은 관계에서 존댓말·반말이 섞였습니다").

**T1이 먼저인 이유:** 같은 화자가 존댓말과 반말을 섞는 것은 **상대가 다르면
정상**이다. 자막에 상대는 표시되지 않으므로, 관계를 모르는 채 지적하면 오답이 된다.
캐릭터 시트가 관계를 담은 뒤에야 T17이 성립한다.

**어떻게 풀었나.** 반말 판별을 새로 만들지 않았다. 사람이 시트에 "이 인물은 이렇게
말한다"를 적어 주고(`말투 지정`), **확정된 말투만 견준다.**

    정한 것 합니다체 · 이 줄 해요체   -> 어긋남 (둘 다 확정)
    정한 것 반말     · 이 줄 존댓말   -> 어긋남 (존댓말이 확정이므로 한 방향으로 잰다)
    정한 것 무엇이든 · 이 줄 유보     -> 넘긴다
    정해 주지 않은 인물               -> 건드리지 않는다

반말 확정은 여전히 못 한다(`해야` vs `뭐야`). 그래도 위 세 줄로 실제 어긋남은 잡힌다.

프로파일 문구도 실제로 재는 것과 맞췄다("같은 관계에서…" -> "캐릭터 시트에 정한
말투를 벗어났습니다").

### ~~T3. 장르 프로파일 셋~~ 〔끝남〕

`extends` 상속으로 얹는다. **프롬프트가 아니라 프로파일 층**이어야 한다 — 자료
590행이 장르로 **타임코드 길이**를 가르므로 검사기가 봐야 한다.

```
다큐(정통/리얼)  ~씨 금지, 극존칭 금지, TC 3~5초, 합니다체 위주
드라마           ~씨 가능, 극존칭 가능, TC 2~3초        ← 멜로가 여기 들어간다
예능/리얼다큐    TC 2초 이하
```

근거: `rules/sources/작업자-자료/작업 기본 원칙.txt` 146·312·590·658·659·889행.

**만들지 않는 것:**
- **멜로 프로파일** — 일반 대화체 = 드라마 기본값. 빈 프로파일을 늘리지 않는다.
- **느와르 프로파일** — 욕설은 이미 플랫폼 규정이다
  (`censorship.allowed: false` = 검열 금지 = 동등 강도 허용). 장르가 아니라
  **캐릭터**가 정한다 → T1으로 넘긴다.

**`DOCUMENTARY_RULES`(translate.py)는 죽은 코드다.** 정의만 있고 참조가 0건.
장르를 프로파일 층으로 옮기면서 지우거나 프로파일에서 읽게 바꾼다.

### T4. 다큐 합니다체 비율 — 〔절반 끝남: 숫자는 나온다, 임계값은 대기〕

문서·화자 단위로 비율만 낸다. `formality.summary()`가 이미 계산한다.

**줄마다 `요`를 잡으면 안 된다.** 사용자 확인: "다큐는 합니다체 위주이지만
**자연스러움을 위해 가끔씩 요를 사용하는 것은 허용된다**." 허용되는 것을 위반으로
부르면 오답이다.

**임계값을 지어내지 않는다.** "가끔"이 몇 퍼센트인지는 사용자의 **완성본 다큐
자막으로 재야** 안다. 재기 전까지는 숫자만 내고 판정하지 않는다. 근거 없는 임계값은
오답 공장이다(제42항에서 이미 겪은 무늬).

낼 자리: 위반 목록이 아니라 리포트의 별도 칸(`position_suggestions`와 같은 성격 —
추정은 지적하지 않고 따로 낸다, 규칙 4).

### ~~T5. 1차 번역 프롬프트 재배치~~ 〔끝남〕

`translate.SYSTEM`이 1차에 너무 많이 시킨다. 사용자 요구는 "1차는 정확도와
타임코드만".

| 지금 1차에 있는 것 | 가야 할 곳 |
|---|---|
| 마침표 금지, 문장부호 최소화 | ③ 윤문 |
| 인칭대명사 금지 | ③ |
| `수/있/것`·보조용언·`-시-` 덜기 | ③ |
| "불필요한 말은 과감히 뺀다" | ③ |
| 인물 관계 말투 | ② 감수 |
| 한국 문화 과하게 옮기지 않기 | ② |
| 오역 금지 | **① (이것만)** |

`SECOND_PASS`(revise.py)에도 같은 문체 규칙이 중복돼 있다. 규칙은 한 단계에만 둔다.

**주의(그대로 유효):** 1차 결과가 전보다 투박하다. 그게 의도지만 ③까지 안 돌리면
예전보다 나쁘게 보인다. `prefs.translate_passes`는 이미 3이고 GUI 체크박스도 3을
넘긴다. **실제 영상으로 돌려 확인한 적은 없다** — 다음 실사용에서 봐야 한다.

**짓다가 걸린 것:** 2차 프롬프트가 "말투는 1차를 따릅니다"라고 하고 있었다. 1차가
말투를 정하지 않게 되면 **아무도 정하지 않는 상태**가 된다. 그래서 말투를 정하는 것을
2차의 일로 명시했다(1차는 존댓말로 통일만 한다).

### ~~T6. 오역 검증 (층 1)~~ 〔끝남 — 다만 여기 적었던 판단이 틀렸다〕

**정정.** 이 넷을 "확정이므로 자동 지적한다"고 적어 두었는데 **둘은 확정이 아니다.**
짓다가 드러났다.

| 검사 | 실제 |
|---|---|
| 화자 표시 | **확정** — 개수가 다르면 구조가 깨졌다 |
| 용어 | **확정** — 통일표가 정답을 정해 준다 |
| 부정 | **추정** — `I don't know` -> `몰라요`. 부정 표시 없이 부정한다 |
| 숫자 | **추정** — `in 5 minutes` -> `곧`. 자막은 숫자를 버리기도 한다 |

그래서 전부 **플래그로만** 낸다(`mistranslation_flags`). 규정 위반 목록에 섞으면
위반 건수가 거짓이 된다.

**한 음절 한자 수사 함정.** `일 이 사 오 구 십 만`을 수사 목록에 넣었더니 `잠깐만요`가
'숫자 있음'으로 판정되어 **검사가 사실상 돌지 않았다.** 한자 수사는 두 음절 이상이거나
단위가 뒤에 붙을 때만 수사로 본다.

**어느 단계에서 재는지가 중요하다.** 1차 직후에만 뜻이 있다 — 1차는 압축하지 않기로
했으므로 이때 빠진 숫자·부정은 옮기지 않은 것일 가능성이 높다. 3차를 지난 자막에
같은 검사를 걸면 정상적인 압축을 오역으로 부른다.

### ~~T7. 역번역 검증~~ 〔끝남 — 여기 적었던 설계에 순환이 있었다〕

**역번역이 답인 이유는 읽기 편해서가 아니다 — 비교가 같은 언어끼리 되기 때문이다.**
영·한을 직접 견주려면 다국어 임베딩이 필요하고(torch ~2GB, exe가 불어난다) 점수만
나와서 무엇이 틀렸는지 못 본다. 역번역을 거치면 의존성 없이 낱말 겹침으로 재고,
사람에게 보일 근거가 그 자체로 남는다.

**정정.** "층1을 통과했지만 낱말 겹침이 낮은 자막만 역번역한다"고 적어 두었는데
**그건 순환이다** — 겹침은 역번역을 해야 계산된다. 역번역 전에는 어느 자막이 의심스러운지
알 방법이 없다. 그래서 **전수로 돌리고, 점수가 나온 뒤에 고른다.**

```
#42  원문    I never said I'd go alone
     한국어  혼자 가겠다고 했어요
     역번역  I said I would go alone        <- never가 사라졌다  (75%)
```

**임계값을 두지 않았다.** 자르는 것은 점수가 아니라 개수다(`--backtranslate-worst`,
기본 20) — 몇 점 이하가 오역인지는 실제 작업물로 재야 알고, 재기 전에 박으면 오답
공장이 된다. 다큐 합니다체 비율에서 한 판단과 같다.

**줄임말을 펴야 했다.** 안 펴면 원문 `don't`와 역번역 `do not`이 어긋난 것으로 보인다 —
**가장 중요한 신호인 부정이 바로 그 무늬**라 그냥 두면 못 쓴다. `n't`를 일반 규칙으로
먼저 걸면 `can't`가 `ca not`이 되어 또 어긋난다(구체적인 것을 앞에 둔다).

**아직 안 한 것:** 3차 끝에서만 부른다(CLI `--backtranslate`). 1차 직후에도 부를 수
있으나 회차가 하나 더 늘어 시간이 두 배가 되므로, 실사용에서 값을 재 본 뒤 정한다.

### ~~T8. 차수 수렴 조건~~ 〔끝남〕

"완벽해지는 지점까지"를 기계가 알아야 멈춘다. **모델이 고치기를 멈추는 것으로는
안 된다** — LLM은 미사여구를 영원히 만진다.

```
멈춘다 = 층1 오역 플래그 0건
       AND 규정 위반 0건 (글자 수·리딩스피드·줄바꿈)
       AND 이번 차수의 변경이 임계 이하
       OR  차수 상한 도달 (시간 보호)
```

**멈춘 이유를 기록한다.** "5차에서 멈췄다"만으로는 완성인지 상한에 걸린 건지 모른다.

~~알려진 위험~~ **막았다.** `revise(baseline=...)`로 1차 대비를 함께 잰다. 실측:
가드 없이 3회차 돌면 2.70배, 가드 켜면 1.40배에서 멈춘다. 되돌릴 때는 **1차가 아니라
직전을 지킨다** — 중간 회차가 제대로 고친 것을 버릴 이유가 없다.

**수렴은 "고친 개수"로 끊는다.** 모델이 만지기를 멈추는 것을 기다리면 안 된다(LLM은
미사여구를 영원히 만진다). `rounds`가 최소, `max_rounds`가 상한이고 그 사이에서
`settle_at` 이하로 잠잠해지면 멈춘다. **최소 회차는 반드시 채운다** — 2차를 안 돌면
오역·용어를 아무도 보지 않는다.

**아직 안 넣은 조건:** 규정 위반 0건, 오역 플래그 0건. 회차마다 검사를 돌리는 것은
공짜지만(LLM 안 씀) 실사용에서 값을 재 본 뒤 넣는다 — 위반이 0이 되지 않는 자막도
있어서 그것만으로 끊으면 늘 상한까지 돈다.

### ~~T9. `.work/` 중간 산출물 + manifest~~ 〔끝남〕

지금 CLI는 `.source.srt`/`.draft.srt`/`.notes.srt`/`.review.srt`/`.ko.srt`/`.fixed.srt`로
파편적이고, **GUI는 아무것도 남기지 않는다**(메모리에서만 돈다). 15분 돌린 번역이
3차에서 깨지면 처음부터다.

```
<영상이름>.work/
    manifest.json          단계별: 모델, 시각, 소요, 바뀐 줄, 위반, 멈춘 이유
    01-generate.srt        타임코드 확정본 (이후 전 단계에서 불변)
    01-source.json         번호→원문 (감수가 오역 보려면 필수)
    02-first.srt
    03-revise-1.srt        회차마다 별도 파일
    03-revise-2.srt
    04-polish.srt
```

지킬 것: 원본을 덮어쓰지 않는다(규칙 7), `01-generate.srt`의 타임코드는 이후 불변
(각 단계 끝에 `--lock-timecodes` 검증), 전부 로컬.

실제로 나오는 것:

```
ep01.work/
    manifest.json      단계별 모델·시각·걸린 시간·바뀐 줄·역할
    01-source.json     번호별 원문
    02-first.srt
    03-revise-2차.srt
    03-revise-3차.srt
```

`Work.last()`가 마지막 단계와 그 자막을 준다 — **여기서 이어 하면 된다.**
`work.diff(a, b)`로 회차를 견준다.

**아직 안 한 것:** 이어 하기를 UI/CLI 명령으로 노출하지 않았다. 지금은 `.work/` 안의
`.srt`를 열면 되지만, `--resume` 같은 것이 있으면 더 낫다. 그리고 `stage_generate`
경로(전사)는 아직 남기지 않는다 — 번역이 가장 비싼 자리라 거기부터 했다.

### ~~T10. UI에 단계 노출~~ 〔끝남〕

`STAGES`에 `track` 필드를 추가해 번역 트랙과 한국어 트랙을 가른다. `Stage.available()`
/`_refresh_stages()`/`_stage_guard()` 기계장치는 이미 있으니 **데이터만 늘리면
버튼이 생긴다.**

둘 다 넣었다. 실제 모습:

```
소재     [자막 만들기]
번역     [① 1차 번역] [② 번역 감수] 회차[2] [③ 자막 윤문·QA]
         ① 1차 번역 끝 · 자막 2개 · 확인이 필요한 자리 1곳 → 다음은 [② 번역 감수]
한국어   [② 한국어 교정] [③ 자막 QA]
조사     [용어표] [캐릭터 문서]
```

**1차 번역이 이제 1차만 한다.** 전에는 체크박스 하나로 1~3차를 한꺼번에 돌려 단계
사이에 검토할 자리가 없었다.

**아직 안 한 것:** 도구 모음의 `만들 때 번역까지` 체크박스는 남겼다(생성과 번역을
한 번에 하고 싶은 경우가 실제로 있다). 줄기를 `종류`(sdh/translation)에 따라 숨기지
않았다 — 한국어 자막인데 종류가 `translation`인 경우가 있어 짐작하면 틀린다.

### ~~T11. 편집 기능 정리~~ 〔조사 끝 — **지울 것이 없었다**〕

여기 후보로 적어 둔 둘을 실제로 열어 보니 **둘 다 지울 근거가 없다.** 추측으로 적은
것이었고, 코드에 이미 근거가 적혀 있었다.

| 후보 | 열어 보니 |
|---|---|
| 단축키 17개 | 하나하나 "생성물 손보기"에 쓰인다. 처음 판단대로 전부 남긴다 |
| **레이아웃 메뉴** | SE 흉내가 아니다. 코드 주석: "타임코드를 잡을 때는 파형이, 번역을 다듬을 때는 표가 커야 한다" — 작업별 필요다 |
| **단축키 재지정 UI** | 자막 작업자는 SE에서 온 손버릇이 있다. 전문가 도구에서 키를 못 바꾸는 것이 오히려 손해다 |

사용자 경고가 맞았다: "단순화를 위해 유용한 편집 기능을 빼지 말라."

**대신 한 것:** 죽은 코드를 추측이 아니라 세어 봤다. 참조 스캔은 데코레이터 등록
(`@check("...")`)과 문자열 사전 디스패치를 놓쳐 노이즈가 대부분이라 **쓸 수 없었다.**
믿을 수 있는 신호(아무도 import하지 않는 모듈)만 보니 하나가 나왔다 — 아래 §4.

---

## 4. 결정 대기 / 확인 필요

- **`checker/mixer.py`가 고아다.** 아무도 import하지 않고 시험도 문서도 없다. 지우기
  전에 물어야 한다 — 독스트링에 **아직 안 해 본 좋은 설계**가 담겨 있다(VAD 정확도를
  재려면 정답이 필요한데, 깨끗한 말소리에 음악·잡음을 **섞으면** 정답이 공짜로 나온다.
  우리 휴리스틱에서 정답을 뽑는 순환이 아니다).
  다만 `runner.py`에서 배운 것이 있다: **아무도 안 쓰는 파일은 자기 목적을 못 한다.**
  ~~쓸지 지울지 정해야 한다.~~ **2026-08-14 사용자 결정: 일단 남긴다.** 지우는 것은
  되돌리기 쉽지만 저 설계를 다시 떠올리는 것은 어렵다.

- ~~**`dist/자막편집기/`와 옛 데스크톱 바로가기**~~ 〔끝남〕 2026-08-14에 지웠다
  (547MB 회수). 데스크톱에 `자막편집기.lnk`는 이미 없었고, `자막생성기.lnk`는 새
  폴더 이름으로 고쳤다. **`%APPDATA%\자막편집기\`는 건드리지 않았다** — 전사 모델
  1.5GB와 설정이 거기 있고, `paths.py::user_root`가 그 폴더를 계속 본다.
- **`ensure_server()`의 실제 실행 경로 미검증** — 확인할 때 서버가 이미 돌고 있었고,
  사용자의 Ollama를 죽여서 시험하지 않았다.
- **다큐 합니다체 임계값** — 사용자의 완성본 다큐 자막이 필요하다(T4).
- ~~exe 재빌드~~ 22:23과 22:35에 두 번 했다. 새 모듈 일곱 개와 `rules/genre/`가
  실렸는지 빌드 TOC로 확인했다.
- **멈춤(AppHangB1)이 정말 고쳐졌는지는 확인하지 못했다.** Qt 쪽은 재서 빠른 것을
  확인했고(전부 10ms 미만) UI 실의 mpv 왕복을 덜어냈지만, **막힌 지점을 증명하지는
  못했다** — mpv가 붙은 실제 기계에서 다시 돌려 봐야 한다. 다음 실사용이 시험이다.

## 5. 교정기 쪽 미결 (다른 저장소)

사용자가 아직 고르지 않은 후속:
- 뜻풀이 가드에 `document_context` 물리기
- tests → 평가셋 추출 (78+112건)
- 말뭉치 라이선스 확인
- 문서 단위 골드 평가셋 (초벌 vs 완성본, 규범/문체 라벨 분리)

---

## 6. 되풀이하지 말 것 (실제로 당한 것들)

- **`python3 tests/run_tests.py > /dev/null && git commit`** 꼴로 잇는다. 파이프
  (`| tail`)는 종료 코드를 가리고 세미콜론(`;`)은 실패해도 다음 줄을 실행한다.
  둘 다로 실패한 테스트를 커밋한 적이 있다 — **세 번**이다.
- **시험 결과 출력은 파일 맨 끝에 둔다.** 앞에 두었다가 마지막 세 건이 실패해도
  보고되지 않는 상태로 한동안 있었다.
- **cmd.exe에 한글 인수를 직접 넘기지 않는다.** `--input` JSON 파일, `-F` 메시지
  파일, heredoc으로 쓴 `.py`를 쓴다.
- **평가셋은 문장까지 그대로 옮긴다.** 로그의 판정을 내가 지어낸 문장에 옮겨 붙여
  가짜 정답을 만든 적이 있다(사용자가 잡았다).
- **`%APPDATA%\자막편집기\` 폴더명과 `SUBTITLE_EDITOR_HOME` 환경변수는 공개
  계약이라 이름을 바꾸지 않는다.** 바꿨다가 시험 2건이 깨졌다.
- **자료가 없는 프로파일·임계값을 추측해서 만들지 않는다.**
- **시험이 사용자의 앱 로그에 쓴다.** `%APPDATA%\자막편집기\log.txt`에 `_Tiny 시작`,
  `배치: spotting` 같은 줄이 있으면 시험이 남긴 것이다 — 실사용 기록으로 오해하면
  엉뚱한 것을 조사한다. 실제로 두 번 헛짚었다.
- **로그를 두 곳에서 쓰지 않는다.** `Job.say`(일하는 실)와 `_note`(UI 실)가 같은 줄을
  각각 남겨 로그가 두 벌로 섞였다. 멈춤 조사에서 잘못 읽을 뻔했다. 지금은 일하는
  실에서만 남긴다(`_note_ui`가 화면 전용).
