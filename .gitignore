.venv/
__pycache__/
.omc/
bin/
obj/
node_modules/

*.fixed.srt
checker-report.txt

# 아이콘에 끌어다 놓다가 폴더로 떨어진 자막이 커밋되지 않게 한다.
tools/*.srt
tools/*.vtt

# 대용량 미디어는 저장소에 넣지 않는다. 자료 폴더의 영상은 로컬에만 둔다.
*.mp4
*.mkv
*.mov

# 영상·모델 실험용 임시 폴더
.tmp/
