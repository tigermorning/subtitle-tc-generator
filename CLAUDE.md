# 자막 및 TC 생성기 — 작업 규칙

이 파일은 **다시 설명하지 않기 위해** 있다. 아래 것들은 이미 한 번씩 틀렸다가
사용자 지적으로 바로잡은 것들이다. 새 기능을 붙일 때 여기부터 읽는다.

## 0. "우리 교정기"는 하나가 아니다 — 리포 경로부터 확인한다

"교정기"라는 말이 가리키는 프로젝트가 **셋** 있다. 하나로 착각해 다른 리포 코드로
답한 적이 있다(2026-08-28, 업로드 파일 크기 제한 질문 — 이 리포에는 없는 기능인데
있다고 잘못 단정했다가, 실제로는 옆 리포에 있는 실재 기능이었다).

    이 리포(지금 여기)   C:\Users\user\Documents\subtitle-tc-generator
                        자막 및 TC 생성기. 로컬 데스크톱(PySide6) + CLI.
                        전사·대조·번역·재분할·글자수 검사 전 과정. 업로드 API 없음
                        (QFileDialog로 로컬 파일 직접 연다 — 크기 제한 개념 자체가 없다)

    별개 리포 1          C:\Users\user\Documents\korean-subtitle-corrector
                        한국어 맞춤법·띄어쓰기 교정기. FastAPI 웹 서버(subtitle_corrector/api.py)
                        + 정적 프런트(static/index.html). 업로드 크기 제한 **실재**:
                        일반 파일 1MB, PDF 30MB(api.py의 _MAX_UPLOAD_BYTES·_MAX_PDF_BYTES)

    별개 리포 2          C:\Users\user\Documents\korean-corrector-guide-bot
                        위 korean-subtitle-corrector의 사용법을 안내하는 RAG 챗봇.
                        코드가 아니라 **문서 봇**이다 — data/chunks/*.json을 BM25+임베딩
                        하이브리드 검색(lib/search.js)으로 인용해 답한다. 스크린샷으로
                        보이는 "칩 + 점수" UI가 이 봇이다

**"교정기"라고만 말하면 먼저 어느 리포인지 물어보거나, 질문 안의 단서(업로드 API,
CLI `main.py`, PySide6 창, 재분할·스포팅 등)로 구분한다.** 확신 없이 이 리포 코드만
보고 "그런 기능 없다"고 단정하지 않는다 — 없는 게 아니라 다른 리포에 있을 수 있다.

## 1. 단계 순서를 섞지 않는다

    ⓪ 발주처 확인  이 영상이 지금 어느 OTT에 상영 중인지부터 파악한다   (아래 참고)
    ① 전사        글자 수를 무시하고 자연스럽게        (transcribe.py)
    ② 대조        스크립트와 맞춰 텍스트를 정한다      (align.py)
    ③ 번역        원어 -> 한국어                       (translate.py)
    ④ 재분할      의미 단위로 끊고 스포팅 재배치        (resplit.py)
    ⑤ 글자 수     읽기 속도·줄 수·길이를 맞춘다         (timing.py, checks.py)

**글자 수는 마지막이다.** whisper의 `max_len`으로 전사 단계에서 자르면 문장이
부서진 채로 굳는다. 사람이 하는 순서가 옳다 — 먼저 다 받아 적고, 그다음에 나눈다.

**번역이 재분할보다 먼저다.** 원어 기준으로 끊어 놓으면 한국어가 거기 갇힌다.

**발주처 확인이 모든 것보다 먼저다**(사용자 지시, 2026-08-30, 직접 인용):
"내가 앞서 말했듯이 네가 모든 작업에 앞서서 가장 먼저 해야 하는 것은 네가
분석하는 영상이 어느 OTT에 지금 상영이 되고 있는지, 그래서 어떤 발주처의
규칙을 따라야 하는지를 먼저 파악해야 하는 일이야. 그다음에 TC 작업 들어가고,
그다음에 TC 작업 맞춰서 번역에 들어가야 돼."

**왜**: 규칙 5·11이 이미 말하듯 정답(규정)은 발주처마다 다르고, 발주처가 규정을
바꾸면 "정답"도 바뀐다. 어느 발주처 기준을 따라야 하는지 모른 채 TC·번역에
들어가면, 나중에 발주처가 밝혀져도 이미 만든 결과물이 그 발주처 규정과 안 맞을
수 있다 — 순서를 거꾸로 밟은 대가를 나중에 전부 다시 하는 걸로 치른다.

**발주처 식별은 3단계다**(사용자 정정, 2026-08-30 — 이전에 이 파일이 3단계 중
1·2단계를 빠뜨리고 3단계만 "발주처를 못 찾으면"으로 뭉뚱그려 적어 놓은 것을
사용자가 다시 설명해 바로잡음):

    ① 파일명에 발주처가 적혀 있으면    그걸 따른다
       (예: 릴리즈 태그 DSNP=디즈니+, NF=넷플릭스)
    ② 파일명에 없으면                 이 영상이 지금 어느 OTT에서 방영 중인지
                                      인터넷에서 찾는다
    ③ 복수 OTT에서 동시 방영 중이면   정답지(truth)를 먼저 보고, 그 정답지의
                                      관행(스포팅 범위·병합 방식·호칭 규칙 등)이
                                      `rules/<platform>/`의 어느 프로파일과
                                      맞는지 역으로 추적해 어느 발주처 규칙인지 정한다
                                      (규칙 5 하단과 같은 방법)

③은 "발주처를 못 찾을 때" 전반에 쓰는 방법이 아니라, **복수 OTT 동시 방영처럼
①·②로는 하나로 못 좁히는 경우**에 한정해서 쓴다. ①·②로 이미 하나로 정해지면
③은 필요 없다.

## 2. 원어에 한국어 규정을 적용하지 않는다

16자·읽기 속도는 **한국어 기준**이다. 원어 스크립트는 번역을 위한 작업 재료일 뿐
납품물이 아니다. 원어의 글자 수나 스포팅을 맞추느라 시간을 쓰면 헛일이다.
중요한 것은 번역본의 스포팅과 글자 수다.

## 3. 어느 쪽도 정답으로 두지 않는다

전사가 틀리기도 하고 스크립트가 틀리기도 한다(즉흥 대사 누락, 촬영 중 대사 변경,
미완성 대본). 둘을 **대조**하고, 어긋난 자리는 기계가 정하지 않고 **표시**한다.

같은 원리가 곳곳에 적용된다:

- 소리를 못 찾은 스크립트 줄은 **지우지 않고** 길이 0으로 남긴다.
- 번역을 끝내 못 하면 빈칸이 아니라 **원문**을 남긴다(빈칸은 지나치지만 원문은 눈에 띈다).
- 표기 통일표를 어겨도 **고치지 않고 표시**한다(문맥상 안 쓰는 것이 맞을 때가 있다).

## 4. 확실한 근거와 추정을 구분한다

    파일 안에서 알 수 있다   -> 검사하고 자동으로 고친다
    영상에서 추정한다        -> 알리기만 하고 고치지 않는다

말소리 구간(silencedetect), 장면 전환, 화면 아래 글자 검출이 모두 추정이다.
추정으로 자동 교정을 하면, 틀렸을 때 사람이 원인을 찾지 못한다.

## 5. 규정은 절대적 정답이 아니다

발주처가 요구하는 대로 따르는 것이 작업자의 일이다. 그래서 프로파일이 상속되고
(`extends`), 에이전시 기준이 플랫폼 공식 기준을 덮어쓸 수 있다.

- 공식 문건에서 온 것은 `source.official: true`, 실무 자료는 `source.client`로 구분한다.
- SDH 가이드와 번역 가이드는 **따로** 둔다. 규칙이 다르고 섞이면 지적이 뒤집힌다.
- 가이드는 자주 개정된다. 근거를 넣을 때 개정일을 함께 적는다.
  (예: 넷플릭스는 프레임 간격 규정을 2020-07-24에 **삭제**했다. SE는 아직 강제한다.)

## 6. 미공개 자료는 밖으로 내보내지 않는다

방송 전 영상과 대본은 유출되면 계약이 깨지는 물건이다. 번역 API·클라우드 전사에
붙이면 편하지만 **작업자가 쓸 수 없다.** 전사는 ffmpeg 내장 whisper, 번역은 로컬
Ollama로만 한다. 네트워크 포트도 함부로 열지 않는다(WSL에서 Ollama를 부를 때
`0.0.0.0` 대신 명령 호출을 쓰는 이유).

## 7. 원본을 덮어쓰지 않는다

교정 결과는 항상 새 파일로 나간다. 자동 교정이 틀렸을 때 원본이 없으면 손해가
회복 불가능하다. SE 플러그인도 같은 이유로 **[SE에 반영]을 누르기 전에는** 자막을
바꾸지 않는다.

## 8. 받은 타임코드는 절대 건드리지 않는다

TC 작업이 끝난 파일을 받아 번역·교정만 하는 경우가 실무에서 가장 흔하다
(작업자 자료 `작업 기본 원칙` 190행: "TC 작업이 되어 온 파일에 내가 번역만 한
경우는 TC를 절대 건드리면 안 됨!").

`--lock-timecodes`를 켜면:

- 수렴·스포팅·**재분할을 모두 막는다**(나누면 경계가 새로 생긴다 = 건드린 것이다)
- `--fix-timing`·`--fix-spotting`과 함께 쓰면 **오류로 막는다**. 조용히 무시하면
  사람은 고정된 줄 알고 기계는 옮긴다 — 둘 중 더 나쁜 쪽이다
- 결과를 **쓰기 전에** 정말 안 움직였는지 확인하고, 움직였으면 쓰지 않는다

칸에 들어가지 않는 번역은 자막을 나누는 대신 **검사가 잡아** 사람이 문장을 줄인다.

## 9. 완료를 주장하기 전에

- **테스트가 0으로 끝날 때만 커밋한다.** `python3 tests/run_tests.py > /dev/null &&
  git commit ...` 꼴로 잇는다. 파이프(`| tail`)는 종료 코드를 가리고, 세미콜론(`;`)은
  실패해도 다음 줄을 실행한다. 둘 다로 실패한 테스트를 커밋한 적이 있다 —
  **세 번**이다. "파이프 없이"만 적어 두었더니 세미콜론으로 같은 일을 했다.
  **2026-08-14부터 이 규칙은 글이 아니라 기계다** — `tools/hooks/pre-commit`이
  커밋 직전에 시험을 돌리고, 실패하면 커밋이 만들어지지 않는다. 명령을 어떻게 잇든
  상관없다. 새 클론·워크트리에서는 한 번 켠다: `git config core.hooksPath tools/hooks`.
  훅이 도는 것은 747건(시스템 파이썬)이다. `app/`을 건드렸으면 PySide6까지 도는
  776건은 사람이 venv로 따로 돌린다 — 훅이 알려는 주지만 막지는 않는다.
- 검사기가 "미구현"이라고 말하는 항목은 미구현으로 남겨 둔다. 조용히 통과시키지 않는다.
- 자료가 없는 프로파일은 **추측해서 만들지 않는다.** 디즈니·쿠팡 번역 프로파일은
  오래 비어 있다가, "번역 자막에서 달라지는 것은 화면자막 표식과 겹침 처리뿐이고
  화자명·어조 표기는 SDH와 같다"는 작업자 지적을 받고서야 만들었다 — 없던 값을
  지어낸 것이 아니라 같은 값을 상속한 것이다. 여전히 `source.official: false`다.

## 10. Windows에서 도는 것을 잊지 않는다

개발은 WSL, 사용자는 Windows다. 실제로 여기서 세 번 막혔다.

- 배치 파일 본문은 **ASCII만**. cmd가 콘솔 코드페이지로 바이트 단위 파싱을 해서
  한글이 섞이면 파싱이 깨진다.
- ffmpeg 출력은 **UTF-8로 디코딩을 못 박는다**. Windows 파이썬 기본은 cp949라
  깨진 스트림이 `None`으로 돌아와 엉뚱한 자리에서 터진다.
- ffmpeg 필터 옵션에 **절대 경로를 쓸 수 없다**(`C:`의 콜론이 옵션 구분자).
  작업 폴더를 잡고 상대 경로로 부른다.

## 11. 학습이 규정을 건드리지 않는다

정답 자막을 모아 값을 배우기로 했다(백로그 §0-B). **배운 값이 `rules/`를 고쳐서는
안 된다.** 사용자가 못 박았다(2026-08-14).

이유는 규칙 5와 같다. `rules/` 아래 값은 **공식 문건이나 작업자 자료에서 온 것**이고
`source.official`로 출처를 구분한다. 자료 몇 편에서 관측한 숫자가 그것을 덮어쓰면,
어느 값이 규정이고 어느 값이 관측인지 아무도 구분하지 못하게 된다. 발주처와 다투는
자리에서 근거를 댈 수 없다는 뜻이다.

층을 셋으로 가른다.

    규정   rules/<플랫폼>/   문건에서 온 값. 사람이 문건 근거로만 고친다
    학습   rules/learned/    자료에서 관측한 값. 디렉터리부터 갈라 둔다
    코드                     그 둘이 없을 때의 기본값

학습값에는 출처를 단다. 어디서 몇 편으로 언제 잰 값인지 없으면 나중에 그 값을 믿을지
말지 정할 수 없다.

**forced 자막은 어떤 영상에서든 학습하지 않는다**(사용자 지시, 2026-08-25). 전체
대사가 아니라 화면에 다른 언어가 나올 때만 뜨는 부분 자막이라, 반쪽짜리를 완성
번역·SDH로 오인하게 된다. `tools/corpus_build.py`의 `pick()`이 트랙 제목에 `forced`가
있으면 고르지 않는다 — 예외 없음.

**정답지를 우리가 손대 재구성하지 않는다**(사용자 지시, 2026-08-25). 자료 형식이
이상해서(예: 일본어 롤업 자막 — 한 줄씩 밀려 올라가는 방송 자막 방식이라 시간이
겹치는 조각이 수백 개로 쪼개져 나온다) 원래 모양으로 되돌리려면 겹치는 구간을
어느 쪽에 붙일지 **판단을 넣어야 하는 경우**가 있다. 그 판단이 아무리 근거
있어 보여도 우리가 추정으로 채운 값이지 정답지에서 읽은 값이 아니다. 지금은
학습 단계라 이런 자료는 **재구성하지 않고 그냥 못 쓰는 것으로 둔다** — 학습
자료는 사람이 만들어 확정한 값만 옮기지, 기계가 다시 지어낸 값을 옮기지 않는다.

    source:
      official: false
      origin: learned
      from: {영화: 12, 자막: 19_400}
      measured: 2026-08-14

**규정은 범위를 정하고, 학습은 그 범위 안에서 고른다.** 넷플릭스 한국어는 노출을
833~7000ms로 정하지 그 사이 어디를 고르라고는 말하지 않는다. 생성기가 2750ms를 고른
것(사람은 1752ms)은 규정 위반이 아니라 **규정이 비워 둔 자리를 잘못 채운 것**이다.
학습이 들어갈 자리가 정확히 거기다.

그래서 순서가 정해진다 — **규정이 값을 정한 항목에서는 학습값을 아예 보지 않는다.**
충돌하면 규정이 이기는 것이 아니라, 애초에 겨루지 않는다.

### 규정이 되는 길은 열려 있다 — 다만 사람을 거친다

학습값이 규정으로 굳을 수 있다(사용자 확인, 2026-08-14). 막는 것은 승격이 아니라
**섞임**이다. 그래서 길을 하나만 둔다.

**새 규정을 발견하면 사람에게 보고하고 확인을 받는다.** 기계가 옮기지 않는다.
자료가 일관되게 가리키더라도, 그것이 규정인지 이 작품들의 습관인지는 사람이 정한다.

보고할 때 함께 낸다:

    무엇을 발견했나        관측한 값과 지금 값의 차이
    몇 편 몇 개에서 나왔나  한 작품에서만 나온 것은 규정이 아니라 그 작품의 습관이다
    어긋나는 사례          가리키지 않는 자료도 함께 낸다. 유리한 것만 내지 않는다
    어느 발주처·언어인가    언어를 넘어 옮기지 않는다(규칙 2)

승격해도 **`origin: learned`는 지우지 않는다.** 어디서 온 값인지가 남아야 나중에
발주처와 다투는 자리에서 무엇을 근거로 댈 수 있는지 알 수 있다. 공식 문건이 나오면
그때 `official: true`가 되고, 그전까지는 관측이다.

관측이 기존 규정과 어긋날 때는 승격이 아니다. 그것은 **지적**이다(규칙 3 — 어느 쪽도
정답으로 두지 않고 표시한다). 문건을 이기는 것은 문건뿐이다.

### 학습 자료끼리 서로 어긋날 때 — 최신 작품을 우선한다

같은 발주처 자료인데 작품마다 관측값이 다를 수 있다. 습관 차이일 수도 있지만,
**그사이 규정이 개정됐을 가능성**도 있다(규칙 5 — 가이드는 자주 개정된다. 넷플릭스
프레임 간격 규정이 2020-07-24에 삭제된 것처럼). 사용자 지시(2026-08-25): 자막
자료끼리 모순이 있으면 **더 최신 작품 쪽을 맞는 것으로 본다.**

- 그래서 코퍼스에 작품마다 **공개(방영/출시) 시점**을 함께 적어 둔다 — 시점이 없으면
  어느 쪽이 최신인지 가릴 수 없다.
- "최신을 맞는 것으로 본다"는 그 값을 규정으로 승격한다는 뜻이 아니다. 여전히
  `origin: learned`이고, 여전히 사람 확인을 거쳐야 규정이 된다(위 승격 경로). 다만
  **관측값끼리 고를 때는** 오래된 쪽보다 최신 쪽에 무게를 싣는다.
- 최신이 하나뿐이고 나머지가 여럿이어도 최신을 이긴 것으로 보지 않는다 — 여러 작품이
  가리키는 값과 최신 작품 하나가 가리키는 값이 다르면, 그것도 **지적**감이지 자동
  채택감이 아니다. 사람에게 보고할 때 "몇 편 몇 개"와 "가장 최신이 어느 쪽인지"를
  같이 낸다.

### 사용자 실무 자료를 공식 문서처럼 취급한다 — 단, 갱신 가능성을 열어 둔다

사용자 지시(2026-08-26): `rules/sources/작업자-자료/`(구글독스 5건 — 작업 기본
원칙·표기_맞춤법_번역·SDH 효과음 예시·SubtitleEdit·작업 준비 및 기본 규칙)는
**지금 시점에는 공식 문서처럼 간주하고 쓴다.** "미확보"라고 답하기 전에 이 자료부터
뒤진다([[feedback_check_sources_before_claiming_gap]]). 원본 구글독스 링크와
재추출 방법은 `rules/sources/작업자-자료/반영-계획.md` 하단에 있다 — 파일을
못 찾겠다고 답하기 전에 거기서 링크를 확인해 재추출한다.

- `source.official: false` / `source.client` 표기는 **지우지 않는다.** 이 구분이
  있어야 나중에 "그때는 사용자 자료가 최선이었다"는 근거를 댈 수 있다(규칙 5·11과
  같은 이유).
- **코퍼스 학습값이 이 자료와 어긋나면, 비교 기준은 "작품 연도 vs 자료 기준
  시점"이다.** 사용자 예시(2026-08-26): 작업자 자료는 2026년 1월 시점 규정을
  담고 있다.
  - 학습하는 작품이 **자료 기준 시점보다 옛날**(예: 2005년 드라마)이면 **자료를
    따른다** — 옛날 작품은 그 시절 관행일 뿐, 지금 규정이 그때부터 있었다는
    증거가 아니다. 규정 개정 신호로 보지 않는다.
  - 학습하는 작품이 **자료 기준 시점보다 나중**(예: 2027년 작품)인데 어긋나면
    **그사이 공식 규정이 바뀐 것으로 보고 그 값으로 규정을 고친다.**
  - 그래서 정답지마다 **작품 연도**와 **자료 기준 시점**을 함께 봐야 한다 — 최신
    작품끼리의 상대 비교(위 문단)와는 별개로, 항상 이 절대 기준선도 확인한다.
- **그래서 학습하는 영상이 몇 년도 작품인지가 계속 중요하다.** 정답지를 학습할 때
  작품의 공개(방영/출시) 연도를 반드시 같이 적어 둔다(이미 규칙이지만 여기서
  다시 강조한다) — 연도가 없으면 "실무 자료가 낡았다"는 판단 자체를 할 수 없다.
- 사용자가 준 것 말고 다른 구글독스나 자료가 더 있는지는 사용자만 안다. 이 네
  파일 안에서 확인이 끝났으면 "없다"고 하지 말고 "이 자료 안에는 없다, 자료가
  더 있느냐"고 묻는다.

**공식 규정 취급 범위 — 넷플릭스·쿠팡·디즈니뿐이다**(사용자 지시, 2026-08-28):
"내가 적어 준 넷플릭스, 쿠팡, 디즈니가 공식 규정이다. 그 외의 발주처는 공식적인
규정집이 없더라도 원문 영상을 보고 역으로 유추해 낼 수 있다."

- 이 세 발주처는 사용자가 준 자료(공식 문건 + 위 작업자 자료 4건)를 공식 규정으로
  간주한다. **다른 발주처를 위 세 곳과 같은 층으로 취급하지 않는다** — 없는
  자료를 있는 것처럼 대하면 규칙 9를 어긴다.
- 다른 발주처는 문서화된 규정집이 없어도 된다. 그 대신 **원문 영상(과 확정
  자막)을 직접 보고 규칙을 거꾸로 유추**하는 것이 허용된다 — 이때 관측값은
  `origin: learned`로 남기고(규칙 11과 같은 방식), 몇 편·몇 개에서 관측했는지
  근거를 붙인다. 유추한 값을 공식 규정인 것처럼 적지 않는다.

## 12. 학습은 한 번에 몰아 하지 않는다 — 놓치지 않는 것이 빠른 것보다 먼저다

정답 자료를 학습시킬 때 한 번에 다 반영하려다 놓치는 일이 실제로 있었다. 사용자가
절대규칙으로 못 박았다(2026-08-25): **정확히 다 훑는 것이 빠르게 끝내는 것보다
우선한다.** 시간이 걸려도 놓치는 것보다 낫다.

- 큰 자료(수백~수천 행)는 **정독 방식으로 나눠서** 본다 — `작업 기본 원칙.txt`
  (2,076행)를 9차에 걸쳐 정독한 것과 같은 방식이다. 한 회차가 다룬 범위와 안 다룬
  범위를 분명히 남긴다(예: `67/127`). 회차를 건너뛰어 "대충 훑고 다 봤다"고 하지 않는다.
- **갈래를 동시에 벌이지 않는다.** 코퍼스 추출·문서 정독·코드 수정처럼 성격이 다른
  작업을 한꺼번에 벌이면 어느 것도 끝까지 확인하지 못한 채 다음 것으로 넘어가게
  된다. 한 갈래를 끝내고 확인받은 뒤 다음으로 넘어간다.
- **"다 됐습니다"라고 보고하기 전에 정말 전부 봤는지 스스로 확인한다.** 자료의 일부만
  보고 전체를 봤다고 보고하지 않는다 — 안 본 범위가 있으면 그 범위를 남겨서 말한다.

## 13. 정답지 학습 — 목적·경계·절차 (2026-08-30 전면 정리)

이 프로젝트가 최종적으로 만들려는 것: TC 작업자·SDH 작업자·번역 작업자·감수자
여럿이 붙어야 나오는 완성본을, **정답지 없는 실전에서도** 전문가가 감수만 하면
될 만큼 스스로 만들어내는 도구. 정답지 대조는 그 목표를 이루는 **유일한 훈련
수단**이다 — 정답지 없이 만든 초안은 어디가 틀렸는지 아무도 모른 채 납품되지만,
정답지가 있을 때만 "생성기가 구조적으로 이렇게 틀린다"가 드러나고, 그 원인을
코드로 고치면 정답지 없는 다음 작업에도 그대로 적용된다.

### 목적: 구성 방식을 배우는 것 — 텍스트를 옮겨 적는 것이 아니다

"정답지를 학습한다"는 **정답이 어떻게 구성됐는지(패턴)**를 코드 규칙으로
뽑아내는 것이다. 정답 텍스트 자체를 우리 결과물에 그대로 옮기는 것과는
다른 일이다 — 그건 완성된 답 하나를 저장해 두는 것이지, "왜 그렇게
끊었는지·왜 그 타이밍인지"를 도구가 스스로 재현하게 만드는 게 아니다.
정답 하나를 베끼면 그 정답 하나에만 통하고, **여러 정답에서 공통된 규칙을
뽑아 코드에 넣어야** 정답지 없는 다음 영상에도 통한다. 그래서:

- **고치는 대상은 코드다, 내 판단력이 아니다.** 세션이 끝나면 판단력은
  안 남는다([[project_learning_workflow]]) — "대조하며 감이 는다"는 이
  프로젝트에 없는 경로다. 코드(`checker/`)에 남긴 것만 다음 세션에 남는다.
- **정답지 있는 영상은 "완성본을 만들어 전달"할 필요가 없다.** 정답지
  자체가 이미 완성본이다(`학습한 TC 및 자막 모음/`). 값어치 있는 산출물은
  `checker/` 코드 고침·`rules/` 실측값뿐이다. 2026-08-28~29 세션이 예능A
  15·16회 초안을 3차 윤문까지 입혀 "완성본"으로 전달한 것은 헛수고였다
  (`docs/AGENT_INCIDENTS.md` 5번). 정답지 있는 언어·방향에는 생성·윤문·전달을
  하지 않는다 — `--generate`+`--against`로 도구를 검증·수정하는 것까지만.
- **정답 파일 전체를 다른 이름으로 복제해 두지 않는다**(2026-08-30). "우리
  결과를 정답과 100% 같게 만들어 달라"는 요청도, 그 결과가 정답 파일을
  통째로 복제하는 것이라면 하지 않는다 — 완성물 전체 복제는 목적(구성
  방식을 코드로 뽑는 것)에 안 맞고, 다음 영상에 전혀 도움이 안 된다.
  `--against`가 내는 통계(짝지음·유사도)로 "얼마나 가까운가"를 확인하는
  것까지가 검증이다.
- **정답지를 우리가 손대 재구성하지 않는다**(규칙 11 하단과 같음). 대조로
  찾은 것을 코드에 반영하되, 정답지 자체를 고치거나 학습 자료에 섞지
  않는다. `학습한 TC 및 자막 모음/`에는 사람이 확정한 원본만 남는다.
- **대조 대상은 항상 "오늘 생성한 것 vs 정답지"뿐이다.** 어제 만든 결과·
  과거 버전·"플래그를 껐을 때 vs 켰을 때" 같은 자체 대조(ablation)는
  설계하지 않는다 — 다듬은 횟수 같은 다른 변수가 섞여 원인을 못 가른다
  (2026-08-30 예능A 15회 영어 번역에서 실제로 겪음). "이 값이 저 값보다
  나은가"를 묻지 않고 "정답과 어디가 다른가"만 묻는다.
- 정답지 확정 후 영상을 지우기 **전에** 아래 B층 진단을 거친다 — 영상이
  없으면 다시 못 한다. 이미 지운 것(드라마A, 예능B,
  영화A, 드라마C 등)은 되돌릴 수 없다.

### 두 층 — 하나가 다른 하나를 대신 못 한다, 순서 고정

    A. 정답지-학습     정답 텍스트·타이밍만 재서 통계·관행(노출시간·글자수·
       (패턴만 본다)    호칭·병합 간격 등)을 뽑는다. `tools/corpus_build.py`.
                       영상·GPU 필요 없음, 비용 거의 0.
    B. 정답지-대조-진단  내가 blind로(정답 안 보고) 만든 결과와 정답을
       (내 코드를 본다)  대조해 **내 코드의 처리 결함**을 찾는다. whisper
                       전사가 필요해 비용이 크다(영상 하나에 몇 분~몇십 분).

A는 "목표가 뭔지"만 알려주고, **B만이 "내가 왜 그 목표를 못 맞추는지"를
알려준다.** 순수 텍스트만 봐서는 알 수 없는 코드 결함(영어 약어 오판·
반응 압축 누락·병합 상한 누락 등)은 전부 "내 코드가 실제로 만든 결과"와
정답을 나란히 놓고 봐야만 드러난다.

**A가 항상 먼저다. 예외 없음.** 콘텐츠 종류가 새것이든 익숙하든 상관없이,
정답지 있는 영상을 받으면 무조건 A(`정답지-학습`)부터 한다 — "내가 먼저
blind로 작업해서 결과를 정답과 비교하는" 순서로 시작하지 않는다
([[feedback_learn_answer_key_first_no_blind_draft]]). B(whisper 필요·비용
큼)로 넘어가는 건 A가 끝난 뒤, **이 코드가 아직 이런 콘텐츠 종류를 안
겪어봤을 때**만, 그리고 **넘어가기 전에 사람에게 필요성을 확인받는다** —
스스로 판단해 조용히 B까지 들어가지 않는다. 매번 B까지 풀로 돌리면
하루짜리 일이 며칠로 늘어난다.

### "성숙" 판정 — 장르 전체 vs 영상 한 편, 층위가 다르다

같은 발주처+장르 조합에서 **연속 3편(N=3)**을 B로 대조했는데 새 구조
버그가 0건이면 그 조합은 "성숙" 후보다. `docs/corpus_status.yaml`의
`genre_maturity`에 발주처/장르별로 몇 편째인지·새 버그가 나왔는지를 기록한다.

- N=3은 안전판이 아니라 실용적 문턱이다(B 비용이 커서 낮춰 잡음) — 드문
  패턴(노래 반복 환각, 반응컷 편집 등)을 3편 안에 다 봤다는 보장은 없다.
  그래서 "성숙"에 닿아도 **자동으로 A만으로 전환하지 않는다** — 규칙 11의
  승격 경로와 같은 방식으로 **사람 확인 뒤에만** `matured: true`로 바꾼다.
- `matured: true` 뒤에도 새 구조 이상이 보이면 `clean_streak`을 0으로
  되돌리고 그 영상부터 다시 B로 돌아간다 — 한 번 성숙 판정을 받았다고
  영구히 안전한 게 아니다.
- 이건 **장르 전체**에 대한 기준이다. **영상 한 편 안에서** 진단 라운드를
  몇 번 더 돌릴지는 다른 층위의 기준(`정답지-대조-진단` 스킬 6절 —
  "정답지에서 배울 걸 배웠으면 넘어간다")이 따로 있다. 둘을 섞지 않는다.

## 14. 오역·번역 오류를 보면 맥락 부패부터 먼저 의심한다

번역 결과가 틀렸을 때 원문·문법·용어 문제로 바로 들어가지 않는다. **먼저 맥락 부패
(context rot) 여부를 판단한다** — 대화·세션이 길어지면서 신호가 희석돼 틀린 것인지,
아니면 원래부터 틀린 것인지 순서를 지킨다. 출처: LangChain, *Context Engineering
for Agents*.

    poisoning(오염)     앞선 생성의 추정이 검증 없이 사실처럼 다음 턴에 인용됨
    distraction(주의분산) 핵심 조항이 있는데 무관한 주변 설명이 답을 차지함
    confusion(혼동)      비슷한 근거가 섞여 적용 범위·시점·대상이 흐려짐
    clash(충돌)          서로 다른 지시·근거가 양립하지 않은 채 답에 함께 반영됨

판단 절차:

    1. 지금 낸 답의 각 사실을 원문·1차 자료와 대조한다(poisoning — 이전 생성물이
       아니라 원본과 맞춰야 한다)
    2. 선택 전후로 핵심 조항 인용이 바뀌었는지 비교한다(distraction)
    3. 비슷한 근거 사이 범위·시점·대상이 뒤섞이지 않았는지 검사한다(confusion)
    4. 충돌하는 지시·근거가 있으면 목록으로 만들고 우선순위·최신성·권한으로
       판정한 근거를 남긴다(clash)

맥락 부패로 판정되면: 원문 재검증(오염), 무관한 정보 제외(주의분산), 근거의 적용
범위·시점·대상 명시(혼동), 우선순위 판정 근거 기록(충돌) — 이 처방을 먼저 적용한
뒤에도 오류가 남으면 그때 규칙 3(어느 쪽도 정답으로 두지 않고 표시)·규칙 4(확실한
근거와 추정을 구분)로 넘어간다. 맥락 부패 확인을 건너뛰고 바로 규칙·용어집 탓으로
돌리지 않는다.

**대조는 매번 원문에서 직접 한다, 이전 대조 결과를 근거로 삼지 않는다.** 대조
횟수를 늘리는 것 자체는 안전하고 오히려 권장한다 — 매번 원문을 다시 보면 못 본
것을 새로 잡아낼 수 있다. 위험한 것은 두 번째 대조가 원문 대신 첫 번째 대조의
결과(내가 방금 낸 답·요약)를 근거로 삼는 경우다. 그러면 대조가 아니라 카피의
카피를 뜨는 것과 같다 — 뜰수록 원본에서 멀어진다. 그 자체가 오염(poisoning)의
정의와 같다: 이전 생성물이 검증 없이 사실처럼 다음 턴에 재입력되는 것.

## 15. 정답지 학습 순서는 TC가 무조건 먼저다

사용자 지시(2026-08-27): **TC를 완벽하게 잡아 놓고 그다음에 자막(텍스트) 작업을
한다.** 생성 파이프라인의 순서(규칙 1 — 전사→대조→번역→재분할→글자 수)와는
다른, **정답지 학습(규칙 13) 안에서의 순서**다.

TC가 성기게 잡힌 채로 텍스트 손교정(단어 선택·고유명사 등)을 먼저 파고들면
헛일이 된다 — TC를 나중에 고치면 자막 경계가 다시 생기고, 텍스트 교정도 그
경계에 맞춰 다시 해야 한다. 그래서:

1. 초안을 만들면 **먼저 TC(재분할·스포팅 단위)만 정답과 맞춘다.** 정답지가
   한 화면에 담는 분량, 화자 턴이 바뀌는 자리에서 끊는지를 확인하고,
   구조적으로 어긋나면 코드(`regroup.py`·`resplit.py`·`timing.py`)부터 고친다.
2. TC가 정답과 맞아떨어진 뒤에야 자막 텍스트(번역·SDH 표현) 손교정으로 넘어간다.
3. 실측(2026-08-27, 예능A 15회 영어 번역 1453개 전수 대조): 텍스트 차이의
   상당수가 사실 TC 문제였다 — 우리 초안 한 자막이 정답 자막 2~4개 분량을
   화자가 바뀌어도 이어 붙이고 있었다(`regroup.py`의 `MAX_GAP_MS=250`이 화자
   교체를 못 본다). 이런 자리를 텍스트만 보고 고치면 TC가 여전히 성긴 채로
   "번역이 어색하다"고 오판하게 된다.

**같은 세션 안에서도 두 갈래를 섞지 않는다**(사용자 재확인, 2026-08-28):
"TC와 자막 작업을 동시에 하지 말고, TC를 100% 맞춰 놓은 뒤 자막 작업을 하는
것으로 박아 둬. TC가 계속 바뀌면 자막도 계속 바뀔 수밖에 없어." 규칙 12의
"갈래를 동시에 벌이지 않는다"와 같은 이유지만, 여기서는 특히 **TC 갈래가
자막 갈래보다 먼저 끝나야 한다**는 순서까지 못 박은 것이다. 한 회차 안에서
TC 버그(`regroup.py`·`resplit.py`·`timing.py`)와 텍스트 버그(`translate.py`·
`revise.py`)를 번갈아 고치면, TC가 나중에 또 바뀔 때 방금 고친 텍스트 경계가
다시 흔들린다 — 손해를 두 번 본다. **TC가 정답과 완전히 맞아떨어졌다고
확인하기 전에는 텍스트 손교정에 들어가지 않는다.**

`.claude/skills/정답지-대조-진단/SKILL.md`에도 이 순서를 반영한다.

## 16. 자막 작업 전에 작품 배경부터 공부한다

사용자 지시(2026-08-29): "작품을 받으면 인물들과 인물 관계 등에 대한 배경
공부를 미리 하고 번역이나 SDH 작업에 들어가야 한다. 화자명을 모른다는 것은
작품에 대한 배경지식이 없이 작업했다는 뜻이다. TC 작업은 내용을 몰라도
괜찮을 수도 있지만 자막 작업은 배경지식이 필수다."

**TC와 자막이 요구하는 지식이 다르다** — 규칙 15가 "TC 먼저, 자막 나중"으로
순서를 나눈 것과 같은 결로, 이번엔 **필요 지식의 종류**를 나눈다.

    TC     목소리 시작·끝만 본다. 내용을 몰라도 된다.
    자막   누가 말하는지·인물 관계·존반·호칭·용어를 정확히 옮겨야 한다.
           내용·배경지식 없이는 못 한다.

**그래서 자막(번역·SDH) 작업에 들어가기 전에 한다**(실존 작품 — 다큐·예능·
시사 등 — 이라 웹에서 조사할 수 있는 경우):

1. 출연진·등장인물 이름과 얼굴(가능하면), 서로의 관계·호칭
2. 작품의 기본 성격(예능/드라마/다큐), 방영 시점, 이전 시즌·회차의 맥락
3. 화자 분리(`--diarize`)가 "1번·2번 화자가 다르다"까지만 안다는 한계를
   이 배경지식으로 어느 정도 메운다 — 이름까지는 못 채워도, 이 시점에
   누가 나올 가능성이 있는지는 알고 들어갈 수 있다

**TC 단계에서 이미 이 조사를 건너뛰고 화자명을 못 채운 경우**, 자막
단계로 넘어가기 전에 지금이라도 조사한다 — "화자명을 모른다"는 배경조사
누락의 증상이지, 원래 못 채우는 게 정상인 게 아니다(규칙 3의 "소리를 못
찾은 자리는 표시만 한다"와는 다른 이야기다 — 그건 소리 자체가 없는
경우고, 이건 소리는 있는데 누구 목소리인지 조사를 안 한 경우다).

**허구 작품(비공개 대본)은 조사할 자료가 없을 수 있다** — 그때는 영상
안에서 얻을 수 있는 단서(자기소개, 서로 부르는 호칭)로 채우고, 그래도
안 되면 규칙 3대로 표시만 한다. 조사 자체를 건너뛰지 않는 것이 핵심이지,
조사로 전부 채워야 한다는 뜻은 아니다.

## 17. 손대기 전에 이미 답이 있는지부터 본다

2026-08-29 하루에 같은 성격의 사고가 세 번 겹쳤다: ① 이미
`rules/genre/variety.yaml`에 고쳐져 있던 값(2026-08-27)을 안 보고
whisper 재전사·gap 스윕부터 시작해 같은 결론을 다시 만들어냄, ②
`SendUserFile`로 이미 전달한 완성본(3차 윤문 끝난 파일)을 "손교정 안 된
초안"으로 오판함, ③ `--genre variety` 플래그를 2세션 연속 빠뜨림. 셋 다
원인이 같다 — **근거가 이미 파일에 있는데 그걸 안 보고 새로 판단했다.**
(네 번째 사고 — 사용자가 말하지 않은 내용을 "사용자 지시"로 기억한 것 —
까지 포함한 상세 기록은 `docs/AGENT_INCIDENTS.md`.)

"다음엔 안 잊는다"는 다짐은 세션이 끝나면 사라진다([[project_learning_workflow]]
— 판단은 안 남고 파일에 박은 것만 남는다). 그래서 이 규칙은 "기억하라"가
아니라 **"손대기 전에 아래 셋을 먼저 열어봐라"**다:

1. `docs/HANDOFF.md` 6절("그동안 찾아 고친 구조적 버그") — 증상이 이미
   기록됐는가
2. `docs/corpus_status.yaml`에서 해당 작품/회차 — 이미 끝난 단계인가
   (`done: true`면 그 증거부터 확인하고, 정말 다시 할 이유가 없으면
   재실행하지 않는다)
3. 이 프로젝트 메모리(특히 `feedback_*` 계열) — 같은 종류 실수를 이미
   지적받았는가

셋 다 확인한 뒤에도 답이 없을 때만 새로 판단한다. **판단이 끝나면 그
판단이 위 세 곳 중 맞는 자리에 남았는지 확인하고 세션을 마친다** —
다음 세션이 또 처음부터 찾지 않도록. 코퍼스(영상) 작업뿐 아니라 일반
코드 수정·규정 질문에도 똑같이 적용한다 — `docs/corpus_status.yaml`은
그중 한 확인 지점일 뿐이다.

**다섯 번째 사고**(2026-08-30): `[습작] SDH+번역/`에 새로 들어온 메이드
인 코리아를 보고 "정답지가 없다"고 단정한 뒤 whisper로 5회차를 통째로
생성했다. 나중에 확인해 보니 mkv 안에 **디즈니 정식 한국어 SDH 자막
트랙이 이미 있었다**(화자명·언어 표시까지 붙은 완전한 트랙, E01~02는
덤으로 부분 트랙까지). `학습한 TC 및 자막 모음/`에 추출본이 없다는 것과
영상 자체에 자막 트랙이 없다는 것은 다른 이야기인데 앞의 것만 보고
뒤의 것을 판단했다 — 이것도 "이미 답이 있는데 안 보고 새로 만든" 같은
사고다. `.claude/skills/정답지-학습/SKILL.md`가 정확히 이 경우("새 정답
영상이 들어왔을 때")를 위해 이미 있었는데 호출하지 않았다.

이번엔 글로만 안 남긴다(위 문단이 이미 말하듯 다짐은 세션을 못 넘긴다).
**`checker --generate`가 영상을 열기 전에 자막 스트림을 스스로 세어
경고한다**(`checker/media.py`의 `list_subtitle_streams`, 2026-08-30
추가) — whisper부터 돌리려 해도 "이 영상에 자막 스트림이 이미 N개
있습니다"가 먼저 뜬다. 규칙 9의 pre-commit 훅과 같은 방식이다: 기억에
맡기지 않고 기계가 알린다. 그래도 사람은 이 경고가 뜨면 무시하고
넘어가지 말고 `정답지-학습` 스킬부터 돌려 확인한다.

**새 영상이 손에 들어오면(코퍼스든 아니든) 파일을 열기 전에 순서가
있다**: ① 이 파일이 `[습작] SDH+번역/`처럼 정답 영상이 놓이는 자리에
있는가 -> 있으면 `정답지-학습` 스킬을 먼저 켠다(스킬이 1단계로 컨테이너
자막 스트림부터 본다). ② 그 스킬을 안 쓰기로 했어도 `--generate`나
`--dry-run`을 돌리면 위 경고가 자동으로 뜬다 — 떴는데 무시하고 진행하지
않는다.

## 18. 주요 diff가 생기면 PR 생성 여부를 먼저 확인한다

이 프로젝트에 국한된 규칙이 아니라 `tigermorning/my-skills` 저장소의
`CLAUDE.md`에 있는 개인 작업 규칙을 그대로 옮긴 것이다. Claude Code는
기본적으로 사용자가 명시적으로 요청하지 않으면 PR을 만들지 않는다. 이
기본값 자체는 유지하되, 다음 조건을 만족하면 작업을 마친 시점에 "PR을
만들까요?"라고 먼저 물어본다:

- 여러 파일에 걸친 기능 추가/버그 수정처럼 리뷰할 가치가 있는 규모의 diff가
  만들어졌고,
- 해당 작업에 대해 PR을 만들지 말라는 명시적 지시가 없었던 경우.

사용자가 동의한 뒤에만 PR을 생성한다 — diff가 커 보인다는 이유만으로 확인 없이
바로 PR을 만들지는 않는다. PR 생성은 다른 사람에게도 보이고 되돌리기 번거로운
행동이라, "필요해 보이면 자동 생성" 대신 "필요해 보이면 먼저 제안"을 기본값으로
삼는다.

사소한 변경(오타 수정, 한 줄짜리 수정, 설정값 조정 등)에는 이 규칙을 적용하지
않는다 — 매번 물어보면 오히려 번거롭다.
