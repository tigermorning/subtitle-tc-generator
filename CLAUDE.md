# 자막 편집기 — 작업 규칙

이 파일은 **다시 설명하지 않기 위해** 있다. 아래 것들은 이미 한 번씩 틀렸다가
사용자 지적으로 바로잡은 것들이다. 새 기능을 붙일 때 여기부터 읽는다.

## 1. 단계 순서를 섞지 않는다

    ① 전사        글자 수를 무시하고 자연스럽게        (transcribe.py)
    ② 대조        스크립트와 맞춰 텍스트를 정한다      (align.py)
    ③ 번역        원어 -> 한국어                       (translate.py)
    ④ 재분할      의미 단위로 끊고 스포팅 재배치        (resplit.py)
    ⑤ 글자 수     읽기 속도·줄 수·길이를 맞춘다         (timing.py, checks.py)

**글자 수는 마지막이다.** whisper의 `max_len`으로 전사 단계에서 자르면 문장이
부서진 채로 굳는다. 사람이 하는 순서가 옳다 — 먼저 다 받아 적고, 그다음에 나눈다.

**번역이 재분할보다 먼저다.** 원어 기준으로 끊어 놓으면 한국어가 거기 갇힌다.

## 2. 원어에 한국어 규정을 적용하지 않는다

16자·읽기 속도는 **한국어 기준**이다. 원어 스크립트는 번역을 위한 작업 재료일 뿐
납품물이 아니다. 원어의 글자 수나 스포팅을 맞추느라 시간을 쓰면 헛일이다.
중요한 것은 번역본의 스포팅과 글자 수다.

## 3. 어느 쪽도 정답으로 두지 않는다

전사가 틀리기도 하고 스크립트가 틀리기도 한다(즉흥 대사 누락, 촬영 중 대사 변경,
미완성 대본). 둘을 **대조**하고, 어긋난 자리는 기계가 정하지 않고 **표시**한다.

같은 원리가 곳곳에 적용된다:

- 소리를 못 찾은 스크립트 줄은 **지우지 않고** 길이 0으로 남긴다.
- 번역을 끝내 못 하면 빈칸이 아니라 **원문**을 남긴다(빈칸은 지나치지만 원문은 눈에 띈다).
- 표기 통일표를 어겨도 **고치지 않고 표시**한다(문맥상 안 쓰는 것이 맞을 때가 있다).

## 4. 확실한 근거와 추정을 구분한다

    파일 안에서 알 수 있다   -> 검사하고 자동으로 고친다
    영상에서 추정한다        -> 알리기만 하고 고치지 않는다

말소리 구간(silencedetect), 장면 전환, 화면 아래 글자 검출이 모두 추정이다.
추정으로 자동 교정을 하면, 틀렸을 때 사람이 원인을 찾지 못한다.

## 5. 규정은 절대적 정답이 아니다

발주처가 요구하는 대로 따르는 것이 작업자의 일이다. 그래서 프로파일이 상속되고
(`extends`), 에이전시 기준이 플랫폼 공식 기준을 덮어쓸 수 있다.

- 공식 문건에서 온 것은 `source.official: true`, 실무 자료는 `source.client`로 구분한다.
- SDH 가이드와 번역 가이드는 **따로** 둔다. 규칙이 다르고 섞이면 지적이 뒤집힌다.
- 가이드는 자주 개정된다. 근거를 넣을 때 개정일을 함께 적는다.
  (예: 넷플릭스는 프레임 간격 규정을 2020-07-24에 **삭제**했다. SE는 아직 강제한다.)

## 6. 미공개 자료는 밖으로 내보내지 않는다

방송 전 영상과 대본은 유출되면 계약이 깨지는 물건이다. 번역 API·클라우드 전사에
붙이면 편하지만 **작업자가 쓸 수 없다.** 전사는 ffmpeg 내장 whisper, 번역은 로컬
Ollama로만 한다. 네트워크 포트도 함부로 열지 않는다(WSL에서 Ollama를 부를 때
`0.0.0.0` 대신 명령 호출을 쓰는 이유).

## 7. 원본을 덮어쓰지 않는다

교정 결과는 항상 새 파일로 나간다. 자동 교정이 틀렸을 때 원본이 없으면 손해가
회복 불가능하다. SE 플러그인도 같은 이유로 **[SE에 반영]을 누르기 전에는** 자막을
바꾸지 않는다.

## 8. 받은 타임코드는 절대 건드리지 않는다

TC 작업이 끝난 파일을 받아 번역·교정만 하는 경우가 실무에서 가장 흔하다
(작업자 자료 `작업 기본 원칙` 190행: "TC 작업이 되어 온 파일에 내가 번역만 한
경우는 TC를 절대 건드리면 안 됨!").

`--lock-timecodes`를 켜면:

- 수렴·스포팅·**재분할을 모두 막는다**(나누면 경계가 새로 생긴다 = 건드린 것이다)
- `--fix-timing`·`--fix-spotting`과 함께 쓰면 **오류로 막는다**. 조용히 무시하면
  사람은 고정된 줄 알고 기계는 옮긴다 — 둘 중 더 나쁜 쪽이다
- 결과를 **쓰기 전에** 정말 안 움직였는지 확인하고, 움직였으면 쓰지 않는다

칸에 들어가지 않는 번역은 자막을 나누는 대신 **검사가 잡아** 사람이 문장을 줄인다.

## 9. 완료를 주장하기 전에

- 테스트는 **파이프 없이** 돌린다. `python3 tests/run_tests.py | tail -1`은 종료
  코드를 가려서, 실패한 채로 커밋한 적이 있다.
- 검사기가 "미구현"이라고 말하는 항목은 미구현으로 남겨 둔다. 조용히 통과시키지 않는다.
- 자료가 없는 프로파일은 **추측해서 만들지 않는다**(디즈니·쿠팡 번역 프로파일이
  아직 없는 이유).

## 10. Windows에서 도는 것을 잊지 않는다

개발은 WSL, 사용자는 Windows다. 실제로 여기서 세 번 막혔다.

- 배치 파일 본문은 **ASCII만**. cmd가 콘솔 코드페이지로 바이트 단위 파싱을 해서
  한글이 섞이면 파싱이 깨진다.
- ffmpeg 출력은 **UTF-8로 디코딩을 못 박는다**. Windows 파이썬 기본은 cp949라
  깨진 스트림이 `None`으로 돌아와 엉뚱한 자리에서 터진다.
- ffmpeg 필터 옵션에 **절대 경로를 쓸 수 없다**(`C:`의 콜론이 옵션 구분자).
  작업 폴더를 잡고 상대 경로로 부른다.
